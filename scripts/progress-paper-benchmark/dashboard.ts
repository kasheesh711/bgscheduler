/** Offline report; all displayed values come from captured benchmark records. */
import { readFile, writeFile, mkdir } from "node:fs/promises";
import { root } from "./fixtures";

async function main() {
  const cohorts = await Promise.all([root, `${root}/images-1600`].map(async dir => JSON.parse(await readFile(`${dir}/report/results.json`, "utf8"))));
  let codex = null;
  try { codex = JSON.parse(await readFile(`${root}/codex-app-images-1600/report/results.json`, "utf8")); } catch {}
  const rows = cohorts.flatMap((c, index) => c.rows.map((r: Record<string, unknown>) => ({ ...r, inputMode: index ? "images" : "pdf" })));
  const font = (await readFile("public/brand/progress-tests/fonts/space-grotesk-latin-600-normal.woff2")).toString("base64");
  const body = (await readFile("public/brand/progress-tests/fonts/sarabun-latin-400-normal.woff2")).toString("base64");
  const data = JSON.stringify({ rows, codex: codex ? { rows: codex.rows, runs: codex.runs, planned: codex.plannedRuns, cliVersion: codex.manifest.cliVersion } : null, completed: cohorts.reduce((s,c)=>s+c.runs,0), expected:279, measuredCost:cohorts.reduce((s,c)=>s+c.totalCostUsd,0), unknown:cohorts.reduce((s,c)=>s+c.unknownCostRequests,0),accountRejections:cohorts.reduce((s,c)=>s+c.accountRejections,0) }).replace(/</g,"\\u003c");
  const html = `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Progress Tests model comparison</title>
<style>
@font-face{font-family:Head;src:url(data:font/woff2;base64,${font})}@font-face{font-family:Body;src:url(data:font/woff2;base64,${body})}
:root{color-scheme:light;--bg:#f5f7fb;--panel:#fff;--ink:#16203a;--muted:#526176;--line:#dbe2ec;--blue:#126dce}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:16px/1.55 Body,Arial,sans-serif}main{max-width:1240px;margin:auto;padding:36px 24px 72px}h1,h2,h3,strong{font-family:Head,Arial,sans-serif}h1{font-size:38px;line-height:1.15;max-width:850px;margin:12px 0}h2{font-size:23px;margin:0 0 12px}p{margin:10px 0}.eyebrow{color:var(--blue);text-transform:uppercase;letter-spacing:2px;font-size:12px}.mast{border-top:4px solid #ff7518;padding-top:24px}.muted,small{color:var(--muted)}.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin:28px 0}.card,.panel{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:22px}.card strong{display:block;font-size:29px;margin:8px 0}.panel{margin-top:20px}.filters{display:flex;flex-wrap:wrap;gap:18px;margin:20px 0}label{display:flex;flex-direction:column;gap:6px;font-size:13px;color:var(--muted)}select,input{font:inherit;color:var(--ink);background:var(--panel);border:1px solid var(--line);padding:10px 12px;border-radius:7px;max-width:100%}input{width:135px}a{color:var(--blue)}button{background:var(--blue);color:white;border:0;border-radius:7px;padding:10px 15px;cursor:pointer}button:focus-visible,input:focus-visible,select:focus-visible,a:focus-visible{outline:3px solid #ff7518;outline-offset:3px}.table{overflow:auto}table{border-collapse:collapse;width:100%;font-size:14px;white-space:nowrap}th,td{padding:12px 10px;border-bottom:1px solid var(--line);text-align:right}th:first-child,td:first-child,th:nth-child(2),td:nth-child(2){text-align:left}th{font-size:12px;color:var(--muted)}svg{display:block;width:100%;height:auto;max-height:480px}.legend{display:flex;gap:20px;flex-wrap:wrap;font-size:13px}.dot{display:inline-block;width:10px;height:10px;border-radius:50%;margin-right:7px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:20px}.callout{border-left:3px solid #ff7518;padding-left:16px}.mono{font-variant-numeric:tabular-nums}.warning{color:#94430b}.method{font-size:14px}details{margin:14px 0}summary{cursor:pointer}.metric-note{font-size:13px;max-width:980px}
@media(max-width:700px){main{padding:24px 14px}.cards,.grid{grid-template-columns:1fr}h1{font-size:29px}.card{padding:16px}.card strong{font-size:24px}.panel{padding:16px}.filters label{flex:1;min-width:140px}}
@media(prefers-color-scheme:dark){:root{color-scheme:dark;--bg:#10182a;--panel:#19243a;--ink:#f1f5fc;--muted:#b0bdd1;--line:#34415a;--blue:#80baff}.warning{color:#ffc289}}
@media print{body{background:white}.filters,button{display:none}.panel{break-inside:avoid}main{padding:0}.cards{margin:18px 0}}
</style>
<main><header class="mast"><div class="eyebrow">BeGifted · Progress Tests</div><h1>What does reliable paper formatting cost?</h1><p class="muted">GPT‑5.6 Luna, Terra, Sol and GPT‑6 Astra · measured on 13 September 2026</p><p id="completion" class="callout" role="status"></p></header>
<div class="cards"><div class="card">Captured API test requests<strong id="runs"></strong><small>Three documents, repeated runs</small></div><div class="card">Known matrix API cost<strong id="spend"></strong><small id="unknown"></small></div><div class="card">PDF build time<strong id="renderTime"></strong><small>Median local render, excluding model time</small></div></div>
<section class="panel"><h2>What the completed tests show</h2><p><strong>Astra low preserved the most source content.</strong> It retained the subpart marks that Sol omitted, but two of its three long-paper Codex trials timed out. Sol none/low are cheaper candidates for the next formatter iteration; the measured omissions still need correction.</p><p>Luna was reliable on the short fixtures at low and medium effort, but all six long-paper repeats had source errors. Higher effort did not consistently improve value.</p><p class="callout">A successful PDF build is not proof that a paper is ready for students. Final scores include visible subpart marks and supersede interim perfect Sol scores. Manual review also found insufficient working space, which the numerical source score does not measure. No production model switch or deployment followed these tests.</p></section>
${codex ? `<section class="panel"><h2>Continued testing inside Codex</h2><p id="codexStatus" class="callout" role="status"></p><p>These runs use the signed-in desktop app runtime and Codex allowance. They make no API-key requests. The same source papers, image size, formatting instructions, schema, source checks and BeGifted renderer are used across models within this experiment.</p><div class="filters"><label>Codex source paper<select id="codexFixture" aria-label="Codex source paper"><option value="all">All three documents</option><option value="typed-key">Typed paper + marking key</option><option value="scan-draft">Scanned paper, no key</option><option value="regression-17">17-page regression paper</option></select></label></div><div class="table"><table><thead><tr><th>Requested model</th><th>Effort</th><th>Runs</th><th>Content + PDF passes</th><th>Response / source / PDF errors</th><th>Source checks</th><th>Median Codex time</th><th>Mean output tokens</th><th>Reasoning tokens included</th></tr></thead><tbody id="codexTable"></tbody></table></div><p class="metric-note muted">Low and medium receive three repeats per source; high, xhigh and max receive one. Complete outputs are scored against the source, with a 300-second capture limit. Tokens and source-check means weight document types equally. Codex adds its own system instructions, image handling, output limit and transport behavior. Its time includes startup and cannot be treated as website latency. Capture timeouts stop at 300 seconds; completion time, usage and quality then remain unknown. No API dollar price is inferred from these runs.</p><p><button id="codexCsv">Download Codex comparison CSV</button></p></section>` : ""}
<section class="panel"><h2>Compare the trade-offs</h2><div class="filters"><label>Input format<select id="mode" aria-label="Input format"><option value="pdf">Visual PDF · all effort levels</option><option value="images">Page images · low and medium</option></select></label><label>Source paper<select id="fixture" aria-label="Source paper"><option value="all">All three documents</option><option value="typed-key">Typed paper + marking key</option><option value="scan-draft">Scanned paper, no key</option><option value="regression-17">17-page regression paper</option></select></label><label>Cost basis<select id="cost" aria-label="Cost basis"><option value="new">New paper, no discounts + write premium</option><option value="cold">Standard uncached input</option><option value="observed">Observed with actual caching</option></select></label><label>Success measure<select id="success" aria-label="Success measure"><option value="strict">Complete PDF + fidelity + under 110s</option><option value="content">Complete PDF + fidelity, any duration</option></select></label></div>
<div class="legend"><span><i class="dot" style="background:#126dce"></i>Luna</span><span><i class="dot" style="background:#237e5c"></i>Terra</span><span><i class="dot" style="background:#bf641e"></i>Sol</span><span><i class="dot" style="background:#7957b7"></i>Astra</span></div><svg id="chart" viewBox="0 0 1100 450" role="img" aria-label="Cost per paper versus proportion of test runs passing all selected checks. Full values are in the table below."></svg><p class="metric-note muted">Higher points passed more tests; points farther left cost less. The horizontal axis is logarithmic. The all-documents view weights the three document types equally, since repeat counts differ. Hover a point for its effort level. These are source-preservation checks, not a universal intelligence or grading score.</p>
<div class="table"><table><thead><tr><th>Model</th><th>Effort</th><th>Runs</th><th>Balanced passes (raw trials)</th><th>API / PDF / timeout errors</th><th>Fidelity</th><th>Median AI</th><th>API / paper</th><th>API / 1,000 papers</th></tr></thead><tbody id="table"></tbody></table></div><p class="metric-note muted">Error categories overlap. A single changed equation causes a fidelity failure even when the average check score is high. All costs are USD; rendering, storage and labour are excluded here.</p></section>
<section class="panel"><h2>Include tutor time in the comparison</h2><p>These assumptions are adjustable. Review and correction time were not measured in this benchmark.</p><div class="filters"><label>Papers each month<input id="volume" aria-label="Papers each month" type="number" min="1" max="100000" value="1000"></label><label>Tutor cost / hour (USD)<input id="hourly" aria-label="Tutor cost per hour in USD" type="number" min="0" value="20"></label><label>Review minutes / paper<input id="review" aria-label="Review minutes per paper" type="number" min="0" value="2"></label><label>Extra minutes per content failure<input id="correction" aria-label="Extra minutes per content failure" type="number" min="0" value="3"></label></div><div class="table"><table><thead><tr><th>Model</th><th>Effort</th><th>Model charges</th><th>Review time</th><th>Illustrative correction time</th><th>Combined estimate</th></tr></thead><tbody id="labour"></tbody></table></div><p class="metric-note muted">Correction cost uses the observed content-failure fraction in the selected sample. It is a scenario, not a forecast. Timeouts, tutor expertise, retries and actual paper mix may change the result.</p></section>
<section class="panel method"><h2>How to read the evidence</h2><div class="grid"><div><h3>What was tested</h3><p>The PDF matrix uses 23 model/effort settings × 3 documents × 3 repeats. A separate image matrix tests all four models at low and medium effort using 1,600-pixel page images, with the same documents and repeat count.</p><p>The typed fixture includes Thai, equations, a table, a diagram, a question continuing onto another page and an optional private marking key. Its raster copy tests image-only input. The long paper tests dense mathematics and worked examples.</p><p>The same prompt, strict schema, output limit and renderer apply within each experiment. Every generated PDF is rasterized page by page and checked for layout overflow. Manual page review is recorded separately in the written analysis.</p></div><div><h3>What the numbers cannot establish</h3><p>The planned nine runs per setting are a small sample, and the interrupted PDF comparison has only four or five captured runs per setting. Even zero failures in nine trials has a roughly 30% upper 95% confidence bound under an independence assumption. Repeats of three documents are correlated, so generalization is more limited.</p><p>Checks do not fully measure wording, crop boundaries, marking-key correctness, working space, aesthetics, or arbitrary handwriting. Manual review found undersized working areas even in some papers with 100% source-check scores. This is a paper-formatting benchmark; grading and progress-report models are outside its scope.</p><p>Reasoning tokens are already included in output usage. Unknown usage is not counted as zero cost. A cost aggregate is marked unknown when any selected document type has no measured cost; those points are omitted from the chart. The separate exploratory PDF/image probes are excluded from the matrix cards and listed in the written report.</p></div></div>
<details><summary>Official price references</summary><p>Per million tokens: Luna $0.20 input / $1.20 output; Terra $2 / $12; Sol $4 / $20; Astra $10 / $50. Cached reads are one tenth of input price; reported cache writes use 1.25× input price. Sol prices are promotional through at least 21 November 2026.</p><p><a href="https://developers.openai.com/api/docs/models/gpt-5.6-luna">Luna</a> · <a href="https://developers.openai.com/api/docs/models/gpt-5.6-terra">Terra</a> · <a href="https://developers.openai.com/api/docs/models/gpt-5.6-sol">Sol</a> · <a href="https://developers.openai.com/api/docs/models/gpt-6-astra">Astra</a></p></details><p><button id="csv">Download selected comparison CSV</button></p></section></main>
<script type="application/json" id="data">${data}</script><script>
const data=JSON.parse(document.getElementById('data').textContent), $=id=>document.getElementById(id), colors={'gpt-5.6-luna':'#126dce','gpt-5.6-terra':'#237e5c','gpt-5.6-sol':'#bf641e','gpt-6-astra':'#7957b7'},rates={'gpt-5.6-luna':.2,'gpt-5.6-terra':2,'gpt-5.6-sol':4,'gpt-6-astra':10},efforts=['none','low','medium','high','xhigh','max'];
const money=n=>n===null?'Unknown':'$'+n.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}), mean=a=>a.length?a.reduce((x,y)=>x+y,0)/a.length:0, median=a=>{a=[...a].sort((a,b)=>a-b);return a.length?(a[Math.floor((a.length-1)/2)]+a[Math.floor(a.length/2)])/2:0},esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const contentPass=r=>r.apiSuccess&&r.renderSuccess===true&&r.quality===100;
const cost=r=>r.costUsd==null?null:$('cost').value==='observed'?r.costUsd:$('cost').value==='cold'?r.uncachedCostUsd:r.uncachedCostUsd+(r.inputTokens||0)*rates[r.model]*.25/1e6;
let grouped=[];
let codexGroups=[];
function updateCodex(){if(!data.codex)return;const chosen=$('codexFixture').value,rows=data.codex.rows.filter(r=>chosen==='all'||r.fixture===chosen),fixtures=chosen==='all'?['typed-key','scan-draft','regression-17']:[chosen];codexGroups=[];for(const model of Object.keys(colors))for(const effort of efforts.filter(e=>e!=='none')){const rs=rows.filter(r=>r.model===model&&r.effort===effort);if(!rs.length)continue;const balanced=fn=>{const bins=fixtures.map(f=>rs.filter(r=>r.fixture===f).map(fn).filter(v=>v!=null));return bins.some(b=>!b.length)?null:mean(bins.map(mean));};codexGroups.push({model,effort,n:rs.length,pass:rs.filter(r=>r.contentPass).length,responseErrors:rs.filter(r=>!r.success).length,sourceErrors:rs.filter(r=>r.success&&r.quality<100).length,pdfErrors:rs.filter(r=>r.renderSuccess===false).length,quality:balanced(r=>r.success?r.quality:null),seconds:median(rs.map(r=>r.seconds)),outputTokens:balanced(r=>r.usage?.output_tokens??null),reasoningTokens:balanced(r=>r.usage?.reasoning_output_tokens??null)});}const num=n=>n===null?'Unavailable':Math.round(n).toLocaleString('en-US');$('codexStatus').textContent=data.codex.runs+' / '+data.codex.planned+' Codex outcomes · '+data.codex.rows.filter(r=>r.success).length+' complete responses · '+data.codex.rows.filter(r=>r.timedOut).length+' capture timeouts · '+data.codex.cliVersion;$('codexTable').innerHTML=codexGroups.map(g=>'<tr><td>'+esc(g.model.replace('gpt-',''))+'</td><td>'+g.effort+'</td><td>'+g.n+'</td><td>'+g.pass+'/'+g.n+'</td><td>'+g.responseErrors+' / '+g.sourceErrors+' / '+g.pdfErrors+'</td><td>'+(g.quality===null?'Unscored':g.quality.toFixed(1)+'%')+'</td><td>'+g.seconds.toFixed(1)+'s</td><td>'+num(g.outputTokens)+'</td><td>'+num(g.reasoningTokens)+'</td></tr>').join('');}
$('runs').textContent=data.completed+' / '+data.expected;$('spend').textContent=money(data.measuredCost);$('unknown').textContent=data.unknown+' request(s) have unreported charges';$('renderTime').textContent=median(data.rows.filter(r=>r.renderSeconds!=null).map(r=>r.renderSeconds)).toFixed(1)+' seconds';$('completion').textContent=data.completed===data.expected?'The matrix is complete. Read the written analysis alongside these measurements.':'Paid API testing paused: credits exhausted. '+data.completed+' evaluable matrix requests recorded; '+data.accountRejections+' billing rejections excluded. Repeats are incomplete, so rankings are provisional.';
function update(){const selected=data.rows.filter(r=>r.inputMode===$('mode').value&&($('fixture').value==='all'||r.fixture===$('fixture').value));grouped=[];
for(const model of Object.keys(colors))for(const effort of efforts){const rs=selected.filter(r=>r.model===model&&r.effort===effort);if(!rs.length)continue;const pass=rs.filter(r=>contentPass(r)&&($('success').value==='content'||!r.productionTimeout)).length;const balanced=fn=>{const bins=[...new Set(rs.map(r=>r.fixture))].map(f=>rs.filter(r=>r.fixture===f).map(fn).filter(n=>n!=null));return bins.some(b=>!b.length)?null:mean(bins.map(mean));};grouped.push({model,effort,n:rs.length,pass,pct:balanced(r=>contentPass(r)&&($('success').value==='content'||!r.productionTimeout)?100:0),api:rs.filter(r=>!r.apiSuccess).length,pdf:rs.filter(r=>r.renderSuccess===false).length,time:rs.filter(r=>r.productionTimeout).length,quality:balanced(r=>r.apiSuccess?r.quality:null),seconds:median(rs.map(r=>r.aiSeconds)),cost:balanced(cost),contentFailure:balanced(r=>contentPass(r)?0:1)});}
$('table').innerHTML=grouped.map(g=>'<tr><td>'+esc(g.model.replace('gpt-',''))+'</td><td>'+g.effort+'</td><td>'+g.n+'</td><td>'+g.pct.toFixed(1)+'% ('+g.pass+'/'+g.n+')</td><td>'+g.api+' / '+g.pdf+' / '+g.time+'</td><td>'+(g.quality===null?'Unscored':g.quality.toFixed(1)+'%')+'</td><td>'+g.seconds.toFixed(1)+'s</td><td>'+(g.cost===null?'Unknown':'$'+g.cost.toFixed(4))+'</td><td>'+money(g.cost===null?null:g.cost*1000)+'</td></tr>').join('');
const svg=$('chart'),xMin=-3,xMax=1,x=v=>90+(Math.log10(Math.max(.001,v))-xMin)/(xMax-xMin)*920,y=v=>365-v*2.95;let draw='';for(const value of [.001,.01,.1,1,10]){const xx=x(value);draw+='<line x1="'+xx+'" x2="'+xx+'" y1="55" y2="365" stroke="currentColor" opacity=".13"/><text x="'+xx+'" y="391" text-anchor="middle" fill="currentColor" font-size="14">$'+value+'</text>';}for(const value of [0,25,50,75,100]){const yy=y(value);draw+='<line x1="90" x2="1010" y1="'+yy+'" y2="'+yy+'" stroke="currentColor" opacity=".13"/><text x="73" y="'+(yy+5)+'" text-anchor="end" fill="currentColor" font-size="14">'+value+'%</text>';}
draw+='<text x="90" y="30" fill="currentColor" font-size="15">Runs passing all selected checks</text><text x="550" y="430" fill="currentColor" font-size="15" text-anchor="middle">Model cost per paper (USD, logarithmic)</text>';for(const g of grouped.filter(g=>g.cost!==null))draw+='<circle cx="'+x(g.cost)+'" cy="'+y(g.pct)+'" r="'+(5+efforts.indexOf(g.effort))+'" fill="'+colors[g.model]+'" fill-opacity=".75" stroke="white" stroke-width="1"><title>'+esc(g.model+' · '+g.effort+': '+g.pass+'/'+g.n+' pass, $'+g.cost.toFixed(4)+' per paper')+'</title></circle>';svg.innerHTML=draw;labour();}
function labour(){const volume=Math.max(1,Number($('volume').value)||1),hourly=Math.max(0,Number($('hourly').value)||0),review=Math.max(0,Number($('review').value)||0),correction=Math.max(0,Number($('correction').value)||0);$('labour').innerHTML=grouped.map(g=>{const api=g.cost===null?null:g.cost*volume,base=volume*review/60*hourly,extra=volume*g.contentFailure*correction/60*hourly;return '<tr><td>'+esc(g.model.replace('gpt-',''))+'</td><td>'+g.effort+'</td><td>'+money(api)+'</td><td>'+money(base)+'</td><td>'+money(extra)+'</td><td>'+money(api===null?null:api+base+extra)+'</td></tr>';}).join('');}
for(const id of ['mode','fixture','cost','success'])$(id).addEventListener('change',update);for(const id of ['volume','hourly','review','correction'])$(id).addEventListener('input',labour);$('csv').onclick=()=>{const headers=['model','effort','n','pass','pct','api','pdf','time','quality','seconds','cost','contentFailure'];const csv=headers.join(',')+'\\n'+grouped.map(g=>headers.map(k=>JSON.stringify(g[k])).join(',')).join('\\n');const url=URL.createObjectURL(new Blob([csv],{type:'text/csv'})),a=document.createElement('a');a.href=url;a.download='progress-tests-api-2026-09-13-'+$('mode').value+'-'+$('fixture').value+'.csv';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),20000);};update();
if(data.codex){$('codexFixture').addEventListener('change',updateCodex);$('codexCsv').onclick=()=>{const headers=['model','effort','n','pass','responseErrors','sourceErrors','pdfErrors','quality','seconds','outputTokens','reasoningTokens'];const csv=headers.join(',')+'\\n'+codexGroups.map(g=>headers.map(k=>JSON.stringify(g[k])).join(',')).join('\\n');const url=URL.createObjectURL(new Blob([csv],{type:'text/csv'})),a=document.createElement('a');a.href=url;a.download='progress-tests-codex-2026-09-13-'+$('codexFixture').value+'-'+data.codex.runs+'-runs.csv';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),20000);};updateCodex();}
</script></html>`;
  await mkdir(`${root}/report`, { recursive: true });
  await writeFile(`${root}/report/comparison.html`, html, { mode: 0o600 });
  console.log(`${root}/report/comparison.html`);
}
main().catch(error=>{console.error(error);process.exitCode=1});
