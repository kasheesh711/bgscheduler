---
phase: 08.6-test-coverage-hardening
slug: test-coverage-hardening
status: verified
asvs_level: 1
block_on: high
threats_total: 31
threats_closed: 31
threats_open: 0
unregistered_flags: 0
created: 2026-04-30
verified: 2026-04-30
---

# Phase 08.6 Security Verification

Security config was absent, so default security verification is treated as enabled. Phase gate is `block_on: high`; no HIGH or MEDIUM threats remain open.

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Test process -> Docker daemon | Integration tests start an ephemeral Postgres container through testcontainers. | Test-only container lifecycle metadata; no production data. |
| Test process -> testcontainer Postgres | Drizzle migrations and fixtures run against a `postgres:16-alpine` container. | Synthetic test fixtures and hardcoded `test/test` credentials only. |
| Test process -> mocked auth/DB/Wise boundaries | Route, auth, middleware, and sync unit tests use Vitest mocks or fake clients. | Synthetic sessions, synthetic Wise records, and deterministic stubbed rows. |
| Dev workstation -> npm registry | Phase 08.6 added test-only dev dependencies and committed the lockfile. | Public npm package metadata and resolved dependency tree. |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-08.6.01-01 | I | `src/tests/integration/db-helper.ts` hardcoded credentials | accept | Accepted risk AR-08.6-01-01. Credentials are scoped to `postgres:16-alpine` with `test/test`; connection comes from the container URI in `src/tests/integration/db-helper.ts`. | closed |
| T-08.6.01-02 | T | New devDependencies | mitigate | Dev-only dependencies are in `devDependencies` and resolved in `package-lock.json`; production `dependencies` are unchanged. | closed |
| T-08.6.01-03 | D | testcontainers cleanup | mitigate | Helper exports `stopTestDb`; integration suites pair `startTestDb`/`stopTestDb`. | closed |
| T-08.6.01-04 | I | Test fixture secret exposure | mitigate | `db-helper.ts` references no `DATABASE_URL`, `WISE_*`, or `AUTH_*`; phase fixtures use synthetic data. | closed |
| T-08.6.01-05 | E | Vitest `extends: true` inheritance | accept | Accepted risk AR-08.6-01-05. `extends: true` is used only in the repo-owned Vitest projects config. | closed |
| T-08.6.02-01 | T | Search-index singleton globals | mitigate | `resetGlobals()` runs before and after relevant search-index suites, with mock restoration in teardown. | closed |
| T-08.6.02-02 | I | Search-index fixture PII | accept | Accepted risk AR-08.6-02-02. Fixtures are synthetic names/IDs and contain no real emails. | closed |
| T-08.6.02-03 | T | Same-module ESM spy masking behavior | mitigate | No `vi.spyOn(indexModule...)` usage was found; tests use select-count assertions instead. | closed |
| T-08.6.03-01 | I | Orchestrator integration fixture leakage | mitigate | Orchestrator integration fixtures use synthetic Wise IDs, sessions, and names. | closed |
| T-08.6.03-02 | T | Integration test uses shared/prod DB | mitigate | Integration test imports `startTestDb`, `stopTestDb`, and `truncateAll`; it contains no `getDb()` call. | closed |
| T-08.6.03-03 | D | Real Wise API calls from tests | mitigate | Fake Wise client dispatches hardcoded paths, throws on unmocked paths, and contains no `fetch`. | closed |
| T-08.6.03-04 | T | Cross-test `tutorAliases` pollution | mitigate | `truncateAll(handle.db)` runs in `beforeEach`. | closed |
| T-08.6.03-05 | I | Test DB credentials in logs | accept | Accepted risk AR-08.6-03-05. `test/test` credentials are ephemeral testcontainer credentials and are not logged by the helper. | closed |
| T-08.6.04-01 | I | Route test auth token exposure | mitigate | Route tests use mocked sessions only; no `process.env.AUTH_*` references are present in route tests. | closed |
| T-08.6.04-02 | I | Sync-wise test `CRON_SECRET` leakage | mitigate | Sync-wise tests use a synthetic secret and restore `process.env` in `afterEach`. | closed |
| T-08.6.04-03 | T | `vi.mock` after route import | mitigate | Hoisted mocks precede route imports across protected route tests. | closed |
| T-08.6.04-04 | E | Route tests accidentally bypass auth | mitigate | Protected route 401 cases explicitly set the auth mock to `null`. | closed |
| T-08.6.04-05 | T | Sync-wise test asserts status rather than timing-safe comparison | accept | Accepted risk AR-08.6-04-05. CRON gate behavior is tested for missing, wrong, and correct secrets; production route uses `timingSafeEqual`. | closed |
| T-08.6.05-01 | T | Old broken past-session unit test remains | mitigate | `src/lib/sync/__tests__/past-sessions-diff-hook.test.ts` is absent. | closed |
| T-08.6.05-02 | I | Past-session fixture PII | accept | Accepted risk AR-08.6-05-02. Fixtures use synthetic session IDs and canonical keys. | closed |
| T-08.6.05-03 | T | Orphan test cascades and tests nothing | mitigate | Orphan anomaly path is explicitly exercised using a temporary `session_replication_role` override and completeness assertions. | closed |
| T-08.6.06-01 | T | Bangkok DST surprise | accept | Accepted risk AR-08.6-06-01. Fixed Asia/Bangkok no-DST boundary tests document the intended behavior. | closed |
| T-08.6.06-02 | T | Time arithmetic rounding | mitigate | Exact `.toBe(...)` assertions cover minute and elapsed-time arithmetic. | closed |
| T-08.6.07-01 | E | Auth.js sign-in callback semantics | mitigate | `signInCallback` is exported and wired into Auth.js; tests cover allowlisted, normalized, rejected, and missing-email cases. | closed |
| T-08.6.07-02 | I | Real admin email in auth tests | accept | Accepted risk AR-08.6-07-02. The email is a documented allowlist target, not a secret, token, or session credential. | closed |
| T-08.6.07-03 | T | Middleware mock leakage | mitigate | Middleware tests use a per-file `vi.mock("@/lib/auth")` and isolated request cases. | closed |
| T-08.6.07-04 | E | Middleware bypass for `/api/internal/*` | accept | Accepted risk AR-08.6-07-04. Middleware bypass is intentional; `/api/internal/sync-wise` self-protects with `CRON_SECRET`, and both behaviors are tested. | closed |
| T-08.6.08-01 | T | Incomplete DB stub lets test pass by accident | mitigate | Test asserts `db.insert(schema.dataIssues)` and inspects the captured insert payload. | closed |
| T-08.6.08-02 | I | Modality-conflict fixture PII | accept | Accepted risk AR-08.6-08-02. Fixtures use synthetic tutor names and Wise IDs only. | closed |
| T-08.6.08-03 | T | Identity resolution skipped, `conflict_model` path untested | mitigate | The test fails if no `conflict_model` row is generated and also asserts required metadata. | closed |
| T-08.6.08-04 | T | Consistent fixture emits `conflict_model` unexpectedly | mitigate | Consistent fixture asserts zero `conflict_model` rows. | closed |

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-08.6-01-01 | T-08.6.01-01 | Hardcoded `test/test` credentials are limited to a testcontainer-created ephemeral Postgres instance and never use production `DATABASE_URL`. | GSD security workflow | 2026-04-30 |
| AR-08.6-01-05 | T-08.6.01-05 | `extends: true` is required by the repo-owned Vitest projects config and inherits only local reviewed config. | GSD security workflow | 2026-04-30 |
| AR-08.6-02-02 | T-08.6.02-02 | Search-index fixtures are synthetic and contain no real emails or production session identifiers. | GSD security workflow | 2026-04-30 |
| AR-08.6-03-05 | T-08.6.03-05 | Test runtime DB credentials are ephemeral testcontainer credentials and are not persisted by the helper. | GSD security workflow | 2026-04-30 |
| AR-08.6-04-05 | T-08.6.04-05 | Route test verifies the CRON gate behavior; timing-safe comparison is covered by the production route implementation path. | GSD security workflow | 2026-04-30 |
| AR-08.6-05-02 | T-08.6.05-02 | Past-session diff fixtures use synthetic session IDs and canonical keys only. | GSD security workflow | 2026-04-30 |
| AR-08.6-06-01 | T-08.6.06-01 | Bangkok is fixed UTC+7 with no DST; tests intentionally document fixed Asia/Bangkok behavior rather than DST handling. | GSD security workflow | 2026-04-30 |
| AR-08.6-07-02 | T-08.6.07-02 | The real admin email in auth tests is a documented allowlist target, not a secret, token, or session credential. | GSD security workflow | 2026-04-30 |
| AR-08.6-07-04 | T-08.6.07-04 | Middleware bypass for `/api/internal/*` is intentional because the internal route self-protects with `CRON_SECRET`; both layers are tested. | GSD security workflow | 2026-04-30 |
| AR-08.6-08-02 | T-08.6.08-02 | Modality-conflict fixtures use synthetic tutor names and Wise IDs only. | GSD security workflow | 2026-04-30 |

## Summary Threat Flags

No `## Threat Flags` sections were present in the Phase 08.6 summary files, so there are no unregistered flags to record.

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-04-30 | 31 | 31 | 0 | gsd-security-auditor |

## Residual Notes

- T-08.6.08-03 uses an equivalent assertion shape (`rows` from `conflictRows(valuesSpy)`) rather than the literal variable name `conflictCalls`. The security objective is satisfied because the test fails if no `conflict_model` row is generated and also asserts the required metadata.
- No transfer-disposition threats were present in the Phase 08.6 threat register.

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-04-30
