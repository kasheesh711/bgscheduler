---
phase: 08.6-test-coverage-hardening
plan: 02
subsystem: testing
tags: [search-index, buildIndex, ensureIndex, vitest]
requires: []
provides:
  - "SearchIndex denormalization and byWeekday tests"
  - "Cached fallback when active snapshot lookup is temporarily empty"
affects: [search-index, sync-promotion]
tech-stack:
  added: []
  patterns:
    - "Per-table fake DB dispatch keyed by Drizzle schema table reference."
key-files:
  created: []
  modified:
    - "src/lib/search/__tests__/index.test.ts"
    - "src/lib/search/index.ts"
key-decisions:
  - "Returned cached index when active snapshot lookup yields zero rows to satisfy the defensive fallback requirement."
patterns-established:
  - "buildIndex unit tests seed Drizzle row fixtures per schema table reference."
requirements-completed: [TCOV-01]
duration: 12min
completed: 2026-04-30
---

# Phase 08.6-02: Search Index Coverage Summary

**SearchIndex denormalization, weekday lookup, and active-snapshot race fallback are covered**

## Accomplishments

- Added `makeFakeDbWithTables` for table-specific buildIndex fixtures.
- Added denormalization tests for group members, qualifications, windows, leaves, sessions, modes, and data issues.
- Added `byWeekday` tests for multi-day and duplicate-same-day windows.
- Added active-snapshot zero-row fallback coverage for `ensureIndex`.

## Task Commits

1. **Task 1: Extend index tests and defensive fallback** - committed by orchestrator.

## Files Created/Modified

- `src/lib/search/__tests__/index.test.ts` - Added 6 TCOV-01 tests.
- `src/lib/search/index.ts` - Added cached fallback when the active snapshot lookup returns no rows.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Cached fallback test exposed missing production fallback**
- **Found during:** Task 1
- **Issue:** With a cached index present, `ensureIndex` rebuilt and threw `No active snapshot found` if the active snapshot lookup returned zero rows.
- **Fix:** Return the cached index when the active lookup returns no rows.
- **Files modified:** `src/lib/search/index.ts`
- **Verification:** `npx vitest run --project unit src/lib/search/__tests__/index.test.ts` passed 9 tests; full `npm test` passed 202 tests.
- **Committed in:** plan commit

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Narrows behavior to the documented defense-in-depth fallback. Normal stale-cache rebuild behavior is unchanged when an active snapshot row exists.

## Issues Encountered

- The plan-provided `--reporter=basic` command is not supported by this Vitest 4.1.5 install; tests were run without that reporter flag.

## Verification

- `npx vitest run --project unit src/lib/search/__tests__/index.test.ts` passed: 1 file, 9 tests.
- `npm test` passed: 26 files, 202 tests.
- `npx tsc --noEmit --project tsconfig.json` passed.

## User Setup Required

None.

## Next Phase Readiness

Search index invariants are locked for sync promotion and API-route coverage.

## Self-Check: PASSED

- Key files exist.
- Targeted and full unit tests passed.
- No `## Self-Check: FAILED` marker.

---
*Phase: 08.6-test-coverage-hardening*
*Completed: 2026-04-30*
