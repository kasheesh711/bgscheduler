---
phase: 06-mod-01-reliable-modality-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/search/cache-version.ts
  - src/hooks/use-compare.ts
autonomous: true
requirements: []
objective: >
  Establish the CACHE_VERSION client-cache constant convention (per D-17..D-20 /
  research Pitfall 14). Introduces a single-line module and suffixes all three
  `tutorCache` Map keys with `:${CACHE_VERSION}`. Wave-1 foundation so any shape
  change from Plan 02 lands on an invalidatable cache. No MOD-XX requirement
  listed here — this plan is infrastructure that supports MOD-01/02/03/04; MOD
  requirements live on Plans 02/03/04/05 per the requirements frontmatter there.

must_haves:
  truths:
    - "A single-line module `src/lib/search/cache-version.ts` exports `CACHE_VERSION = \"v1\"` with a comment explaining the bump rule"
    - "All three `tutorCache.current.set/get/delete` call sites in `src/hooks/use-compare.ts` use keys suffixed with `:${CACHE_VERSION}`"
    - "669 existing tests continue passing after the change (no test regression)"
  artifacts:
    - path: "src/lib/search/cache-version.ts"
      provides: "CACHE_VERSION constant + bump-rule documentation comment"
      exports: ["CACHE_VERSION"]
    - path: "src/hooks/use-compare.ts"
      provides: "tutorCache keys suffixed with cache version"
      contains: ":${CACHE_VERSION}"
  key_links:
    - from: "src/hooks/use-compare.ts"
      to: "src/lib/search/cache-version.ts"
      via: "import { CACHE_VERSION }"
      pattern: "from [\"']@/lib/search/cache-version[\"']"
---

<objective>
Create the `CACHE_VERSION` constant that governs client-side tutor cache
invalidation across v1.1 shape-changing phases. Wire every existing
`tutorCache` Map-key construction site to suffix keys with the version. This is
Wave 1 — a prerequisite for Plan 02 (resolver + `CompareSessionBlock` shape
change) so the client-side cache in long-lived tabs cannot serve stale-shape
tutors after deploy.

Purpose: Close research Pitfall 14 (client cache shape drift) before any v1.1
phase changes `CompareTutor`/`CompareSessionBlock`. Phase 6 is the first
shape-changing phase per `.planning/phases/05-polish-drain/05-CONTEXT.md`
deferred note, so the constant lands here.

Output: `src/lib/search/cache-version.ts` (new, ~6 lines) + 3 tutorCache
key-string edits in `src/hooks/use-compare.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mod-01-reliable-modality-detection/06-CONTEXT.md
@.planning/research/PITFALLS.md
@src/hooks/use-compare.ts

<interfaces>
<!-- Key existing shape the executor must preserve. Extracted from src/hooks/use-compare.ts. -->
<!-- These three call sites are the entire surface area this plan modifies. -->

From src/hooks/use-compare.ts (BEFORE this plan):
```typescript
// Line 84 — declaration (UNCHANGED by this plan):
const tutorCache = useRef(new Map<string, CompareTutor>());

// Line 139 — set (inside fetchCompare, merging returned tutors into cache):
for (const t of data.tutors) {
  tutorCache.current.set(`${t.tutorGroupId}:${week}`, t);
}

// Line 144 — get (inside fetchCompare, building full tutor list from cache):
const mergedTutors = ids
  .map((id) => tutorCache.current.get(`${id}:${week}`))
  .filter((t): t is CompareTutor => t !== undefined);

// Line 169 — delete (inside removeTutor):
tutorCache.current.delete(`${id}:${weekStart}`);
```

AFTER this plan, all three template-literal keys must read
`` `${id}:${week}:${CACHE_VERSION}` `` (or `weekStart` for line 169), importing
`CACHE_VERSION` from `@/lib/search/cache-version`. No other behavior changes.

The existing `tutorCache.current.clear()` calls at lines 125 and 199 require NO
changes — `.clear()` takes no key, so versioning does not apply.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/search/cache-version.ts module</name>
  <files>src/lib/search/cache-version.ts</files>
  <read_first>
    - .planning/phases/06-mod-01-reliable-modality-detection/06-CONTEXT.md (D-17, D-19, D-20 — locked decisions on file path, starting value, and bump discipline)
    - .planning/research/PITFALLS.md §"Pitfall 14" (lines 344-363 — why this constant exists and what it protects against)
  </read_first>
  <action>
    Create a new file `src/lib/search/cache-version.ts` with EXACTLY this content (no other exports, no runtime logic, no default export):

    ```typescript
    /**
     * Client-side cache version for `CompareTutor`-shaped data held in the
     * `tutorCache` Map inside `useCompare` (see `src/hooks/use-compare.ts`).
     *
     * Bump this string whenever the shape of `CompareTutor` /
     * `CompareSessionBlock` / any client-cached server shape changes. Future
     * v1.1 phases (PAST-01, VPOL-03) MUST bump this alongside their shape
     * change. The bump invalidates long-lived client tabs without a hard
     * reload.
     *
     * See `.planning/research/PITFALLS.md` Pitfall 14 for rationale.
     */
    export const CACHE_VERSION = "v1";
    ```

    Notes on intent (do NOT add to file):
    - Starting value `"v1"` per D-19.
    - No invalidation machinery beyond this string — D-20 explicitly forbids runtime version-skew detection, hard-reload banners, etc. The constant + grep discipline is the mechanism.
    - The file sits alongside `src/lib/search/compare.ts`, `types.ts`, `index.ts` to keep the "one-concern module" convention per D-17.
  </action>
  <verify>
    <automated>test -f src/lib/search/cache-version.ts && grep -c 'export const CACHE_VERSION = "v1";' src/lib/search/cache-version.ts | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at EXACTLY `src/lib/search/cache-version.ts`.
    - `grep -n 'export const CACHE_VERSION = "v1";' src/lib/search/cache-version.ts` returns exactly one line.
    - `grep -c "Bump this string" src/lib/search/cache-version.ts` returns `1` (the bump-rule comment exists).
    - `grep -c "PITFALLS.md" src/lib/search/cache-version.ts` returns `1` (research reference for future maintainers).
    - File is ≤30 lines (one-concern module discipline).
    - No runtime logic (no functions, no classes, no conditionals) — `grep -E "^(function|class|if|const [a-z])" src/lib/search/cache-version.ts` returns zero matches (only the single `export const CACHE_VERSION` line is code).
    - `npx tsc --noEmit` runs without new errors attributable to this file (pre-existing env-type noise per STATE.md Anti-patterns is OK; only NEW errors related to `cache-version.ts` are blockers).
  </acceptance_criteria>
  <done>
    New file `src/lib/search/cache-version.ts` exists with the exact export above and the bump-rule comment; typecheck is not broken by this file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Suffix all three tutorCache call sites in use-compare.ts with :${CACHE_VERSION}</name>
  <files>src/hooks/use-compare.ts</files>
  <read_first>
    - .planning/phases/06-mod-01-reliable-modality-detection/06-CONTEXT.md (D-18 — enumerates the three call sites; names localStorage as NOT versioned; localStorage is out of scope for this plan)
    - src/hooks/use-compare.ts (the file being modified — read in full so the `.clear()` calls at lines 125 and 199 are NOT touched)
    - src/lib/search/cache-version.ts (the file created in Task 1 — verify the import path works)
  </read_first>
  <action>
    Step 1: Add an import at the top of the imports block in `src/hooks/use-compare.ts` (near the existing `import type { CompareResponse, CompareTutor, Conflict } from "@/lib/search/types";` line — keep alphabetical-ish locality):

    ```typescript
    import { CACHE_VERSION } from "@/lib/search/cache-version";
    ```

    Step 2: Edit EXACTLY these three key-string template literals. Line numbers reference the file as of 2026-04-21 — before editing, confirm by running `grep -n "tutorCache.current" src/hooks/use-compare.ts` and verify the key expressions match byte-for-byte what is shown in the `<interfaces>` block at the top of this plan. If the file has drifted, read the current three lines verbatim before applying the edit; the FROM text below must match the file's actual current source.

    The three edits are verbatim key expressions (NOT TypeScript escape sequences — these are the literal template-literal expressions you will find in the source file):

        Site 1 — tutorCache.current.set at line 139:
          FROM key:  `${t.tutorGroupId}:${week}`
          TO key:    `${t.tutorGroupId}:${week}:${CACHE_VERSION}`

        Site 2 — tutorCache.current.get at line 144:
          FROM key:  `${id}:${week}`
          TO key:    `${id}:${week}:${CACHE_VERSION}`

        Site 3 — tutorCache.current.delete at line 169:
          FROM key:  `${id}:${weekStart}`
          TO key:    `${id}:${weekStart}:${CACHE_VERSION}`

    Copy the FROM key expressions verbatim from `src/hooks/use-compare.ts` lines 139/144/169 before applying the edit. Do NOT paste any escape artifacts (no backslashes, no `"${...}"` quoting) — the keys are plain TypeScript template literals.

    Step 3: Do NOT modify `tutorCache.current.clear()` at lines 125 and 199 — `.clear()` takes no key so versioning is irrelevant. Do NOT version the `localStorage` recent-searches key elsewhere in the codebase (D-18 explicitly scopes versioning to the in-memory `tutorCache` Map only).

    Step 4: Run `npm test -- --run` and confirm the test count is still exactly 669 passed.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -5 | grep -E "Tests\s+669 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "@/lib/search/cache-version"' src/hooks/use-compare.ts` returns `1` (import added once).
    - `grep -c ':\${CACHE_VERSION}' src/hooks/use-compare.ts` returns exactly `3` (three key-string sites suffixed). If any other tool reports a different count, run `rg -n ':\$\{CACHE_VERSION\}' src/hooks/use-compare.ts` — must show exactly 3 lines.
    - `grep -n 'tutorCache.current.set' src/hooks/use-compare.ts` shows the line contains `CACHE_VERSION` in the key template.
    - `grep -n 'tutorCache.current.get' src/hooks/use-compare.ts` shows the line contains `CACHE_VERSION`.
    - `grep -n 'tutorCache.current.delete' src/hooks/use-compare.ts` shows the line contains `CACHE_VERSION`.
    - `grep -c 'tutorCache.current.clear' src/hooks/use-compare.ts` returns `2` and those two lines do NOT contain `CACHE_VERSION` (clear takes no key).
    - `npm test -- --run 2>&1 | tail -5` shows `Tests 669 passed (669)` — no test regression.
    - `npm run build` exits 0 (OR if it fails, the failure is NOT traceable to lines this task touched — check the error output).
  </acceptance_criteria>
  <done>
    Three `tutorCache` key strings in `src/hooks/use-compare.ts` are suffixed with `:${CACHE_VERSION}`, the import is added, and all 669 existing tests pass.
  </done>
</task>

</tasks>

<verification>
  Phase-level checks this plan contributes to:
  - `src/lib/search/cache-version.ts` exists and exports `CACHE_VERSION = "v1"`.
  - All three `tutorCache` call sites in `src/hooks/use-compare.ts` reference `:${CACHE_VERSION}`.
  - `npm test -- --run` exits 0 with 669 tests passing.
  - `npm run build` exits 0.
</verification>

<success_criteria>
  - File `src/lib/search/cache-version.ts` is committed with exactly one `export const CACHE_VERSION = "v1";` line plus documentation comment.
  - `src/hooks/use-compare.ts` contains an import of `CACHE_VERSION` and exactly three occurrences of `:${CACHE_VERSION}` in template-literal Map key strings.
  - Plan commits atomically without breaking the 669-test baseline.
  - Plan 02 can proceed in Wave 2 knowing the cache-invalidation discipline is in place for `CompareSessionBlock` shape changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mod-01-reliable-modality-detection/06-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Files modified / created (with line counts)
- Any commits (SHA + message)
- Test results (expect 669 passed)
- Notes for Plan 02 executor: reference `CACHE_VERSION` is now available at `@/lib/search/cache-version` — if Plan 02 changes `CompareSessionBlock` shape, DO NOT bump the constant (this phase uses `"v1"` end-to-end; bumping would invalidate the cache mid-phase for no benefit).
</output>
</content>
