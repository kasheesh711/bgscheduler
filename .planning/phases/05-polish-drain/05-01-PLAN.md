---
phase: 05-polish-drain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/api/auth/[...nextauth]/route 2.ts"
  - "src/app/api/search/range/route 2.ts"
  - .planning/phases/FULL-APP-UI-REVIEW.md
  - .planning/ui-reviews/
autonomous: true
requirements: []
must_haves:
  truths:
    - "Duplicate `route 2.ts` files no longer exist in the repo"
    - "`.planning/phases/FULL-APP-UI-REVIEW.md` stale artifact is gone"
    - "`.planning/ui-reviews/` empty directory is gone"
    - "~40 staged `.planning/phases/D` archival deletions from v1.0 milestone completion are committed"
    - "`git status --short` reports no stale orphan/untracked files at Phase 5 kickoff"
  artifacts:
    - path: ".planning/phases/05-polish-drain/05-01-SUMMARY.md"
      provides: "Phase 5 prep-commit execution summary"
      min_lines: 10
  key_links: []
---

<objective>
Clean the working tree so every subsequent POLISH commit is focused and readable. Bundle the four orphan deletions from CONTEXT.md D-09 with the ~40 staged `D .planning/phases/*` archival deletions inherited from `/gsd-complete-milestone 1.0` into a single prep commit that lands BEFORE any POLISH-* commit.

Purpose: `git log --oneline .planning/phases/05-polish-drain/` reads cleanly; executors of later plans see only per-POLISH-item diffs, not mixed cleanup + POLISH noise.
Output: One atomic prep commit + `05-01-SUMMARY.md`. No POLISH-* requirements are touched in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-drain/05-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit working-tree cleanup (D-09 + D-10)</name>
  <files>
    src/app/api/auth/[...nextauth]/route 2.ts (delete),
    src/app/api/search/range/route 2.ts (delete),
    .planning/phases/FULL-APP-UI-REVIEW.md (delete),
    .planning/ui-reviews/ (delete directory),
    .planning/phases/01-component-architecture/* (~11 staged deletions, already tracked as `D`),
    .planning/phases/02-streaming-lazy-loading/* (~10 staged deletions),
    .planning/phases/03-calendar-readability-workflow-polish/* (~14 staged deletions),
    .planning/phases/04-ui-audit-polish/* (~8 staged deletions)
  </files>
  <read_first>
    .planning/phases/05-polish-drain/05-CONTEXT.md (D-09, D-10 decisions §"Working-Tree Cleanup"),
    .planning/STATE.md (line 101: "Orphan working-tree noise still untouched"),
    output of `git status --short` at phase kickoff (verifies the exact deletion set before committing)
  </read_first>
  <action>
Execute these exact steps:

1. Verify the four orphan files/dirs exist:
   - `ls -la "src/app/api/auth/[...nextauth]/route 2.ts"`
   - `ls -la "src/app/api/search/range/route 2.ts"`
   - `ls -la .planning/phases/FULL-APP-UI-REVIEW.md`
   - `ls -la .planning/ui-reviews/` (should show only `.gitignore`)

2. Verify the duplicates are byte-identical to their canonical counterparts (per CONTEXT.md D-09: "macOS Finder duplicate, byte-for-byte identical"):
   - `diff "src/app/api/auth/[...nextauth]/route.ts" "src/app/api/auth/[...nextauth]/route 2.ts"` → exit 0 (no diff)
   - `diff "src/app/api/search/range/route.ts" "src/app/api/search/range/route 2.ts"` → exit 0 (no diff)
   If either diff reports differences, STOP and escalate — do NOT delete.

3. Delete the four orphans:
   - `rm "src/app/api/auth/[...nextauth]/route 2.ts"`
   - `rm "src/app/api/search/range/route 2.ts"`
   - `rm .planning/phases/FULL-APP-UI-REVIEW.md`
   - `rm -rf .planning/ui-reviews/`

4. Stage everything together — both the new deletions from step 3 AND the ~40 pre-staged `D .planning/phases/*` archival deletions from the `/gsd-complete-milestone 1.0` archival:
   - `git add -A src/app/api/auth src/app/api/search .planning/phases .planning/ui-reviews`

5. Verify staging:
   - `git status --short` should show: all `.planning/phases/01-*/`, `02-*/`, `03-*/`, `04-*/` files as `D` (staged deletion); `src/app/api/auth/[...nextauth]/route 2.ts` and `src/app/api/search/range/route 2.ts` as `D`; `.planning/phases/FULL-APP-UI-REVIEW.md` as `D`; `.planning/ui-reviews/` gone.
   - `git diff --cached --stat | tail -3` should report a summary line showing ≥40 files deleted.

6. Run the test suite to confirm deletions do not break anything (they shouldn't — orphans are not imported, archival docs are not consumed at runtime):
   - `npm test` → all tests pass, exits 0.

7. Commit with the exact message mandated by CONTEXT.md D-09:
   - `git commit -m "chore(05): clean working tree + commit phase archival deletions"` (include the standard Co-Authored-By trailer)

**Do NOT touch any POLISH-* code in this commit.** If any unrelated working-tree change is present (e.g., edits to `src/`), escalate — this commit is deletion-only.

**Do NOT use `git add .` or `git add -A` blindly at the repo root** — restrict staging to the paths above to avoid accidentally picking up `.claude/` or other ignorable/unrelated changes.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | grep -q "^chore(05): clean working tree" && git status --short | grep -vE "^(\?\?|M  )" | wc -l | awk '{exit ($1 == 0 ? 0 : 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format="%s"` outputs exactly `chore(05): clean working tree + commit phase archival deletions`
    - `ls "src/app/api/auth/[...nextauth]/route 2.ts" 2>&1 | grep -c "No such file"` returns `1`
    - `ls "src/app/api/search/range/route 2.ts" 2>&1 | grep -c "No such file"` returns `1`
    - `ls .planning/phases/FULL-APP-UI-REVIEW.md 2>&1 | grep -c "No such file"` returns `1`
    - `ls -d .planning/ui-reviews 2>&1 | grep -c "No such file"` returns `1`
    - `git diff --cached --stat` returns empty (commit happened, nothing left staged)
    - `git log -1 --name-status | grep -cE "^D\s"` returns at least `40` (40+ deletions in the single commit)
    - `npm test` exits 0 (baseline ≥ 246 tests continue passing)
    - `src/app/api/auth/[...nextauth]/route.ts` still exists and is unchanged (`git log -1 --name-status | grep -c "route.ts$"` → 0; canonical file is not in the deletion list)
    - `src/app/api/search/range/route.ts` still exists and is unchanged
  </acceptance_criteria>
  <done>
    Working tree is clean at POLISH-* kickoff. Running `git status --short` shows no `??` entries for the four orphans and no `D` entries for the ~40 archival files. One prep commit exists on the current branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This is a filesystem cleanup commit — no code execution surface added, no input validation changed, no authentication touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | T (Tampering) | deleted `route 2.ts` files | accept | Files are byte-identical duplicates of canonical `route.ts`; Step 2 in action block runs `diff` to verify identity BEFORE deletion. If diff is non-empty, executor aborts. No tampering risk — the canonical route remains untouched. |
| T-05-01-02 | I (Information Disclosure) | archival `.planning/phases/*` deletions | accept | These files are already tracked as deletions in git staging from `/gsd-complete-milestone 1.0`. Commit content remains recoverable via `git log`. No sensitive data was stored in these files (they were planning artifacts, not secrets). |

Phase 5 posture per CONTEXT.md: cleanup/polish, no new security surfaces. Prep commit introduces zero new threats.
</threat_model>

<verification>
Phase-level checks executed after this plan's commit lands:

1. `git status --short` shows zero `??` or `D` entries for the orphan / archival file set
2. `npm test` exits 0 (246+ test baseline maintained)
3. `git log -1 --format="%s"` matches the mandated subject line
4. Both canonical route files (`route.ts` in the two API directories) still exist and pass their contract:
   - `grep -c "export" "src/app/api/auth/[...nextauth]/route.ts"` returns at least `1`
   - `grep -c "export" "src/app/api/search/range/route.ts"` returns at least `1`
</verification>

<success_criteria>
- Single commit on the branch with subject `chore(05): clean working tree + commit phase archival deletions`
- All four orphans removed; all ~40 staged archival deletions included in the same commit
- `npm test` exits 0 post-commit
- `git status --short` is clean (modulo any unrelated tracked changes the user intends to keep, e.g. `.planning/STATE.md` modifications)
- `05-01-SUMMARY.md` created in phase directory
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-drain/05-01-SUMMARY.md` summarizing: files deleted, commit SHA, test count post-commit, any deviations from the action plan.
</output>
