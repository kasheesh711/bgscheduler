---
phase: 05-polish-drain
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/hooks/use-compare.ts
  - src/components/compare/tutor-selector.tsx
autonomous: true
requirements: [POLISH-11, POLISH-14]
must_haves:
  truths:
    - "`addTutor` is wrapped in `useCallback` with explicit deps `[compareTutors, weekStart, fetchCompare]` so it has stable identity across renders (POLISH-11 / L3)"
    - "Orphan `TutorSelector` component body at `src/components/compare/tutor-selector.tsx:19-49` is removed while `interface TutorChip`, `TUTOR_COLORS` re-export, and `type { TutorChip }` export are preserved (POLISH-14)"
  artifacts:
    - path: src/hooks/use-compare.ts
      provides: "`addTutor` wrapped in useCallback + preserved `removeTutor` / `changeWeek` behavior"
      contains: "useCallback"
    - path: src/components/compare/tutor-selector.tsx
      provides: "Types-and-constants-only module (no component body)"
      contains: "interface TutorChip"
    - path: .planning/phases/05-polish-drain/05-04-SUMMARY.md
      provides: "Plan 04 execution summary"
      min_lines: 10
  key_links:
    - from: src/hooks/use-compare.ts
      to: src/components/compare/tutor-selector.tsx
      via: "`import type { TutorChip } from '@/components/compare/tutor-selector'` + `TUTOR_COLORS` re-export"
      pattern: "from \"@/components/compare/tutor-selector\"|from \"./session-colors\""
---

<objective>
Close two Phase 03/cross-cutting findings:

- **POLISH-11 (L3):** Wrap `useCompare.addTutor` in `useCallback` so downstream consumers can safely memoize on its identity. Wrap with explicit deps `[compareTutors, weekStart, fetchCompare]` per CONTEXT.md Claude's Discretion.
- **POLISH-14 (cross-cutting):** Remove the unused `TutorSelector` React component body at `src/components/compare/tutor-selector.tsx:19-49`. The file stays — `interface TutorChip`, the `type { TutorChip }` export, and the `TUTOR_COLORS` re-export are still consumed elsewhere.

Purpose: clean up stale component code + stabilize hook identity. Independent of the other Wave 2 plans.
Output: 2 files modified, 2 atomic commits (one per POLISH-XX), `05-04-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-drain/05-CONTEXT.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@src/hooks/use-compare.ts
@src/components/compare/tutor-selector.tsx
@src/components/compare/session-colors.ts

<interfaces>
<!-- tutor-selector.tsx currently exports three things. Only one is a component; the other two are widely consumed. -->

Current exports from src/components/compare/tutor-selector.tsx:

- export function TutorSelector(...)       // UNUSED — remove body + props interface
- export { TUTOR_COLORS };                 // re-export from ./session-colors — KEEP
- export type { TutorChip };               // KEEP

The interface TutorChip declaration (lines 7-11) must remain (it is re-exported as a type and consumed by use-compare.ts, search-workspace.tsx, etc.).

The interface TutorSelectorProps declaration (lines 13-17) can be removed alongside the component body — it is only used by TutorSelector itself.

Imports inside the file that become unused after removal: Button, X (lucide). Remove them to keep the file lint-clean.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: POLISH-11 — Wrap addTutor in useCallback</name>
  <files>src/hooks/use-compare.ts</files>
  <read_first>
    src/hooks/use-compare.ts (current state of lines 178-192 — addTutor body; also lines 88-164 to see fetchCompare already uses useCallback),
    .planning/milestones/v1.0-MILESTONE-AUDIT.md:138 (L3 finding: compare.addTutor not wrapped in useCallback),
    .planning/phases/05-polish-drain/05-CONTEXT.md §"Claude's Discretion" (POLISH-11 bullet: deps [compareTutors, weekStart, fetchCompare] per standard practice)
  </read_first>
  <action>
Current code (lines 178-192):

    const addTutor = (id: string, name: string) => {
      if (compareTutors.length >= 3) return;
      const updated = [
        ...compareTutors,
        {
          tutorGroupId: id,
          displayName: name,
          color: TUTOR_COLORS[compareTutors.length],
        },
      ];
      setCompareTutors(updated);
      setDiscoveryOpen(false);
      // Only fetch the newly added tutor
      fetchCompare(updated.map((t) => t.tutorGroupId), weekStart, { fetchOnly: [id] });
    };

Change to:

    const addTutor = useCallback(
      (id: string, name: string) => {
        if (compareTutors.length >= 3) return;
        const updated = [
          ...compareTutors,
          {
            tutorGroupId: id,
            displayName: name,
            color: TUTOR_COLORS[compareTutors.length],
          },
        ];
        setCompareTutors(updated);
        setDiscoveryOpen(false);
        // Only fetch the newly added tutor
        fetchCompare(updated.map((t) => t.tutorGroupId), weekStart, { fetchOnly: [id] });
      },
      [compareTutors, weekStart, fetchCompare],
    );

Notes:

- Deps [compareTutors, weekStart, fetchCompare] per CONTEXT.md. setCompareTutors and setDiscoveryOpen are React setState functions — always stable; safe to omit. TUTOR_COLORS is a module-level constant — stable.
- useCallback is already imported at line 3. No import change needed.
- Do NOT wrap removeTutor or changeWeek in this commit (not in POLISH scope; re-wrapping them would expand the diff beyond POLISH-11). Those are left alone.

Step 2 — Run tests:

- `npm test` exits 0.

Step 3 — Commit:

- `git add src/hooks/use-compare.ts`
- `git commit -m "fix(05): wrap addTutor in useCallback for stable identity (POLISH-11)"` with Co-Authored-By trailer.
  </action>
  <verify>
    <automated>grep -cE "const addTutor = useCallback" src/hooks/use-compare.ts | awk '{exit ($1==1?0:1)}' &amp;&amp; grep -c "compareTutors, weekStart, fetchCompare" src/hooks/use-compare.ts | awk '{exit ($1&gt;=1?0:1)}' &amp;&amp; npm test --silent 2&gt;&amp;1 | tail -5 | grep -qE "Tests|pass"</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "const addTutor = useCallback" src/hooks/use-compare.ts returns 1
    - grep -c "[compareTutors, weekStart, fetchCompare]" src/hooks/use-compare.ts returns at least 1 (the new dep array; bracket literal check)
    - grep -c "const addTutor = (id: string, name: string) =>" src/hooks/use-compare.ts returns 0 (old un-wrapped form is gone)
    - npm test exits 0 (246+ tests pass)
    - git log -1 --format="%s" outputs "fix(05): wrap addTutor in useCallback for stable identity (POLISH-11)"
    - git log -1 --name-only | grep -c "^src/hooks/use-compare.ts$" returns 1 (single-file commit)
  </acceptance_criteria>
  <done>
    addTutor has stable reference identity across renders as long as compareTutors, weekStart, and fetchCompare do not change. Downstream memoization (e.g. SearchResults re-renders) now works. Test baseline unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: POLISH-14 — Remove orphan TutorSelector component body</name>
  <files>src/components/compare/tutor-selector.tsx</files>
  <read_first>
    src/components/compare/tutor-selector.tsx (current full file — 52 lines),
    .planning/milestones/v1.0-MILESTONE-AUDIT.md:32 (cross-cutting finding: orphaned TutorSelector component),
    .planning/phases/05-polish-drain/05-CONTEXT.md §"Claude's Discretion" (POLISH-14 bullet: remove component body at lines 19-49; preserve interface TutorChip, TUTOR_COLORS re-export, type { TutorChip } export),
    output of `grep -rn "TutorSelector\b" src/ --include="*.tsx" --include="*.ts"` to confirm ComparePanel does NOT import TutorSelector (should show zero imports; only this file declares it)
  </read_first>
  <action>
Verify zero consumers before deleting:

- Run `grep -rn "from [\"']@/components/compare/tutor-selector[\"']" src/ --include="*.tsx" --include="*.ts"`. Any import that pulls `{ TutorSelector }` (the component) MUST be zero — the component is unused. Imports of `{ TUTOR_COLORS }` and `type { TutorChip }` are expected and MUST continue to work.
- Run `grep -rn "TutorSelector\b" src/ --include="*.tsx" --include="*.ts"`. Matches outside `src/components/compare/tutor-selector.tsx` must be zero.

If ANY consumer imports `{ TutorSelector }`, STOP — escalate. Otherwise proceed.

Target final file contents (replace the full file):

    "use client";

    import { TUTOR_COLORS } from "./session-colors";

    interface TutorChip {
      tutorGroupId: string;
      displayName: string;
      color: string;
    }

    export { TUTOR_COLORS };
    export type { TutorChip };

Notes on what is removed:

- export function TutorSelector(...) {...} body at lines 19-49
- interface TutorSelectorProps declaration at lines 13-17 (used only by the removed component)
- imports that become unused: Button (from @/components/ui/button), X (from lucide-react)

Notes on what is kept:

- "use client" directive (still accurate; the file will live next to client code and the re-export boundary benefits from client-side hoisting even though a types-and-constants-only module does not strictly need it — keeping it avoids a cross-module boundary change)
- TUTOR_COLORS import from ./session-colors
- interface TutorChip (still consumed via the type re-export)
- Both exports at the bottom

Step 2 — Run tests:

- `npm test` exits 0.
- `grep -rn "TutorSelector" src/ --include="*.tsx" --include="*.ts"` returns no matches OUTSIDE this file. Confirm zero broken imports.

Step 3 — Commit:

- `git add src/components/compare/tutor-selector.tsx`
- `git commit -m "chore(05): remove orphan TutorSelector component body (POLISH-14)"` with Co-Authored-By trailer.
  </action>
  <verify>
    <automated>grep -c "export function TutorSelector" src/components/compare/tutor-selector.tsx | awk '{exit ($1==0?0:1)}' &amp;&amp; grep -c "interface TutorChip" src/components/compare/tutor-selector.tsx | awk '{exit ($1==1?0:1)}' &amp;&amp; grep -c "TUTOR_COLORS" src/components/compare/tutor-selector.tsx | awk '{exit ($1&gt;=1?0:1)}' &amp;&amp; grep -c "export type { TutorChip }" src/components/compare/tutor-selector.tsx | awk '{exit ($1==1?0:1)}' &amp;&amp; npm test --silent 2&gt;&amp;1 | tail -5 | grep -qE "Tests|pass"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export function TutorSelector" src/components/compare/tutor-selector.tsx returns 0
    - grep -c "interface TutorSelectorProps" src/components/compare/tutor-selector.tsx returns 0 (props interface also removed)
    - grep -c "interface TutorChip" src/components/compare/tutor-selector.tsx returns 1 (preserved)
    - grep -c "TUTOR_COLORS" src/components/compare/tutor-selector.tsx returns at least 1 (import) and the module re-exports it: grep -c "export { TUTOR_COLORS }" src/components/compare/tutor-selector.tsx returns 1
    - grep -c "export type { TutorChip }" src/components/compare/tutor-selector.tsx returns 1
    - grep -c "from \"lucide-react\"" src/components/compare/tutor-selector.tsx returns 0 (unused X import removed)
    - grep -c "from \"@/components/ui/button\"" src/components/compare/tutor-selector.tsx returns 0 (unused Button import removed)
    - wc -l src/components/compare/tutor-selector.tsx returns a value ≤ 15 (down from 52)
    - grep -rn "TutorSelector\b" src/ --include="*.tsx" --include="*.ts" | grep -vc "tutor-selector.tsx:" returns 0 (no external consumers of the removed component)
    - npm test exits 0 (246+ tests pass)
    - git log -1 --format="%s" outputs "chore(05): remove orphan TutorSelector component body (POLISH-14)"
    - git log -1 --name-only | grep -c "^src/components/compare/tutor-selector.tsx$" returns 1
  </acceptance_criteria>
  <done>
    The file is down to ~12 lines: directive + import + TutorChip interface + two exports. All consumers of TUTOR_COLORS and TutorChip still compile. Tests pass. ComparePanel continues rendering tutor chips inline (unchanged).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. Hook refactor + dead-file cleanup.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | T (Tampering) | addTutor identity | accept | Wrapping in useCallback with [compareTutors, weekStart, fetchCompare] preserves prior behavior exactly. No new code paths. |
| T-05-04-02 | T (Tampering) | TutorSelector removal | accept | Removing unreachable code (grep proves zero external consumers). No behavior change. |

Phase 5 posture: cleanup only. No new threats.
</threat_model>

<verification>
After both tasks commit (two atomic commits total):

1. npm test exits 0 (246+ baseline)
2. grep -rn "TutorSelector\b" src/ --include="*.tsx" --include="*.ts" shows zero matches outside the tutor-selector.tsx file itself
3. grep -cE "const addTutor = useCallback" src/hooks/use-compare.ts returns 1
4. git log --oneline | head -2 shows both POLISH-11 and POLISH-14 commits
</verification>

<success_criteria>
- Two atomic commits, one per POLISH-XX
- All acceptance criteria pass
- npm test exits 0 after each commit
- 05-04-SUMMARY.md records commit SHAs + grep outputs
</success_criteria>

<output>
After completion, create .planning/phases/05-polish-drain/05-04-SUMMARY.md recording: two commit SHAs, grep counts, final test count, any deviations.
</output>
