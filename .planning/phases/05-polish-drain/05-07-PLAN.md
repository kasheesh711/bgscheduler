---
phase: 05-polish-drain
plan: 07
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-polish-drain/05-VERIFICATION.md
autonomous: false
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04, POLISH-05, POLISH-15]
user_setup:
  - service: "production deploy"
    why: "Human QA must run against bgscheduler.vercel.app, not localhost. All prior plans (05-01 through 05-06) must be deployed to production BEFORE this plan runs. POLISH-09 token swap in particular must be live for POLISH-03 sign-off."
    env_vars: []
    dashboard_config:
      - task: "Deploy current branch to production"
        location: "`npx vercel --prod` (per CLAUDE.md §'Running Commands') — OR merge to main if Vercel Git integration handles prod auto-deploy"
must_haves:
  truths:
    - "A single production walkthrough against https://bgscheduler.vercel.app captures pass/fail for POLISH-01 (VoiceOver, relaxed scope per D-02), POLISH-02, POLISH-03, POLISH-04, POLISH-05, and POLISH-15"
    - "`.planning/phases/05-polish-drain/05-VERIFICATION.md` records one line per QA item with `status: pass|fail` + ISO8601 timestamp (D-03)"
    - "Screenshots captured ONLY for failing items (D-03)"
    - "`.planning/REQUIREMENTS.md` is amended per D-02: POLISH-01 is scoped to VoiceOver only, and `NVDA-v12` is added to the v1.2+ deferred list"
  artifacts:
    - path: .planning/phases/05-polish-drain/05-VERIFICATION.md
      provides: "One-line-per-item QA results: POLISH-01 through POLISH-05 + POLISH-15"
      min_lines: 15
      contains: "status:"
    - path: .planning/REQUIREMENTS.md
      provides: "Amended to reflect VoiceOver-only POLISH-01 + NVDA-v12 deferral"
      contains: "NVDA-v12"
    - path: .planning/phases/05-polish-drain/05-07-SUMMARY.md
      provides: "Plan 07 execution summary"
      min_lines: 10
  key_links:
    - from: .planning/phases/05-polish-drain/05-VERIFICATION.md
      to: .planning/phases/05-polish-drain/05-UI-SPEC.md
      via: "Per-item pass criteria specified in UI-SPEC.md §'Human-QA Walkthrough Contract'"
      pattern: "POLISH-0[1-5]|POLISH-15"
---

<objective>
Execute the single prod-site human-QA walkthrough (CONTEXT.md D-01) against https://bgscheduler.vercel.app and record results in `05-VERIFICATION.md` using the one-line-per-item format (D-03). Simultaneously amend `.planning/REQUIREMENTS.md` to reflect the VoiceOver-only relaxation (D-02) with `NVDA-v12` added to the v1.2+ deferred list.

Purpose: closes POLISH-01 through POLISH-05 and POLISH-15 in one sitting, matching the user's explicit "one prod-site sitting" preference. All pass criteria are specified verbatim in UI-SPEC.md §"Human-QA Walkthrough Contract" — this plan's executor does NOT reinterpret them; it reads them and records user verdicts.

**This plan is NOT autonomous.** It pauses for human walkthrough at the prod site. Claude drives the checklist presentation and records the results; the user walks through the UI, VoiceOver, forced failures, etc.

**This plan requires prior production deployment.** Plans 05-01 through 05-06 must be merged / deployed to https://bgscheduler.vercel.app before this plan runs. POLISH-09's `--today-indicator` token must be live on production for POLISH-03 to verify it.

Output: 2 modified/created files in `.planning/`, 2 atomic commits (amendment + verification), `05-07-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-drain/05-CONTEXT.md
@.planning/phases/05-polish-drain/05-UI-SPEC.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Amend REQUIREMENTS.md — VoiceOver-only POLISH-01 + NVDA-v12 deferred</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (current file — focus on line 53 POLISH-01 text, the §"v1.2+ Requirements" section around line 75 and the §"Traceability" table around line 137),
    .planning/phases/05-polish-drain/05-CONTEXT.md §"Human-QA Execution (POLISH-01..05)" D-02 (explicit amendment: POLISH-01 is relaxed to VoiceOver-only; NVDA deferred to v1.2 as `NVDA-v12`; recorded in traceability table; added to v1.2+ deferred list)
  </read_first>
  <action>
Make three surgical edits to `.planning/REQUIREMENTS.md`.

**Edit 1 — Amend POLISH-01 text at line 53.**

BEFORE:
    - [ ] **POLISH-01**: Screen-reader AT QA signed off on VoiceOver + NVDA for search + compare flows

AFTER:
    - [ ] **POLISH-01**: Screen-reader AT QA signed off on VoiceOver for search + compare flows (NVDA deferred to v1.2 as NVDA-v12 per Phase 5 CONTEXT.md D-02, 2026-04-21)

**Edit 2 — Add NVDA-v12 to the §"v1.2+ Requirements" section.**

Locate the first v1.2+ subsection that can accept an accessibility entry — likely §"Advanced Workflow" or a new §"Accessibility (deferred)" subsection. Per the existing file convention, add a new grouping if no natural home exists.

Append (after the existing §"Recommended Slots (deferred)" subsection around line 112, before the §"Out of Scope" heading at line 115):

    ### Accessibility (deferred)

    - **NVDA-v12**: Screen-reader AT QA signed off on NVDA for search + compare flows (Windows-only; deferred from v1.1 POLISH-01 per Phase 5 CONTEXT.md D-02 — user has macOS-only access)

**Edit 3 — Append NVDA-v12 to the §"Traceability" table as an un-mapped deferred entry.**

Per the existing table convention (lines 137-178 — "Requirement | Phase | Status"), add NVDA-v12 as a deferred-to-v1.2 row. The table's existing rows all map v1.1 requirements to Phase 5-10; NVDA-v12 is a v1.2 carry-forward. Append at the end of the table (after line 178):

    | NVDA-v12 | v1.2+ (deferred) | Pending |

Then update the footer counts (around lines 180-182):

    **Coverage:**
    - v1.1 requirements: 40 total
    - Mapped to phases: 40 ✓
    - Unmapped: 0

These counts stay correct — NVDA-v12 is a v1.2+ entry, not a v1.1 requirement, so v1.1 still has 40/40. No change to the coverage stats.

Update the final line (`*Last updated: 2026-04-20 after /gsd-roadmap — all 40 v1.1 requirements mapped to Phases 5–10*`) to:

    *Last updated: 2026-04-21 — Phase 5 CONTEXT.md D-02 amendment: POLISH-01 scoped to VoiceOver-only; NVDA-v12 added to v1.2+ deferred list*

**Step 2 — Commit the amendment as a focused doc commit.**

- `git add .planning/REQUIREMENTS.md`
- `git commit -m "docs(05): amend POLISH-01 to VoiceOver-only; defer NVDA-v12 (D-02)"` with Co-Authored-By trailer.

Do NOT touch any `src/` file or any other `.planning/` file in this commit.
  </action>
  <verify>
    <automated>grep -c "NVDA-v12" .planning/REQUIREMENTS.md | awk '{exit ($1&gt;=3?0:1)}' &amp;&amp; grep -c "VoiceOver for search + compare flows" .planning/REQUIREMENTS.md | awk '{exit ($1&gt;=1?0:1)}' &amp;&amp; grep -c "VoiceOver + NVDA for search" .planning/REQUIREMENTS.md | awk '{exit ($1==0?0:1)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NVDA-v12" .planning/REQUIREMENTS.md` returns at least 3 (POLISH-01 body mention + new v1.2+ bullet + traceability row)
    - `grep -c "VoiceOver for search + compare flows" .planning/REQUIREMENTS.md` returns at least 1 (the amended POLISH-01 text)
    - `grep -c "VoiceOver + NVDA for search" .planning/REQUIREMENTS.md` returns 0 (old text removed)
    - `grep -c "### Accessibility (deferred)" .planning/REQUIREMENTS.md` returns 1 (new subsection header)
    - `grep -cE "^\\| NVDA-v12 \\|" .planning/REQUIREMENTS.md` returns 1 (traceability row)
    - `git log -1 --format="%s"` outputs `docs(05): amend POLISH-01 to VoiceOver-only; defer NVDA-v12 (D-02)`
    - `git log -1 --name-only | grep -c "^.planning/REQUIREMENTS.md$"` returns 1 (single-file commit)
    - No `src/` changes: `git log -1 --name-only | grep -c "^src/"` returns 0
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects the VoiceOver-only scope for POLISH-01 and tracks NVDA-v12 as a v1.2+ deferred item in three places (requirement text, v1.2+ deferred list, traceability table). Single doc commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production deploy is live (MUST precede QA walkthrough)</name>
  <files>(none — this is a pre-QA gate checking prod state, not a file modification)</files>
  <what-built>
    Plans 05-01 through 05-06 have produced code changes and a new attestation doc. Before the user runs the human-QA walkthrough, all code changes (especially POLISH-09 `--today-indicator` token swap from Plan 05-03) MUST be deployed to https://bgscheduler.vercel.app.

    The QA walkthrough checklist (Task 3 below) explicitly verifies the token swap (POLISH-03 pass criterion 5: "Today-indicator line + dot visible in red on current week; same red in light AND dark mode"). Without a prod deploy, the walkthrough cannot pass POLISH-03.
  </what-built>
  <how-to-verify>
    Run these checks before proceeding to Task 3:

    1. Deploy if not already deployed:
       - `npx vercel --prod` (per CLAUDE.md §"Running Commands"), OR
       - If Vercel Git integration handles auto-deploy from `main`, confirm the most recent commit from Plans 05-02/03/04 is present on main and successfully deployed.

    2. Verify production reachability:
       - `curl -sS -o /dev/null -w "%{http_code}\\n" https://bgscheduler.vercel.app/` returns `200` (or 302 redirect to /login — either is fine; means the site is up)

    3. Verify token is live in the built CSS:
       - `curl -sS https://bgscheduler.vercel.app/ | grep -oE "/_next/static/css/[^\\\"']+" | head -1` returns a CSS bundle path
       - `curl -sS https://bgscheduler.vercel.app/_next/static/css/<path from above> | grep -c "today-indicator\\|0.628 0.2577 29.23"` returns at least 1 (token present in built CSS)
       - Alternatively the user spot-checks visually: log into the app, open `/search`, add a tutor to the compare panel, confirm the red today-indicator renders (only if current Bangkok time is between 7AM–9PM, else skip visual spot-check — the CSS-bundle grep above is sufficient).

    4. Confirm no server-side regression by loading `/data-health`:
       - URL: https://bgscheduler.vercel.app/data-health (auth required; log in first)
       - Expected: page renders, shows the latest snapshot timestamp and stats. No 500 error.

    Type "approved" to proceed with the QA walkthrough, OR describe deploy-blocking issues (build failure, 500 on production, missing token in CSS) so this plan can defer to a fix-up.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe the blocking issue.</resume-signal>
  <action>
This is a blocking checkpoint. No files are modified. Claude's task here is to drive the human verification steps listed in `<how-to-verify>` and wait for the resume signal.

Concrete steps for the executor to run / request the user to run:

1. Run the production-reachability curl check: `curl -sS -o /dev/null -w "%{http_code}\n" https://bgscheduler.vercel.app/` — expect 200 or 302. Report the actual code to the user.
2. Run the CSS-bundle token-presence check: `curl -sS https://bgscheduler.vercel.app/ | grep -oE "/_next/static/css/[^\"']+" | head -1` → capture path; then `curl -sS https://bgscheduler.vercel.app<path> | grep -c "today-indicator\\|0.628 0.2577 29.23"`. Report the count (must be ≥ 1 for the POLISH-09 token swap to be live on prod).
3. Ask the user to spot-check `/search` and `/data-health` in a browser; wait for a "yes both load" confirmation.
4. If any of 1-3 fail → report the blocker in-line and do NOT proceed to Task 3. Escalate with the failing command output so the user can deploy or debug.
5. When all three checks pass, request the resume signal verbatim per `<resume-signal>`.

Do NOT modify any file in this task. Do NOT create any commit. The checkpoint's entire job is to gate Task 3.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w "%{http_code}" https://bgscheduler.vercel.app/ | grep -qE "^(200|302)$"</automated>
  </verify>
  <done>
    User has typed "approved". Production is confirmed live at https://bgscheduler.vercel.app, the POLISH-09 token is present in the built CSS bundle, and `/data-health` renders without server error. Task 3 is cleared to begin.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-QA walkthrough against https://bgscheduler.vercel.app (single sitting)</name>
  <files>
    .planning/phases/05-polish-drain/05-VERIFICATION.md (created by this task),
    .planning/phases/05-polish-drain/screenshots/ (directory — created only if at least one QA item fails)
  </files>
  <what-built>
    The user walks through `/search` + `/data-health` on production against the per-item pass criteria in UI-SPEC.md §"Human-QA Walkthrough Contract". Claude presents the checklist verbatim, records the user's pass/fail per item with ISO8601 timestamps, and writes `05-VERIFICATION.md` with one line per item per D-03.

    Claude does NOT navigate the UI. The user does. Claude records.
  </what-built>
  <how-to-verify>
    **Format of `05-VERIFICATION.md` (D-03 contract):**

    Every entry must follow this exact shape:

        ## POLISH-0X — [short item name]
        - status: pass | fail
        - timestamp: 2026-04-21T<HH:MM:SS>+07:00   (Asia/Bangkok ISO8601)
        - notes: [user's one-liner — e.g. "VO announced Main, landmark correctly", or "Toast did not appear; modal spinner hung 5s then reset"]
        - screenshots: [paths only if status = fail; else omit]

    Screenshots are captured ONLY for failing items (D-03). Store under `.planning/phases/05-polish-drain/screenshots/` (create the directory only if at least one fail occurs).

    **Walkthrough sequence (one sitting):**

    | Order | Item | UI-SPEC.md reference | Environment prep |
    |-------|------|----------------------|-------------------|
    | 1 | POLISH-01 | §"POLISH-01 — VoiceOver screen-reader sign-off" (14-step checklist; pass = 12/14) | macOS Safari + VoiceOver on (Cmd+F5), logged in as admin |
    | 2 | POLISH-15 | §"POLISH-15 — v1.0.1 production UAT (bundled)" (6 interaction steps) | Same prod session (already on `/search`) |
    | 3 | POLISH-02 | §"POLISH-02 — Discovery modal error state" (force fail via DevTools Block Request URL, confirm 4 pass criteria) | Chrome DevTools → Network → Block `/api/compare/discover` |
    | 4 | POLISH-03 | §"POLISH-03 — Semantic color tokens in light + dark mode" (7 tokens × 2 themes) | macOS Appearance toggle (System Settings → Appearance) |
    | 5 | POLISH-04 | §"POLISH-04 — /data-health skeleton proportions" (4 pass criteria; Slow 3G throttle) | Chrome DevTools → Network → Slow 3G throttle + hard-refresh |
    | 6 | POLISH-05 | §"POLISH-05 — text-[10px] legibility on production displays" (5 pass criteria; 3 tutors selected) | 13" MacBook intrinsic display, 100% zoom, 3 tutors in compare |

    **If a hard fail occurs (e.g. POLISH-02 error is silent, POLISH-05 text is unreadable):**

    Do NOT modify production code in this plan. Record the fail with evidence and note in `05-07-SUMMARY.md` that the failure rolls forward to a follow-up polish task. UI-SPEC.md §"Fail handling" per item explicitly allows this — e.g. POLISH-05 fail "is NOT scoped to Phase 5" (UI-SPEC.md §POLISH-05 fail handling).

    Type "walkthrough complete" when 6 items are recorded and the commit is made.
  </how-to-verify>
  <resume-signal>Type "walkthrough complete" after recording all 6 items and committing.</resume-signal>
  <action>
Claude's task sequence during the walkthrough:

1. Open a new file at `.planning/phases/05-polish-drain/05-VERIFICATION.md`. Write the header:

        # Phase 05 — Human-QA Verification Record

        **Production URL:** https://bgscheduler.vercel.app
        **Walkthrough date (Asia/Bangkok):** 2026-04-21
        **Format:** per CONTEXT.md D-03 — one section per item, status + ISO8601 timestamp + notes; screenshots on fail only.

        ---

2. For each item in the walkthrough-sequence table (POLISH-01 first, then POLISH-15, POLISH-02, POLISH-03, POLISH-04, POLISH-05 — the order deliberately batches the `/search` prep + VO prep so the user does not keep re-toggling VO):
   a. Read the UI-SPEC.md pass criteria aloud (or paste the item's sub-table into chat). UI-SPEC.md §"Human-QA Walkthrough Contract" has the exhaustive per-item criteria; DO NOT paraphrase — copy verbatim.
   b. Walk user through the environment prep (one line from the table above).
   c. Wait for the user's explicit pass/fail verdict + a one-line note.
   d. Append the entry to `05-VERIFICATION.md` using the exact format in `<how-to-verify>` above (## POLISH-0X — ...; - status: ...; - timestamp: ...; - notes: ...; optional - screenshots: ...).
   e. If fail: request a screenshot from the user, save under `.planning/phases/05-polish-drain/screenshots/POLISH-0X.png`, and add the `- screenshots: [.planning/phases/05-polish-drain/screenshots/POLISH-0X.png]` line to that entry.

3. After all 6 items recorded, write the footer section:

        ## Summary
        - Total items: 6
        - Pass: <count>
        - Fail: <count>
        - Tester: <user handle or email from the active admin_users list — e.g. kevhsh7@gmail.com>

4. Commit:
   - `git add .planning/phases/05-polish-drain/05-VERIFICATION.md`
   - If screenshots exist: `git add .planning/phases/05-polish-drain/screenshots/`
   - `git commit -m "docs(05): human QA walkthrough verdicts (POLISH-01..05, POLISH-15)"` with Co-Authored-By trailer.

5. Wait for the user's "walkthrough complete" resume signal.

Do NOT:
- Touch any `src/` file.
- Fix failing items in this plan. Failures roll forward per each item's §"Fail handling" in UI-SPEC.md.
- Generate placeholder/fake pass verdicts. Every entry must reflect a real user-driven check. If the user cannot complete an item in this sitting (e.g. no Windows machine for NVDA — already covered by the D-02 relaxation; or if the user is offline and cannot reach prod), mark the item as `status: deferred` with notes, and explicitly escalate to the user whether to commit a partial record or retry later.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-polish-drain/05-VERIFICATION.md &amp;&amp; grep -cE "^## POLISH-0[1-5]|^## POLISH-15" .planning/phases/05-polish-drain/05-VERIFICATION.md | awk '{exit ($1&gt;=6?0:1)}' &amp;&amp; grep -cE "^- status: (pass|fail|deferred)$" .planning/phases/05-polish-drain/05-VERIFICATION.md | awk '{exit ($1&gt;=6?0:1)}' &amp;&amp; grep -cE "^- timestamp: 2026-" .planning/phases/05-polish-drain/05-VERIFICATION.md | awk '{exit ($1&gt;=6?0:1)}'</automated>
  </verify>
  <done>
    `05-VERIFICATION.md` exists with six section headers (one per POLISH-01/02/03/04/05/15), each with status + ISO8601 timestamp + notes. Screenshots folder exists only if at least one item failed. One commit on the branch with the mandated message. User has typed "walkthrough complete".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Human-QA is observation-only; Task 1 is a doc amendment.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | R (Repudiation) | human-QA sign-off | mitigate | Every verdict recorded with ISO8601 timestamp + tester handle in `05-VERIFICATION.md`; commit creates a permanent git-log record. |
| T-05-07-02 | I (Information Disclosure) | screenshots (fail path) | accept | Screenshots captured only on fail; stored under `.planning/phases/05-polish-drain/screenshots/` which is inside the repo's tracked directory. No admin PII should be visible if VO walkthrough uses anonymized test data — if real student names appear, tester may blur before committing. Not a gate; flag in 05-07-SUMMARY.md if encountered. |

Phase 5 posture: human verification only. No runtime changes.
</threat_model>

<verification>
After both commits land (amendment + walkthrough):

1. `test -f .planning/phases/05-polish-drain/05-VERIFICATION.md` exits 0
2. `grep -cE "^## POLISH-0[125]|^## POLISH-0[34]|^## POLISH-15" .planning/phases/05-polish-drain/05-VERIFICATION.md` returns at least 6 (one heading per item; matches POLISH-01, -02, -03, -04, -05, -15)
3. `grep -cE "^- status: (pass|fail|deferred)$" .planning/phases/05-polish-drain/05-VERIFICATION.md` returns at least 6
4. `grep -cE "^- timestamp: 2026-" .planning/phases/05-polish-drain/05-VERIFICATION.md` returns at least 6 (ISO8601 timestamps)
5. `grep -c "NVDA-v12" .planning/REQUIREMENTS.md` returns at least 3 (D-02 amendment visible in three places)
6. `git log --oneline | head -4` shows the amendment commit AND the walkthrough commit
7. `npm test` exits 0 (baseline unchanged; no runtime code touched)
</verification>

<success_criteria>
- Two atomic commits: D-02 amendment + human-QA walkthrough record
- `05-VERIFICATION.md` records all 6 items (POLISH-01, -02, -03, -04, -05, -15) with `status:` + ISO8601 timestamp
- Screenshots captured only for fails
- REQUIREMENTS.md amended to reflect VoiceOver-only scope + NVDA-v12 deferred in three locations (body, v1.2+ list, traceability table)
- `05-07-SUMMARY.md` records both commit SHAs, pass/fail counts, any follow-up items that rolled forward
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-drain/05-07-SUMMARY.md` recording: two commit SHAs, pass/fail tally across the 6 QA items, any hard-fail items that rolled forward to v1.2, final test count (should be unchanged from 05-05 plan's final count).
</output>
