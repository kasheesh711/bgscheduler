---
phase: 08.7-operational-maturity
plan: 03
subsystem: ops-ui
tags: [manual-sync, authjs, route-handlers, data-health, nextjs]

requires:
  - phase: 08.7-02
    provides: data-health status area and in-app operational visibility baseline
provides:
  - Authenticated admin-session POST path for `/api/internal/sync-wise`
  - Cron-secret-only GET behavior preserved for Vercel Cron
  - Confirmed `/data-health` Sync now button with in-flight disable and inline feedback
  - Route regression tests for cron and session auth branches
affects: [sync-route, data-health, operational-controls]

tech-stack:
  added: []
  patterns:
    - Explicit sync route auth helper returning `valid | invalid | missing-secret`
    - Browser manual sync uses same-origin session-cookie POST without secret headers
    - Data-health client refresh is centralized in `loadHealthData()`

key-files:
  created:
    - .planning/phases/08.7-operational-maturity/08.7-03-SUMMARY.md
  modified:
    - src/app/api/internal/sync-wise/route.ts
    - src/app/api/internal/sync-wise/__tests__/route.test.ts
    - src/app/(app)/data-health/page.tsx

key-decisions:
  - "GET /api/internal/sync-wise remains cron-secret only; Auth.js session auth is only allowed on POST."
  - "The browser Sync now flow sends a same-origin POST with no Authorization header and no CRON_SECRET reference."
  - "Manual sync feedback stays inline in /data-health; no toast or notification dependency was added."

patterns-established:
  - "Internal sync route branches by auth mechanism before running the shared sync body."
  - "Manual ops actions on /data-health refresh the same health data contract after completion."

requirements-completed: [OPS-05]

duration: 5 min
completed: 2026-05-05
---

# Phase 08.7 Plan 03: Manual Sync Now Summary

**Authenticated in-app manual Wise sync using POST session auth, with cron-secret GET behavior preserved and `/data-health` inline feedback.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-05T03:49:23Z
- **Completed:** 2026-05-05T03:54:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added an explicit `hasValidCronSecret()` helper and `allowSessionAuth` route branch so GET remains cron-secret only while POST can use either a valid cron secret or an authenticated admin session.
- Expanded route tests to cover session-auth POST success, unauthenticated POST 401, cron-secret POST compatibility, cron-secret GET success, and session-only GET 401.
- Added a confirmed `Sync now` control in the top `/data-health` sync status area with `Syncing...` disabled state, exact success/failure feedback, and health-data refresh after completion.

## Task Commits

1. **Task 1 RED: manual sync session auth route tests** - `304735c` (test)
2. **Task 1 GREEN: admin-session POST auth branch** - `a58c8f4` (feat)
3. **Task 2: data-health manual sync button** - `47bf96f` (feat)

## Files Created/Modified

- `src/app/api/internal/sync-wise/route.ts` - Adds the Auth.js session fallback only for POST and keeps `revalidateTag("snapshot", { expire: 0 })` success-gated.
- `src/app/api/internal/sync-wise/__tests__/route.test.ts` - Mocks `auth()` and locks cron/session auth behavior.
- `src/app/(app)/data-health/page.tsx` - Adds reusable `loadHealthData()`, manual sync state, confirmation prompt, status message, and a header-free POST.

## Decisions Made

- Preserved the existing server misconfiguration response when `CRON_SECRET` is missing, while allowing normal browser manual sync to avoid sending or referencing that secret.
- Kept the UI feedback inline in the existing status card instead of adding a toast provider or notification dependency.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None. Stub scan found only the existing skeleton placeholder comment, empty-object JSON parse fallback, and React null/empty state initializers; no user-facing placeholder data was introduced.

## User Setup Required

None - no external service configuration required.

## Verification

- `npm test -- src/app/api/internal/sync-wise/__tests__/route.test.ts` - 1 file, 9 tests passed
- `npm test` - 27 files, 218 tests passed
- `npx tsc --noEmit --project tsconfig.json` - passed
- Acceptance greps confirmed route auth helpers, GET/POST `allowSessionAuth` split, required route test cases, no browser-side `CRON_SECRET`/`Authorization` references, exact confirmation/success/failure strings, exact POST call, and duplicate-click prevention.

## Next Phase Readiness

Ready for 08.7-05. OPS-05 is complete; remaining Phase 8.7 work is compare/modality hygiene.

## Self-Check: PASSED

- Created files exist: `.planning/phases/08.7-operational-maturity/08.7-03-SUMMARY.md`.
- Modified files exist: `src/app/api/internal/sync-wise/route.ts`, `src/app/api/internal/sync-wise/__tests__/route.test.ts`, and `src/app/(app)/data-health/page.tsx`.
- Recorded commits exist: `304735c`, `a58c8f4`, and `47bf96f`.
- Shared tracking files were not updated by this executor per orchestration constraint.

---
*Phase: 08.7-operational-maturity*
*Completed: 2026-05-05*
