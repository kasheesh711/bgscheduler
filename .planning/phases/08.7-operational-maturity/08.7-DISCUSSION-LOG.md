# Phase 8.7: Operational Maturity - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves the alternatives considered.

**Date:** 2026-05-01
**Phase:** 8.7 - Operational Maturity
**Areas discussed:** Sync Failure Visibility, Stale Snapshot Banner, Manual Sync Now UX, Snapshot Pruning Semantics

---

## Sync Failure Visibility

| Option | Description | Selected |
|--------|-------------|----------|
| External notification integration | Implement an external provider from the audit recommendation | |
| Generic notification integration | Implement provider-neutral notification delivery | |
| Email notification | Add email-based failure notification | |
| In-app `/data-health` visibility | Keep failed sync visibility inside the app, centered on `/data-health` | yes |

**User's choice:** `/data-health`.
**Notes:** The initial external-notification direction from the audit was rejected as out of scope. Final decision: no external notification provider, no notification env var, and no browser exposure of cron credentials. `/data-health` is the primary operational failure surface.

---

## Stale Snapshot Banner

| Option | Description | Selected |
|--------|-------------|----------|
| App-level under nav | Shared banner below navigation on `/search` and `/compare` | yes |
| Inside search/compare panels | Local warning inside each affected workspace area | |
| Only on `/data-health` | Keep stale warning only on the health dashboard | |

**User's choice:** App level under nav.
**Notes:** Banner should live in the shared authenticated app shell so it is visible across the scheduling workspace.

| Option | Description | Selected |
|--------|-------------|----------|
| Only stale data over 48h | Show banner only when last successful sync is more than 48 hours old | yes |
| Stale over 48h or latest sync failed | Warn on either old data or latest sync failure | |
| Any failed sync in recent history | Warn if there has been any recent failure | |

**User's choice:** Only stale data over 48h.
**Notes:** Failed sync details remain on `/data-health`; the main workspace warns only when active data is genuinely old.

| Option | Description | Selected |
|--------|-------------|----------|
| Dismiss per browser session | Use `sessionStorage` | yes |
| Dismiss until next successful sync | Track sync identity before showing again | |
| Not dismissible | Persistent warning | |

**User's choice:** Dismiss per browser session.
**Notes:** Simple and sufficient for the admin-only tool.

| Option | Description | Selected |
|--------|-------------|----------|
| Plain admin warning | Neutral wording plus `View data health` link | yes |
| Urgent warning | Stronger destructive warning style | |
| Detailed warning | Include exact sync time and failure summary in banner | |

**User's choice:** Plain admin warning.
**Notes:** Locked copy: `Tutor data may be outdated. Last successful sync was over 48 hours ago.` Include `View data health`.

---

## Manual Sync Now UX

| Option | Description | Selected |
|--------|-------------|----------|
| Top sync status area | Place the button near sync status cards | yes |
| Separate operations card | Add a dedicated operations section | |
| Recent Sync History header | Put action near sync run table | |

**User's choice:** Top sync status area.

| Option | Description | Selected |
|--------|-------------|----------|
| Confirm first | Require confirmation before starting the long Wise sync | yes |
| Run immediately | One-click sync | |
| Only confirm when data is fresh | Conditional confirmation | |

**User's choice:** Confirm first.

| Option | Description | Selected |
|--------|-------------|----------|
| Disable and show progress state | Prevent duplicate requests while sync is running | yes |
| Allow repeated clicks | Permit duplicate starts | |
| Fire-and-forget | Start sync without waiting for result | |

**User's choice:** Disable and show progress state.

| Option | Description | Selected |
|--------|-------------|----------|
| Show success/failure feedback, then refresh health data | Show result and reload cards/history | yes |
| Refresh page only | Reload without explicit feedback | |
| Navigate to recent sync history | Jump to the history table | |

**User's choice:** Show success/failure feedback, then refresh health data.

| Option | Description | Selected |
|--------|-------------|----------|
| Authenticated admin-session POST path | Same route accepts browser POST through Auth.js session | yes |
| Separate `/api/data-health/sync-now` route | New route wraps sync trigger | |
| Expose cron secret to browser | Not acceptable | |

**User's choice:** Add authenticated admin-session POST path.
**Notes:** Vercel cron remains protected by `CRON_SECRET`; browser manual sync uses the Auth.js session cookie.

---

## Snapshot Pruning Semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Keep latest 30 by `createdAt`, preserve active | Retain 30 newest snapshots and always keep active snapshot | yes |
| Keep latest 30 including active if naturally included | Simpler but less defensive | |
| Keep latest 60 | More history, more storage | |

**User's choice:** Keep latest 30 snapshots by `createdAt`, always preserve active snapshot.

| Option | Description | Selected |
|--------|-------------|----------|
| Manual ordered deletes | Delete child rows first, then snapshots | yes |
| Add `ON DELETE CASCADE` migrations | Change FK behavior broadly | |
| Raw SQL multi-table cascade script | Compact but less aligned with Drizzle patterns | |

**User's choice:** Manual ordered deletes of snapshot-scoped tables.

| Option | Description | Selected |
|--------|-------------|----------|
| After successful promotion and sync run success update | Run cleanup after the sync is already successful | yes |
| After promotion but before sync run success update | Make pruning part of success path | |
| Before promotion | Cleanup before live snapshot is known good | |

**User's choice:** After successful promotion and sync run success update.

| Option | Description | Selected |
|--------|-------------|----------|
| Log and record metadata, sync remains successful | Cleanup failure is visible but does not rewrite sync outcome | yes |
| Mark sync as failed | Treat pruning failure as sync failure | |
| Retry inline until success | Risk function timeout | |

**User's choice:** Log and record metadata, but do not flip sync to failed.

| Option | Description | Selected |
|--------|-------------|----------|
| Do not prune `past_session_blocks` | Leave cross-snapshot past sessions untouched | yes |
| Prune rows older than 1 year | Add retention for past sessions | |
| Prune rows linked to deleted snapshots | Invalid for cross-snapshot design | |

**User's choice:** No, explicitly untouched.

---

## the agent's Discretion

- Dependency cleanup exact mechanics.
- Package pinning exact lockfile changes.
- Pruning metadata key names.
- Banner component implementation shape.
- Tests needed to lock the OPS behavior.

## Deferred Ideas

- External notification integrations.
- CI coverage threshold gate.
- `past_session_blocks` retention policy.
