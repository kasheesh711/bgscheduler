---
phase: 08.7-operational-maturity
plan: 01
subsystem: sync-operations
tags: [drizzle-orm, postgres, snapshot-retention, sync-orchestrator, integration-tests]

requires:
  - phase: 08.6-test-coverage-hardening
    provides: "Docker-backed real-Postgres integration test infrastructure"
provides:
  - "Snapshot pruning helper retaining the latest 30 snapshots plus the active snapshot"
  - "Post-success sync orchestrator pruning integration with metadata capture"
  - "Real-Postgres regression coverage for pruning and promoted sync cleanup"
affects: [08.7-operational-maturity, data-health, sync-observability]

tech-stack:
  added: []
  patterns:
    - "Manual ordered deletes for snapshot-scoped tables before deleting snapshots"
    - "Post-success cleanup errors recorded in sync_runs.metadata without changing SyncResult.success"

key-files:
  created:
    - "src/lib/sync/snapshot-pruning.ts"
    - "src/lib/sync/__tests__/snapshot-pruning.integration.test.ts"
  modified:
    - "src/lib/sync/orchestrator.ts"
    - "src/lib/sync/__tests__/orchestrator.integration.test.ts"

key-decisions:
  - "Pruning protects latest 30 snapshots by createdAt plus any active snapshot; prior active snapshots can be pruned after successful promotion because they are no longer active."
  - "sync_runs.snapshot_id and sync_runs.promoted_snapshot_id are nulled before deleting pruned snapshots to satisfy FK constraints without preserving old snapshots indefinitely."
  - "Pruning failures are logged and stored under sync_runs.metadata.pruning while the successful promoted sync remains success=true."

patterns-established:
  - "OPS cleanup helpers return structured metadata suitable for sync_runs.metadata."
  - "Real-Postgres integration tests seed snapshot-scoped child rows to prove manual delete ordering."

requirements-completed: [OPS-01]

duration: 9min
completed: 2026-05-05
---

# Phase 08.7 Plan 01: Snapshot Pruning Summary

**Bounded Wise snapshot retention with post-promotion pruning metadata and regression tests protecting active snapshots and cross-snapshot past sessions**

## Performance

- **Duration:** 9 min
- **Started:** 2026-05-05T03:17:25Z
- **Completed:** 2026-05-05T03:26:11Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Added `pruneOldSnapshots` with `SNAPSHOT_RETENTION_COUNT = 30`, latest-30 retention by `createdAt`, active snapshot protection, sync-run FK nulling, ordered child deletes, row counts, and duration metadata.
- Integrated pruning after successful promotion and after the success sync-run update, gated on non-null `promotedSnapshotId`.
- Preserved sync success on pruning failure while logging and recording `{ attempted: true, failed: true, error }` in `sync_runs.metadata.pruning`.
- Added real-Postgres tests proving latest-30 retention, active preservation, older inactive deletion, sync-run nulling, no-op behavior, and unchanged `past_session_blocks` row count.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Snapshot pruning regression tests** - `3058f88` (test)
2. **Task 1 GREEN: Snapshot pruning helper** - `19eee08` (feat)
3. **Task 2 RED: Orchestrator pruning integration tests** - `963b83f` (test)
4. **Task 2 GREEN: Post-success orchestrator pruning** - `a4922cc` (feat)
5. **Verification fix: Type-safe pruning test assertions** - `8ede565` (fix)

## Files Created/Modified

- `src/lib/sync/snapshot-pruning.ts` - New pruning helper with retention calculation, sync-run reference nulling, ordered snapshot-scoped deletes, and metadata result.
- `src/lib/sync/__tests__/snapshot-pruning.integration.test.ts` - New real-Postgres pruning regression suite with `past_session_blocks` sentinel coverage.
- `src/lib/sync/orchestrator.ts` - Calls pruning after successful sync-run update only when a snapshot was promoted; records success or failure pruning metadata.
- `src/lib/sync/__tests__/orchestrator.integration.test.ts` - Adds promoted-sync pruning coverage and verifies no pruning metadata on no-promote syncs.

## Decisions Made

- Followed D-15/D-16 with manual ordered deletes instead of schema cascade changes.
- Preserved only the currently active snapshot beyond the retention set; after promotion, the new snapshot is active and previous snapshots are eligible by age.
- Kept `past_session_blocks` outside the helper entirely; the helper has no import, query, update, delete, or string reference to that table.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed non-type-safe integration test helper**
- **Found during:** Final verification after Task 2
- **Issue:** `npx tsc --noEmit --project tsconfig.json` failed because a generic Drizzle helper passed an `unknown` column to `inArray`.
- **Fix:** Replaced the generic helper with explicit typed Drizzle queries for each asserted snapshot-scoped table.
- **Files modified:** `src/lib/sync/__tests__/snapshot-pruning.integration.test.ts`
- **Verification:** `npx tsc --noEmit --project tsconfig.json` passed; integration suites still passed.
- **Committed in:** `8ede565`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** No product scope change. The fix only made the new regression test type-safe.

## Issues Encountered

None blocking.

## User Setup Required

None - no external service configuration required. No notification provider or notification environment variable was added.

## Verification

- `npm run test:integration -- src/lib/sync/__tests__/snapshot-pruning.integration.test.ts` - passed (1 file, 2 tests)
- `npm run test:integration -- src/lib/sync/__tests__/orchestrator.integration.test.ts src/lib/sync/__tests__/snapshot-pruning.integration.test.ts` - passed (2 files, 5 tests)
- `npm test` - passed (26 files, 208 tests)
- `npx tsc --noEmit --project tsconfig.json` - passed
- `rg -n "pastSessionBlocks|past_session_blocks" src/lib/sync/snapshot-pruning.ts` - no matches

## Next Phase Readiness

OPS-01 is complete. Phase 08.7 can continue to in-app sync visibility, stale banner, manual sync UI, and dependency hygiene plans without schema changes or external notification work.

## Self-Check: PASSED

- Found `src/lib/sync/snapshot-pruning.ts`
- Found `src/lib/sync/__tests__/snapshot-pruning.integration.test.ts`
- Found `.planning/phases/08.7-operational-maturity/08.7-01-SUMMARY.md`
- Found commits `3058f88`, `19eee08`, `963b83f`, `a4922cc`, and `8ede565`
- No `pastSessionBlocks` or `past_session_blocks` references exist in `src/lib/sync/snapshot-pruning.ts`

---
*Phase: 08.7-operational-maturity*
*Plan: 01*
*Completed: 2026-05-05*
