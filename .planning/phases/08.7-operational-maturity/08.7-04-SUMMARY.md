---
phase: 08.7-operational-maturity
plan: 04
subsystem: infra
tags: [dependencies, npm, authjs, drizzle, shadcn]

requires:
  - phase: 08.7-operational-maturity
    provides: OPS-06/OPS-07 dependency hygiene plan context
provides:
  - Runtime dependency cleanup for unused Auth Drizzle adapter
  - Exact package pins for next-auth, @base-ui/react, and drizzle-orm
  - Lockfile-consistent shadcn devDependency move
affects: [runtime-dependencies, build, package-lock]

tech-stack:
  added: []
  patterns:
    - npm lockfile regeneration via npm install --package-lock-only

key-files:
  created:
    - .planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md
  modified:
    - package.json
    - package-lock.json

key-decisions:
  - "Kept the existing Next.js, React, Tailwind, Neon, Wise, and test package versions unchanged while cleaning dependency declarations."
  - "Moved shadcn to devDependencies because it is CLI-only while keeping shadcn/tailwind.css available through the lockfile."

patterns-established:
  - "Package hygiene plans document package impact and build-backed bundle impact in the phase summary."

requirements-completed: [OPS-06, OPS-07]

duration: 4 min
completed: 2026-05-05
---

# Phase 08.7 Plan 04: Dependency Cleanup and Version Pinning Summary

**Runtime dependency surface reduced by removing unused Auth adapter code, moving the shadcn CLI to dev-only, and exact-pinning the requested Auth.js/Base UI/Drizzle packages.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-05T03:41:31Z
- **Completed:** 2026-05-05T03:45:11Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Removed `@auth/drizzle-adapter` from runtime dependencies and from `package-lock.json`.
- Moved `shadcn` from runtime dependencies to `devDependencies` at the existing `^4.1.2` version.
- Exact-pinned `next-auth` to `5.0.0-beta.30`, `@base-ui/react` to `1.3.0`, and `drizzle-orm` to `0.45.2`.
- Verified the package cleanup against unit tests, TypeScript, and a production Next.js build.

## Package and Bundle Impact

Package impact: removed unused `@auth/drizzle-adapter` from runtime dependencies, moved CLI-only `shadcn` to `devDependencies`, and exact-pinned `next-auth`, `@base-ui/react`, and `drizzle-orm`.

Bundle impact: no app bundle size delta expected from moving CLI-only shadcn to devDependencies and removing unused @auth/drizzle-adapter; npm run build completed successfully after lockfile update.

## Task Commits

Each task was handled atomically:

1. **Task 1: Update package declarations and lockfile** - `4aeeef1` (chore)
2. **Task 2: Verify dependency cleanup and document impact** - docs commit created after this summary file was written

## Files Created/Modified

- `package.json` - Removed the unused adapter, moved `shadcn`, and exact-pinned the requested packages.
- `package-lock.json` - Regenerated with npm so root declarations and dev/runtime flags match `package.json`.
- `.planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md` - Documents package and bundle impact.

## Verification

- `npm install --package-lock-only` - initial run hit npm cache ownership `EPERM`; rerun with `npm_config_cache=/private/tmp/npm-cache` passed.
- `node -e 'const p=require("./package.json"); ...'` - passed exact dependency/dependency-section assertions.
- `rg -n '"@auth/drizzle-adapter"' package.json package-lock.json` - no matches.
- `rg -n '"next-auth": "5.0.0-beta.30"|"@base-ui/react": "1.3.0"|"drizzle-orm": "0.45.2"' package.json package-lock.json` - found exact root declarations in both files.
- `npm test` - passed: 27 files, 215 tests.
- `npx tsc --noEmit --project tsconfig.json` - passed.
- `npm run build` - passed with Next.js 16.2.2.
- `rg -n "webhook|notification|NOTIFICATION" package.json package-lock.json .env.example src/lib/env.ts` - no matches.

## Decisions Made

- Followed the plan's package-only scope. OPS-07 code-hygiene items for `detectConflicts` and modality medium-tier TODO remain assigned to Plan 05.
- Did not add notification providers, notification env vars, or any source-code documentation file.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `npm install --package-lock-only` failed once because the user npm cache has root-owned files. The plan specified the fallback, and `npm_config_cache=/private/tmp/npm-cache npm install --package-lock-only` completed successfully.
- `npm install` reported 12 audit findings. They were not changed because this plan is limited to the specified package cleanup and exact pins.
- `npm run build` emitted the existing Next.js middleware-to-proxy deprecation warning, but the build completed successfully.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None.

## Threat Flags

None - no new endpoints, auth paths, file access patterns, schema changes, or notification surfaces were introduced.

## Next Phase Readiness

Ready for Phase 08.7 Plan 05 to complete the remaining OPS-07 code-hygiene work outside package declarations.

## Self-Check: PASSED

- `package.json` exists and contains the requested dependency declarations.
- `package-lock.json` exists and has no `@auth/drizzle-adapter` entry.
- Task commit `4aeeef1` exists in git history.
- Verification commands above passed.

---
*Phase: 08.7-operational-maturity*
*Completed: 2026-05-05*
