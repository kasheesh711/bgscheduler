---
phase: 08.7-operational-maturity
plan: 04
type: execute
wave: 1
depends_on: []
requirements: [OPS-06, OPS-07]
files_modified:
  - package.json
  - package-lock.json
  - .planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - "@auth/drizzle-adapter is removed from runtime dependencies"
    - "shadcn is a devDependency, not a runtime dependency"
    - "next-auth, @base-ui/react, and drizzle-orm are exact-pinned to the requested versions"
    - "Lockfile reflects package.json without changing the application stack"
  artifacts:
    - path: "package.json"
      provides: "Dependency declaration cleanup"
    - path: "package-lock.json"
      provides: "Resolved lockfile after cleanup"
    - path: ".planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md"
      provides: "Package and bundle impact documentation"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install --package-lock-only"
      pattern: "\"next-auth\": \"5.0.0-beta.30\""
---

<objective>
Clean up risky or unused dependency declarations.

Purpose: Reduce runtime dependency surface and lock unstable packages to known-good versions while keeping the existing Next.js 16 / Auth.js / Drizzle / shadcn stack.

Output: Updated `package.json` and `package-lock.json`.
</objective>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.7-operational-maturity/08.7-CONTEXT.md
@.planning/phases/08.7-operational-maturity/08.7-RESEARCH.md
@package.json
@package-lock.json
@src/lib/auth.ts
@src/app/globals.css

<interfaces>
Current required package targets:
```json
{
  "dependencies": {
    "@base-ui/react": "1.3.0",
    "drizzle-orm": "0.45.2",
    "next-auth": "5.0.0-beta.30"
  },
  "devDependencies": {
    "shadcn": "^4.1.2"
  }
}
```
</interfaces>
</context>

<must_haves>
- OPS-06 is fully covered by removing `@auth/drizzle-adapter` and moving `shadcn` out of runtime dependencies.
- The package-version portion of OPS-07 is fully covered by exact-pinning `next-auth`, `@base-ui/react`, and `drizzle-orm`.
- No stack replacement is allowed.
- No notification provider or notification env var is added.
</must_haves>

<tasks>
<task type="auto">
  <name>Task 1: Update package declarations and lockfile</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/08.7-operational-maturity/08.7-CONTEXT.md
    - .planning/phases/08.7-operational-maturity/08.7-RESEARCH.md
    - package.json
    - package-lock.json
    - src/lib/auth.ts
    - src/app/globals.css
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Edit `package.json` only for the requested dependency hygiene:
    - Remove `"@auth/drizzle-adapter"` from `dependencies`.
    - Move `"shadcn": "^4.1.2"` from `dependencies` to `devDependencies`.
    - Change `"next-auth": "^5.0.0-beta.30"` to exact `"next-auth": "5.0.0-beta.30"`.
    - Change `"@base-ui/react": "^1.3.0"` to exact `"@base-ui/react": "1.3.0"`.
    - Change `"drizzle-orm": "^0.45.2"` to exact `"drizzle-orm": "0.45.2"`.

    Do not remove `@base-ui/react`, `next-auth`, `drizzle-orm`, `shadcn`, or `shadcn/tailwind.css`; these are still used. Do not change Next.js, React, Tailwind, Neon, Wise, or test dependency versions.

    Run `npm install --package-lock-only` to update `package-lock.json` without installing/changing source files. If npm cache permission issues appear, rerun with `npm_config_cache=/private/tmp/npm-cache npm install --package-lock-only`.

    In the eventual `.planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md`, record package delta: removed `@auth/drizzle-adapter` runtime dependency, moved `shadcn` to devDependencies, and exact-pinned the three risky packages. Also reserve a `Bundle impact:` line for Task 2 to fill after `npm run build`. No source-code documentation file is required in this plan.
  </action>
  <verify>
    <automated>npm install --package-lock-only</automated>
  </verify>
  <acceptance_criteria>
    - `node -e 'const p=require("./package.json"); if (p.dependencies["@auth/drizzle-adapter"]) process.exit(1); if (p.dependencies.shadcn) process.exit(2); if (p.devDependencies.shadcn !== "^4.1.2") process.exit(3); if (p.dependencies["next-auth"] !== "5.0.0-beta.30") process.exit(4); if (p.dependencies["@base-ui/react"] !== "1.3.0") process.exit(5); if (p.dependencies["drizzle-orm"] !== "0.45.2") process.exit(6);'` exits 0.
    - `rg -n '"@auth/drizzle-adapter"' package.json package-lock.json` returns no matches.
    - `rg -n '"next-auth": "5.0.0-beta.30"|"@base-ui/react": "1.3.0"|"drizzle-orm": "0.45.2"' package.json package-lock.json` finds exact root package declarations.
  </acceptance_criteria>
  <done>Package declarations and lockfile reflect the requested dependency cleanup exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Verify dependency cleanup does not break runtime/test behavior</name>
  <read_first>
    - package.json
    - package-lock.json
    - vitest.config.ts
    - src/lib/auth.ts
    - src/app/globals.css
  </read_first>
  <files>package.json, package-lock.json, .planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md</files>
  <action>
    Run the verification commands below. If `npm run build` fails due to a code issue caused by dependency declarations, fix only the package declaration/lockfile cause. Do not refactor source code in this plan.

    In `.planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md`, add a bundle-impact note backed by `npm run build`. If no before/after bundle baseline is available, use this exact sentence:
    `Bundle impact: no app bundle size delta expected from moving CLI-only shadcn to devDependencies and removing unused @auth/drizzle-adapter; npm run build completed successfully after lockfile update.`
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npx tsc --noEmit --project tsconfig.json</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes.
    - `npx tsc --noEmit --project tsconfig.json` passes.
    - `npm run build` passes.
    - `.planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md` contains `Bundle impact:` and `npm run build completed successfully after lockfile update`.
    - `rg -n "webhook|notification|NOTIFICATION" package.json package-lock.json .env.example src/lib/env.ts` returns no new notification provider/env references.
  </acceptance_criteria>
  <done>Dependency cleanup is lockfile-consistent and verified against tests, TypeScript, and production build.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> lockfile | Dependency metadata is resolved into `package-lock.json`. |
| Runtime bundle -> admin browser/server | Runtime dependency set affects shipped attack surface and build behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.7-13 | Tampering | `package-lock.json` | mitigate | Use npm lockfile regeneration and verify exact package declarations with Node/rg checks. |
| T-08.7-14 | Elevation of Privilege | Auth dependency | mitigate | Pin `next-auth` exactly to `5.0.0-beta.30`; run auth/route tests through `npm test`. |
| T-08.7-15 | Denial of Service | Build/runtime dependencies | mitigate | Run `npm run build` after lockfile update. |
| T-08.7-16 | Information Disclosure | Removed adapter | accept | `@auth/drizzle-adapter` is unused because auth uses custom allowlist callback; removal reduces unused code surface. |
</threat_model>

<verification>
Automated commands:
- `npm install --package-lock-only`
- `npm test`
- `npx tsc --noEmit --project tsconfig.json`
- `npm run build`
</verification>

<success_criteria>
- OPS-06 is satisfied in full.
- OPS-07 package pinning is satisfied in full.
- No source code or environment surface outside package declarations changes in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md`.
</output>
