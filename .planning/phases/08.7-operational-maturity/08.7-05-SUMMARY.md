---
phase: 08.7-operational-maturity
plan: 05
subsystem: code-hygiene
tags: [compare, modality, vitest, typescript]

requires:
  - phase: 08.7-02
    provides: compare route baseline and OPS-07 operational context
  - phase: 06-mod-01-reliable-modality-detection
    provides: modality confidence contract and medium-tier reservation
provides:
  - Clean one-argument `detectConflicts` API
  - One-argument compare and discovery route call sites
  - Regression coverage for modality display labels across high, low, unknown, and medium fallback cases
  - Intentional medium-confidence modality display documentation
affects: [compare-api, compare-discovery, compare-engine, modality-display, OPS-07]

tech-stack:
  added: []
  patterns:
    - TDD red/green coverage for small code-hygiene changes
    - Source hygiene assertion for TODO-free intentional medium-tier documentation

key-files:
  created:
    - src/components/compare/__tests__/modality-display.test.ts
  modified:
    - src/lib/search/compare.ts
    - src/lib/search/__tests__/compare.test.ts
    - src/app/api/compare/route.ts
    - src/app/api/compare/discover/route.ts
    - src/components/compare/modality-display.ts

key-decisions:
  - "Removing the unused `indexedGroups` parameter is a pure API cleanup; conflict detection behavior remains driven only by `CompareTutor[]`."
  - "Medium modality confidence remains type-reserved and intentionally follows the high-confidence display branch until a producer emits it."

patterns-established:
  - "`detectConflicts.length` is pinned by test coverage so the public helper signature cannot regress silently."
  - "Medium-tier modality fallback behavior is covered by label tests plus source-level comment hygiene."

requirements-completed: [OPS-07]

duration: 4 min
completed: 2026-05-05
---

# Phase 08.7 Plan 05: Small Code Hygiene Summary

**One-argument compare conflict detection and intentional medium-tier modality display coverage for OPS-07.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-05T03:56:50Z
- **Completed:** 2026-05-05T04:01:15Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Removed the unused `indexedGroups` parameter from `detectConflicts` without changing same-student overlap behavior.
- Updated compare and discovery route call sites, plus compare tests, to use the clean one-argument API.
- Replaced the modality medium-tier TODO with an explicit intentional fallback comment.
- Added modality display tests for high, low/unknown, and medium fallback labels.

## Task Commits

1. **Task 1 RED: detectConflicts API regression test** - `91974cb` (test)
2. **Task 1 GREEN: remove unused indexedGroups parameter** - `0a5051e` (refactor)
3. **Task 2 RED: modality display label and hygiene tests** - `4eef16e` (test)
4. **Task 2 GREEN: document medium fallback** - `cdd4d9f` (refactor)

## Files Created/Modified

- `src/lib/search/compare.ts` - `detectConflicts` now accepts only `CompareTutor[]`.
- `src/lib/search/__tests__/compare.test.ts` - Updated conflict tests to one-argument calls and added a signature regression test.
- `src/app/api/compare/route.ts` - Calls `detectConflicts(allCompareTutors)`.
- `src/app/api/compare/discover/route.ts` - Removes dead `allIndexedGroups` construction and calls `detectConflicts(allCompareTutors)`.
- `src/components/compare/modality-display.ts` - Replaces the open TODO with intentional medium-confidence fallback documentation.
- `src/components/compare/__tests__/modality-display.test.ts` - Adds label coverage for high, low/unknown, and medium fallback behavior.

## Decisions Made

- Conflict behavior was intentionally left unchanged; the removed argument was unused and did not participate in matching, overlap checks, or deduping.
- Medium confidence is still not emitted by current resolvers, so the display helper keeps medium on the same branch as high confidence until a producer exists.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None. Stub scan found only ordinary typed empty-array initializers and test assertions; no user-facing placeholder data or unwired UI stubs were introduced.

## Threat Flags

None. The plan changed existing helper signatures, route call sites, comments, and tests only; it introduced no new endpoint, auth path, file access, or schema trust boundary.

## User Setup Required

None - no external service configuration required.

## Verification

- `npm test -- src/lib/search/__tests__/compare.test.ts src/app/api/compare/__tests__/route.test.ts src/app/api/compare/discover/__tests__/route.test.ts` - passed, 3 files / 51 tests.
- `npm test -- src/components/compare/__tests__/modality-display.test.ts` - passed, 1 file / 4 tests.
- `npm test -- src/lib/search/__tests__/compare.test.ts src/app/api/compare/__tests__/route.test.ts src/app/api/compare/discover/__tests__/route.test.ts src/components/compare/__tests__/modality-display.test.ts` - passed, 4 files / 55 tests.
- `npm test` - passed, 28 files / 223 tests.
- `npx tsc --noEmit --project tsconfig.json` - passed.
- Acceptance greps confirmed one-argument `detectConflicts`, no `allIndexedGroups`, no modality-display TODO/future-phase text, the intentional medium comment, and required label assertions.

## Next Phase Readiness

OPS-07 code hygiene is complete for the compare helper and modality display surfaces. Shared tracking files were intentionally not updated by this executor because the orchestrator owns `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md`.

## Self-Check: PASSED

- Created files exist: `.planning/phases/08.7-operational-maturity/08.7-05-SUMMARY.md` and `src/components/compare/__tests__/modality-display.test.ts`.
- Recorded commits exist: `91974cb`, `0a5051e`, `4eef16e`, `cdd4d9f`.
- Shared tracking files were not staged or updated by this executor per orchestration constraint.

---
*Phase: 08.7-operational-maturity*
*Completed: 2026-05-05*
