---
phase: 08.7-operational-maturity
plan: 02
subsystem: ops-ui
tags: [stale-data, data-health, app-shell, vitest, nextjs]

requires:
  - phase: 08.7-01
    provides: snapshot pruning baseline and operational maturity context
provides:
  - Shared stale-data thresholds and helper predicates
  - 26-hour API/search stale warning threshold aligned to daily cron cadence
  - App-level stale snapshot banner for search and compare routes
  - Data-health stale badge using the shared API stale helper
affects: [search, compare, data-health, app-layout, operational-visibility]

tech-stack:
  added: []
  patterns:
    - Shared ops constants in `src/lib/ops/stale.ts`
    - App-shell client banner mounted below `AppNav`
    - Session-scoped browser dismissal via `sessionStorage`

key-files:
  created:
    - src/lib/ops/stale.ts
    - src/lib/ops/__tests__/stale.test.ts
    - src/components/layout/stale-snapshot-banner.tsx
  modified:
    - src/lib/search/engine.ts
    - src/lib/search/__tests__/engine.test.ts
    - src/app/api/compare/route.ts
    - src/app/api/compare/__tests__/route.test.ts
    - src/app/api/compare/discover/route.ts
    - src/app/api/compare/discover/__tests__/route.test.ts
    - src/app/(app)/layout.tsx
    - src/app/(app)/data-health/page.tsx

key-decisions:
  - "API stale warnings use a 26-hour threshold while the app banner uses a stricter 48-hour threshold."
  - "The stale banner reuses `/api/data-health` and only reads `staleAgeMs`; failed sync metadata alone does not trigger the banner."
  - "Banner dismissal is browser-session scoped with `sessionStorage`, not persisted server-side."

patterns-established:
  - "Operational copy and thresholds live in `src/lib/ops/stale.ts`."
  - "App-level operational warnings mount in the `(app)` layout below shared navigation."

requirements-completed: [OPS-02, OPS-03, OPS-04]

duration: 9 min
completed: 2026-05-05
---

# Phase 08.7 Plan 02: Sync Visibility and Stale Banner Summary

**In-app stale-data visibility with 26-hour API warning thresholds and a dismissible 48-hour search/compare banner backed by existing data-health status.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-05-05T03:29:24Z
- **Completed:** 2026-05-05T03:37:56Z
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments

- Centralized stale threshold constants and helper predicates in `src/lib/ops/stale.ts`.
- Raised search, compare, and discovery API stale warnings from 35 minutes to 26 hours.
- Added a shared app-shell stale-data banner for `/search` and `/compare`, dismissed per browser session and linked to `/data-health`.
- Updated `/data-health` to use the same API stale helper while preserving failed-sync details and recent sync history.

## Task Commits

1. **Task 1 RED: stale threshold boundary tests** - `4fd8e81` (test)
2. **Task 1 GREEN: centralized stale thresholds** - `63b9f5e` (feat)
3. **Task 2 RED: stale banner mount tests** - `61f8384` (test)
4. **Task 2 GREEN: stale snapshot banner** - `6d243f0` (feat)
5. **Task 3: data-health stale badge alignment** - `2c1e649` (fix)
6. **Auto-fix: Vitest cleanup hook imports** - `78e7aa3` (fix)

## Files Created/Modified

- `src/lib/ops/stale.ts` - Shared API/banner stale thresholds, banner copy, session key, and helper predicates.
- `src/lib/ops/__tests__/stale.test.ts` - Boundary tests for 26-hour API stale logic, 48-hour banner logic, and banner/layout integration checks.
- `src/lib/search/engine.ts` - Default search stale threshold now uses `API_STALE_THRESHOLD_MS` and 26-hour warning copy.
- `src/lib/search/__tests__/engine.test.ts` - Search default threshold boundary coverage for 25h59m and 26h+1ms.
- `src/app/api/compare/route.ts` - Compare snapshot stale flag now uses `API_STALE_THRESHOLD_MS` and 26-hour warning copy.
- `src/app/api/compare/__tests__/route.test.ts` - Compare route 26-hour stale boundary coverage.
- `src/app/api/compare/discover/route.ts` - Discovery snapshot stale flag now uses `API_STALE_THRESHOLD_MS`.
- `src/app/api/compare/discover/__tests__/route.test.ts` - Discovery route 26-hour stale boundary coverage.
- `src/app/(app)/layout.tsx` - Mounts `StaleSnapshotBanner` below `AppNav` and before `<main>`.
- `src/components/layout/stale-snapshot-banner.tsx` - Client banner fetching `/api/data-health`, reading only `staleAgeMs`, and storing dismissal in `sessionStorage`.
- `src/app/(app)/data-health/page.tsx` - Stale badge now uses `isApiSnapshotStale(data.staleAgeMs)`.

## Decisions Made

- Kept failed-sync visibility in-app only and reused the existing `/data-health` endpoint rather than adding a new status endpoint.
- Made the app-level banner depend only on `staleAgeMs > 48h`; latest failed sync state remains visible on `/data-health` but does not trigger the banner.
- Preserved the existing `/data-health` layout and status surfaces so Plan 03 can add manual sync in the same top area.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Imported Vitest `afterEach` explicitly**
- **Found during:** Overall TypeScript verification
- **Issue:** The new route tests used `afterEach` with Vitest globals at runtime, but TypeScript required the named import.
- **Fix:** Added `afterEach` to the Vitest imports in both compare route test files.
- **Files modified:** `src/app/api/compare/__tests__/route.test.ts`, `src/app/api/compare/discover/__tests__/route.test.ts`
- **Verification:** `npm test -- src/app/api/compare/__tests__/route.test.ts src/app/api/compare/discover/__tests__/route.test.ts`; `npx tsc --noEmit --project tsconfig.json`
- **Committed in:** `78e7aa3`

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** No scope change. The fix was required for TypeScript verification of the planned tests.

## Issues Encountered

None beyond the auto-fixed TypeScript import issue above.

## Known Stubs

None. Stub scan found only existing empty-array initializers, test fixture defaults, and skeleton placeholder comments; no user-facing placeholder data was introduced.

## User Setup Required

None - no external service configuration required.

## Verification

- `npm test -- src/lib/ops/__tests__/stale.test.ts src/lib/search/__tests__/engine.test.ts src/app/api/compare/__tests__/route.test.ts src/app/api/compare/discover/__tests__/route.test.ts` - 4 files, 30 tests passed
- `npm test` - 27 files, 215 tests passed
- `npx tsc --noEmit --project tsconfig.json` - passed
- Acceptance greps confirmed the exact `26 * 60 * 60 * 1000` threshold, `daily Hobby cron + 2h grace` comment, no remaining 35-minute stale checks in required consumers, exact banner copy/link, sessionStorage dismissal, and no notification-service/env additions.

## Next Phase Readiness

Ready for 08.7-03. `/data-health` still contains the top sync status area and failed-sync details required for the manual sync button plan.

## Self-Check: PASSED

- Created files exist: summary, `src/lib/ops/stale.ts`, `src/lib/ops/__tests__/stale.test.ts`, and `src/components/layout/stale-snapshot-banner.tsx`.
- Recorded commits exist: `4fd8e81`, `63b9f5e`, `61f8384`, `6d243f0`, `2c1e649`, `78e7aa3`.
- Required shared tracking files were not updated by this executor per orchestration constraint.

---
*Phase: 08.7-operational-maturity*
*Completed: 2026-05-05*
