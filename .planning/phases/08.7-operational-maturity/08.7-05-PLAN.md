---
phase: 08.7-operational-maturity
plan: 05
type: execute
wave: 2
depends_on: [08.7-02]
requirements: [OPS-07]
files_modified:
  - src/lib/search/compare.ts
  - src/lib/search/__tests__/compare.test.ts
  - src/app/api/compare/route.ts
  - src/app/api/compare/discover/route.ts
  - src/components/compare/modality-display.ts
  - src/components/compare/__tests__/modality-display.test.ts
autonomous: true
must_haves:
  truths:
    - "detectConflicts has no unused indexedGroups parameter"
    - "All detectConflicts call sites and tests pass one argument"
    - "modality-display.ts has no open TODO for the medium confidence tier"
    - "Compare conflict and modality display behavior is unchanged"
  artifacts:
    - path: "src/lib/search/compare.ts"
      provides: "Clean detectConflicts signature"
    - path: "src/components/compare/modality-display.ts"
      provides: "Intentional medium-tier handling comment"
    - path: "src/components/compare/__tests__/modality-display.test.ts"
      provides: "Regression coverage for modality display labels"
  key_links:
    - from: "src/app/api/compare/route.ts"
      to: "src/lib/search/compare.ts"
      via: "detectConflicts(allCompareTutors)"
      pattern: "detectConflicts\\(allCompareTutors\\)"
    - from: "src/app/api/compare/discover/route.ts"
      to: "src/lib/search/compare.ts"
      via: "detectConflicts(allCompareTutors)"
      pattern: "detectConflicts\\(allCompareTutors\\)"
---

<objective>
Close the small OPS-07 code hygiene items without changing behavior.

Purpose: Remove dead API surface and convert the modality medium-tier TODO into an intentional documented state, keeping compare behavior stable.

Output: One-argument `detectConflicts`, updated call sites/tests, and medium-tier modality display regression coverage.
</objective>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.7-operational-maturity/08.7-CONTEXT.md
@.planning/phases/08.7-operational-maturity/08.7-RESEARCH.md
@src/lib/search/compare.ts
@src/lib/search/__tests__/compare.test.ts
@src/app/api/compare/route.ts
@src/app/api/compare/discover/route.ts
@src/components/compare/modality-display.ts

<interfaces>
Target function signature:
```ts
export function detectConflicts(compareTutors: CompareTutor[]): Conflict[];
```

Existing modality display contract:
```ts
export function modalityDisplay(
  modality: "online" | "onsite" | "unknown",
  confidence: "high" | "medium" | "low",
): { Icon: typeof Video; label: string; ariaLabel: string };
```
</interfaces>
</context>

<must_haves>
- OPS-07 code-hygiene items are fully covered here.
- `detectConflicts` target signature removes the unused `indexedGroups` parameter exactly as requested.
- Medium-tier TODO is removed. Behavior can remain intentionally high-style fallback because Phase 6 emits no `medium` today.
- No package changes are made here; Plan 04 owns package pinning.
</must_haves>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Remove unused detectConflicts indexedGroups parameter</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/08.7-operational-maturity/08.7-CONTEXT.md
    - src/lib/search/compare.ts
    - src/lib/search/__tests__/compare.test.ts
    - src/app/api/compare/route.ts
    - src/app/api/compare/discover/route.ts
  </read_first>
  <files>src/lib/search/compare.ts, src/lib/search/__tests__/compare.test.ts, src/app/api/compare/route.ts, src/app/api/compare/discover/route.ts</files>
  <action>
    Change `detectConflicts` in `src/lib/search/compare.ts` from:
    `export function detectConflicts(compareTutors: CompareTutor[], indexedGroups: IndexedTutorGroup[]): Conflict[]`
    to exact target signature:
    `export function detectConflicts(compareTutors: CompareTutor[]): Conflict[]`.

    Do not change conflict behavior: same-student matching remains case-insensitive, same-tutor overlaps are ignored, weekday and time overlap logic stays the same, and deduping by student/day/tutor pair stays the same.

    Update all call sites:
    - `src/app/api/compare/route.ts`: `detectConflicts(allCompareTutors)`.
    - `src/app/api/compare/discover/route.ts`: remove `allIndexedGroups` and call `detectConflicts(allCompareTutors)`.
    - `src/lib/search/__tests__/compare.test.ts`: all test calls pass only `[compareA, compareB]` or equivalent compare tutor array.
  </action>
  <verify>
    <automated>npm test -- src/lib/search/__tests__/compare.test.ts src/app/api/compare/__tests__/route.test.ts src/app/api/compare/discover/__tests__/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "detectConflicts\\([^\\n]*indexedGroups|indexedGroups: IndexedTutorGroup\\[\\]" src/lib/search/compare.ts src/app/api/compare/route.ts src/app/api/compare/discover/route.ts src/lib/search/__tests__/compare.test.ts` returns no matches.
    - `rg -n "const allIndexedGroups" src/app/api/compare/discover/route.ts` returns no matches.
    - `rg -n "export function detectConflicts\\(compareTutors: CompareTutor\\[\\]\\)" src/lib/search/compare.ts` finds the target signature.
    - Compare unit and route tests pass.
  </acceptance_criteria>
  <done>`detectConflicts` has a clean one-argument API and all call sites/tests match it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace modality medium-tier TODO with intentional behavior and tests</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/08.7-operational-maturity/08.7-CONTEXT.md
    - .planning/phases/06-mod-01-reliable-modality-detection/06-CONTEXT.md
    - src/components/compare/modality-display.ts
  </read_first>
  <files>src/components/compare/modality-display.ts, src/components/compare/__tests__/modality-display.test.ts</files>
  <action>
    In `src/components/compare/modality-display.ts`, remove the open `TODO(future phase)` comment. Replace it with an explicit non-TODO comment:
    `Medium confidence is type-reserved by Phase 6 but no resolver emits it today; until a producer exists, medium intentionally follows the high-confidence display branch.`

    Keep current behavior unless the code already has a clean medium branch available. Add `src/components/compare/__tests__/modality-display.test.ts` covering:
    - `modalityDisplay("online", "high").label === "Online"`
    - `modalityDisplay("onsite", "high").label === "Onsite"`
    - low confidence online returns `Likely online — unconfirmed`
    - unknown/low returns `Unknown`
    - medium online returns `Online` and medium onsite returns `Onsite`, proving the intentional fallback.

    Do not change session-card colors, borders, or modality icons beyond this helper.
  </action>
  <verify>
    <automated>npm test -- src/components/compare/__tests__/modality-display.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "TODO|future phase" src/components/compare/modality-display.ts` returns no matches.
    - `rg -n "Medium confidence is type-reserved by Phase 6" src/components/compare/modality-display.ts` finds the intentional comment.
    - `npm test -- src/components/compare/__tests__/modality-display.test.ts` passes.
    - `rg -n "Likely online|Unknown|Online|Onsite" src/components/compare/__tests__/modality-display.test.ts` finds assertions for labels.
  </acceptance_criteria>
  <done>Medium-tier modality display is intentional and covered by tests, with no open TODO.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compare API -> client | Conflict data and modality labels are serialized for admin UI. |
| Wise-derived session data -> display helper | Modality confidence affects how uncertain data is shown to admins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.7-17 | Tampering | `detectConflicts` refactor | mitigate | No behavior change; compare tests assert existing conflict detection. |
| T-08.7-18 | Information Disclosure | Conflict labels | accept | Existing same-student conflict output is unchanged and already admin-only. |
| T-08.7-19 | Spoofing | Modality display | mitigate | Low/unknown behavior remains fail-closed; medium fallback is documented and tested until a producer exists. |
| T-08.7-20 | Denial of Service | API route refactor | mitigate | Removing unused parameter reduces call-site work and TypeScript surface; route tests verify responses still succeed. |
</threat_model>

<verification>
Automated commands:
- `npm test -- src/lib/search/__tests__/compare.test.ts src/app/api/compare/__tests__/route.test.ts src/app/api/compare/discover/__tests__/route.test.ts src/components/compare/__tests__/modality-display.test.ts`
- `npm test`
- `npx tsc --noEmit --project tsconfig.json`
</verification>

<success_criteria>
- OPS-07 code hygiene is satisfied in full.
- `detectConflicts` has one parameter everywhere.
- `modality-display.ts` contains no TODO marker and has explicit tested behavior for `medium`.
</success_criteria>

<output>
After completion, create `.planning/phases/08.7-operational-maturity/08.7-05-SUMMARY.md`.
</output>
