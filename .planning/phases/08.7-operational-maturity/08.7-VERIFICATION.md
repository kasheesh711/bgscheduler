---
phase: 08.7-operational-maturity
verified: 2026-05-05T04:18:20Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 08.7: Operational Maturity Verification Report

**Phase Goal:** Add the operational features and lower-priority cleanups required to run BGScheduler reliably in production for the next 12+ months - retention, in-app failure visibility, observability, and dependency hygiene
**Verified:** 2026-05-05T04:18:20Z
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | OPS-01: Sync orchestrator prunes snapshots, keeps latest 30 by `createdAt`, preserves active snapshot, leaves `past_session_blocks` untouched, handles sync-run references, and runs only after successful promotion | VERIFIED | `pruneOldSnapshots` orders snapshots by `createdAt DESC`, protects the first 30 plus all active snapshots, then prunes only unprotected IDs (`src/lib/sync/snapshot-pruning.ts:56-74`). It nulls `sync_runs.snapshot_id` / `promoted_snapshot_id` before manually deleting all snapshot-scoped child tables and then `snapshots` (`src/lib/sync/snapshot-pruning.ts:88-179`). The helper has no `pastSessionBlocks` / `past_session_blocks` reference. `runFullSync` updates the sync run to `success` first, then gates pruning on `promotedSnapshotId`, catches pruning and metadata-update failures, and still returns `success: true` (`src/lib/sync/orchestrator.ts:498-549`). |
| 2 | OPS-02: Failed sync runs are visible in-app only through `/data-health`, stale banner, and manual sync feedback; no external provider/env var was added | VERIFIED | `/api/data-health` returns last successful sync, last failed sync, last failure error, stale age, active snapshot, issue counts, and recent sync history (`src/app/api/data-health/route.ts:42-143`). `/data-health` renders Last Successful Sync, Active Snapshot, Last Failed Sync, failure error, issue tables, recent sync history, and manual sync feedback (`src/app/(app)/data-health/page.tsx:167-227`, `:377-421`). Stale banner fetches only `/api/data-health` stale age (`src/components/layout/stale-snapshot-banner.tsx:55-63`). `rg` over `src`, package files, `.env.example`, and `src/lib/env.ts` found no notification/webhook/provider/env references. |
| 3 | OPS-03: Stale-snapshot banner displays on `/search` and `/compare` only when `staleAgeMs > 48h`, is dismissible per session, and links to `/data-health` | VERIFIED | Banner route filter is limited to `/search` and `/compare` paths (`src/components/layout/stale-snapshot-banner.tsx:18-25`). Trigger uses `shouldShowStaleBanner(staleAgeMs)`, whose threshold is strict greater-than `48 * 60 * 60 * 1000` (`src/components/layout/stale-snapshot-banner.tsx:55-63`, `src/lib/ops/stale.ts:3-14`). Dismissal uses `sessionStorage` with `STALE_BANNER_SESSION_KEY` (`src/components/layout/stale-snapshot-banner.tsx:40-45`, `:79-87`). The banner link is `href="/data-health"` and is mounted below `AppNav` before `main` (`src/components/layout/stale-snapshot-banner.tsx:97-104`, `src/app/(app)/layout.tsx:7-9`). |
| 4 | OPS-04: API stale threshold is 26h for search, compare, and discover, with the Hobby cron + 2h grace rationale documented | VERIFIED | Shared constant is `API_STALE_THRESHOLD_MS = 26 * 60 * 60 * 1000` with comment `daily Hobby cron + 2h grace` (`src/lib/ops/stale.ts:1-2`). Required consumers import/use the constant: search default threshold and warning copy (`src/lib/search/engine.ts:17-37`), compare `snapshotMeta.stale` and 26h warning (`src/app/api/compare/route.ts:17-91`), and discover `snapshotMeta.stale` (`src/app/api/compare/discover/route.ts:8-62`). Grep found no remaining `35 * 60 * 1000` or `35 minutes` in the required consumers. |
| 5 | OPS-05: Manual Sync now uses authenticated session POST; GET remains cron-only; browser never sends `CRON_SECRET`/`Authorization` | VERIFIED | Sync route keeps timing-safe cron-secret validation (`src/app/api/internal/sync-wise/route.ts:13-31`). `GET` calls `handleSync(... allowSessionAuth: false)`, while `POST` allows session auth and calls `auth()` when the cron secret is absent/invalid (`src/app/api/internal/sync-wise/route.ts:49-83`). Auth.js sign-in is admin allowlist-backed (`src/lib/auth.ts:7-23`). Data Health calls `fetch("/api/internal/sync-wise", { method: "POST" })` with no Authorization header, confirms first, disables during sync, shows success/failure feedback, and refreshes health data (`src/app/(app)/data-health/page.tsx:112-145`, `:181-195`). Route tests cover POST session success, POST unauthenticated 401, POST cron compatibility, and GET session-only rejection (`src/app/api/internal/sync-wise/__tests__/route.test.ts:95-204`). |
| 6 | OPS-06: Dependency cleanup is complete and bundle impact is documented | VERIFIED | `package.json` has no `@auth/drizzle-adapter`, keeps exact runtime deps, and places `shadcn` only in `devDependencies` (`package.json:20-36`, `:38-55`). `package-lock.json` root declarations match and marks `shadcn` under devDependencies (`package-lock.json:10-22`, `:29-47`); Node spot-check confirmed no lockfile `node_modules/@auth/drizzle-adapter`. Bundle impact is documented in the Phase 08.7 Plan 04 summary, including successful `npm run build` after lockfile update (`.planning/phases/08.7-operational-maturity/08.7-04-SUMMARY.md:44-48`). |
| 7 | OPS-07: Risky packages are exact-pinned, `detectConflicts` is one-arg, and medium modality TODO is intentionally resolved | VERIFIED | `next-auth`, `@base-ui/react`, and `drizzle-orm` are exact-pinned in both package files (`package.json:20-30`, `package-lock.json:10-21`). `detectConflicts` signature is `detectConflicts(compareTutors: CompareTutor[])`, and all call sites pass one argument (`src/lib/search/compare.ts:321`, `src/app/api/compare/route.ts:145`, `src/app/api/compare/discover/route.ts:129`). `modality-display.ts` contains no TODO and explicitly documents the reserved medium confidence fallback (`src/components/compare/modality-display.ts:3-26`), with label tests for high, low, unknown, and medium branches (`src/components/compare/__tests__/modality-display.test.ts:13-34`). |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/lib/sync/snapshot-pruning.ts` | Manual ordered snapshot pruning helper | VERIFIED | Exists, substantive, exported, imported by orchestrator, deletes all snapshot-scoped tables and not `past_session_blocks`. |
| `src/lib/sync/orchestrator.ts` | Post-success pruning integration | VERIFIED | Imports `pruneOldSnapshots`, runs after success update and promotion, logs/records pruning failures without flipping success. |
| `src/lib/sync/__tests__/snapshot-pruning.integration.test.ts` | Real-Postgres pruning regression coverage | VERIFIED | Covers latest 30, active preservation, sync-run nulling, no-op within retention, and `past_session_blocks` sentinel preservation. |
| `src/lib/ops/stale.ts` | Shared stale thresholds, copy, helpers | VERIFIED | Defines 26h API threshold, 48h banner threshold, exact banner copy/link/session key, and strict `>` helper predicates. |
| `src/components/layout/stale-snapshot-banner.tsx` | App-level stale snapshot warning | VERIFIED | Client component fetches `/api/data-health`, renders only on `/search`/`/compare`, uses sessionStorage dismissal, links to `/data-health`. |
| `src/app/(app)/layout.tsx` | Mount point directly under `AppNav` | VERIFIED | Renders `<StaleSnapshotBanner />` after `<AppNav />` and before `<main>`. |
| `src/app/api/internal/sync-wise/route.ts` | Manual admin-session POST auth branch | VERIFIED | GET cron-only, POST cron-or-session, success-gated `revalidateTag`. |
| `src/app/(app)/data-health/page.tsx` | Manual sync button and feedback | VERIFIED | Confirm prompt, disabled `Syncing...` state, same-origin POST, inline success/failure, health refresh. |
| `package.json` / `package-lock.json` | Dependency cleanup and exact pins | VERIFIED | Runtime adapter removed, `shadcn` dev-only, three requested packages exact-pinned and lockfile-consistent. |
| `src/lib/search/compare.ts` | Clean `detectConflicts` signature | VERIFIED | One-argument API with unchanged same-student overlap logic. |
| `src/components/compare/modality-display.ts` | Intentional medium-tier handling | VERIFIED | No open TODO; medium follows high-confidence branch by documented intent. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `src/lib/sync/orchestrator.ts` | `src/lib/sync/snapshot-pruning.ts` | Import and call after success update | WIRED | Import at line 20; call at line 516 occurs after success update lines 498-508 and inside `if (promotedSnapshotId)` line 510. |
| `src/lib/sync/snapshot-pruning.ts` | `src/lib/db/schema.ts` | Manual child-table deletes by `snapshotId`, then snapshots delete | WIRED | Deletes all schema tables with `snapshot_id` except cross-snapshot `pastSessionBlocks`; schema confirms `past_session_blocks.captured_in_snapshot_id` is intentionally non-FK/cross-snapshot. |
| `src/app/(app)/layout.tsx` | `src/components/layout/stale-snapshot-banner.tsx` | Render below `AppNav` and before main | WIRED | Lines 7-9 mount banner in the shared authenticated app shell. |
| `src/components/layout/stale-snapshot-banner.tsx` | `/api/data-health` | Client fetch reads `staleAgeMs` only | WIRED | Lines 55-63 fetch health, reject non-ok responses, and pass only numeric stale age to the helper. |
| `src/app/(app)/data-health/page.tsx` | `/api/internal/sync-wise` | Browser POST without Authorization | WIRED | Line 125 sends same-origin POST with only `{ method: "POST" }`. |
| `src/app/api/internal/sync-wise/route.ts` | `src/lib/auth.ts` | `auth()` for POST session path | WIRED | Lines 60-64 call `auth()` only when POST session auth is allowed and cron auth was not valid. |
| API routes/search | `src/lib/ops/stale.ts` | `API_STALE_THRESHOLD_MS` | WIRED | Search, compare, and discover all import/use the shared 26h threshold. |
| Compare/discover routes | `src/lib/search/compare.ts` | `detectConflicts(allCompareTutors)` | WIRED | Compare line 145 and discover line 129 pass one argument. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `src/lib/sync/snapshot-pruning.ts` | `prunedSnapshotIds`, `rowCounts` | Real `snapshots` query ordered by `createdAt`, then Drizzle update/delete returning counts | Yes | FLOWING |
| `src/lib/sync/orchestrator.ts` | `sync_runs.metadata.pruning` | `pruneOldSnapshots(db)` result after promoted sync | Yes | FLOWING |
| `src/components/layout/stale-snapshot-banner.tsx` | `staleAgeMs` | Authenticated `/api/data-health` response backed by `sync_runs.finished_at` | Yes | FLOWING |
| `src/app/api/data-health/route.ts` | failure/stale/recent sync status | Real DB queries against `sync_runs`, `snapshots`, `snapshot_stats`, and `data_issues` | Yes | FLOWING |
| `src/app/(app)/data-health/page.tsx` | `syncMessage`, refreshed health data | Manual POST response from `/api/internal/sync-wise`, then `loadHealthData()` fetch | Yes | FLOWING |
| `src/app/api/internal/sync-wise/route.ts` | manual sync result | Auth.js session or cron auth -> `runFullSync` -> JSON response and snapshot tag revalidation on success | Yes | FLOWING |
| `package.json` / `package-lock.json` | dependency graph | Root package declarations and lockfile packages map | Yes | FLOWING |
| `src/components/compare/modality-display.ts` | modality display labels | Helper inputs from compare session `modality` / `modalityConfidence` | Yes | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Focused stale/manual-sync/modality tests pass | `npm test -- src/lib/ops/__tests__/stale.test.ts src/app/api/internal/sync-wise/__tests__/route.test.ts src/components/compare/__tests__/modality-display.test.ts` | 3 files, 19 tests passed | PASS |
| Snapshot pruning covers all required snapshot-scoped tables and excludes `past_session_blocks` | Node source assertion over `src/lib/sync/snapshot-pruning.ts` | Missing required schema deletes: `[]`; `pastMention: false` | PASS |
| Dependency cleanup is package/lockfile-consistent | Node assertion over `package.json` and `package-lock.json` | `bad: []`; `rootDeps: "5.0.0-beta.30"`; `shadcnDev: true` | PASS |
| Notification provider/env leakage absent | `rg -n "notification|webhook|sendgrid|resend|postmark|NOTIFICATION|WEBHOOK" src package.json package-lock.json .env.example src/lib/env.ts` | No matches | PASS |
| Full unit suite | User-provided actual check: `npm test` | 28 files, 225 tests passed | PASS |
| Integration suite | User-provided actual check: `npm run test:integration` | 3 files, 11 tests passed | PASS |
| TypeScript/build/schema drift | User-provided actual checks: `npx tsc --noEmit --project tsconfig.json`, `npm run build`, `gsd-tools verify schema-drift 08.7` | Typecheck passed; build passed with existing Next.js middleware-to-proxy warning; schema drift false | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| OPS-01 | 08.7-01 | Snapshot pruning keeps latest 30, preserves active, leaves `past_session_blocks` untouched, runs after successful promote | SATISFIED | Helper and orchestrator source verified; integration tests cover retention, active preservation, sync-run nulling, and past-session sentinel. |
| OPS-02 | 08.7-02 | Failed sync visibility is in-app only, with no external notification provider/env var | SATISFIED | `/data-health`, stale banner, and manual sync feedback present; provider/env grep returned no matches. |
| OPS-03 | 08.7-02 | Stale banner on `/search` and `/compare` at `staleAgeMs > 48h`, session-dismissible, links to `/data-health` | SATISFIED | Route filter, strict threshold helper, sessionStorage dismissal, and link all verified. |
| OPS-04 | 08.7-02 | API stale threshold raised to 26h with Hobby cron + 2h grace documentation | SATISFIED | Shared constant/comment and all three required consumers verified; stale boundary tests pass. |
| OPS-05 | 08.7-03 | Manual Sync now button calls existing route through Auth.js session; GET remains cron-only; unauthenticated fallback covered | SATISFIED | Route branch and UI POST verified; route tests cover cron/session/401 paths. |
| OPS-06 | 08.7-04 | Remove unused Auth Drizzle adapter, move `shadcn` to devDependency, document bundle impact | SATISFIED | Package files and summary verified. |
| OPS-07 | 08.7-04, 08.7-05 | Exact pins, one-arg `detectConflicts`, and intentional medium modality handling | SATISFIED | Package pins, call sites, signature, and modality-display test/source verified. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/app/(app)/data-health/page.tsx` | 74 | Skeleton comment says `Table placeholder` | Info | Loading skeleton only; not a user-facing implementation stub. |
| `package-lock.json` | 12440 | Integrity hash contains `XXX` substring | Info | Hash text only; not a TODO/placeholder marker. |
| Multiple phase files | Various | Empty arrays/null returns/initializers | Info | Normal typed initial state, pure helper returns, or test fixtures; no user-visible hardcoded empty data or orphaned stubs found. |

### Human Verification Required

None blocking for phase closure. The code-level behavior is covered by route/helper tests, integration tests, source trace, typecheck, and build. A production manual-sync smoke remains operationally prudent before using the control against live Wise data, but no unverified code gap blocks the Phase 08.7 goal.

### Gaps Summary

No gaps found. All OPS-01 through OPS-07 roadmap success criteria are implemented, wired, and covered by source-level or automated evidence. No deferred Phase 8.7 items were identified in later roadmap phases.

---

_Verified: 2026-05-05T04:18:20Z_
_Verifier: Claude (gsd-verifier)_
