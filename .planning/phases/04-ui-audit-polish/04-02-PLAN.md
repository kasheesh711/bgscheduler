---
phase: 04-ui-audit-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/compare/week-overview.tsx
  - src/components/compare/calendar-grid.tsx
  - src/components/search/search-form.tsx
  - src/app/(app)/data-health/page.tsx
autonomous: true
requirements: [UIFIX-04, UIFIX-05, UIFIX-07]

must_haves:
  truths:
    - "No text-[8px] or text-[9px] exists anywhere in the codebase"
    - "SearchForm labels use text-xs (12px) instead of text-[10px]"
    - "SlotBuilder labels already use text-xs (no change needed, but verified consistent)"
    - "Data-health page shows skeleton shimmer during loading"
    - "Data-health page shows retry guidance on failure"
    - "Calendar-grid uses Tailwind classes instead of inline style pixels"
    - "Search button uses h-8 instead of h-[34px]"
  artifacts:
    - path: "src/components/compare/week-overview.tsx"
      provides: "Fixed typography (no text-[8px] or text-[9px])"
      contains: "text-[10px]"
    - path: "src/components/compare/calendar-grid.tsx"
      provides: "Tailwind spacing instead of inline styles"
      contains: "ml-[50px]"
    - path: "src/app/(app)/data-health/page.tsx"
      provides: "Skeleton loading + retry guidance error state"
      contains: "Refresh the page to try again"
  key_links:
    - from: "src/components/search/search-form.tsx"
      to: "Tailwind scale"
      via: "text-xs on form labels"
      pattern: 'text-xs font-medium text-muted-foreground'
---

<objective>
Fix typography scale issues, inline style inconsistencies, and improve data-health error/loading UX identified in the UI audit.

Purpose: Address UIFIX-04 (typography standardization), UIFIX-05 (data-health UX), and UIFIX-07 (inline styles to Tailwind) from the full-app UI review.
Output: Consistent typography scale, Tailwind-only spacing in calendar grid, actionable data-health error state with skeleton loading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-audit-polish/04-CONTEXT.md

<interfaces>
<!-- Key patterns the executor needs to understand -->

Typography decision from CONTEXT.md:
- Remove all text-[8px] — minimum is text-[10px]
- Remove all text-[9px] — use text-[10px] or text-xs
- Form labels in SearchForm: change text-[10px] to text-xs (12px) for readability
- Keep text-[10px] in compare panel dense-UI areas (session cards, calendar labels, badges)
- SlotBuilder labels already use text-xs — no change needed

Spacing decision from CONTEXT.md:
- calendar-grid.tsx line 80: `style={{ marginLeft: 50 }}` -> `ml-[50px]`
- calendar-grid.tsx line 106: `style={{ left: -50, width: 45 }}` -> Tailwind positioning classes
- calendar-grid.tsx line 113: `style={{ top: i * HOUR_HEIGHT - 7, width: 45 }}` -> keep dynamic `top` in style (computed from loop), but width can be `w-[45px]`
- search-form.tsx line 429: `h-[34px]` -> `h-8` (32px standard Tailwind)

Existing skeleton pattern (from Phase 2):
```typescript
// Skeleton convention: src/components/skeletons/{feature}-skeleton.tsx
// Style: bg-muted animate-pulse for solid blocks
// No "use client" directive (server components for zero JS overhead)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typography standardization and inline style cleanup (UIFIX-04, UIFIX-07)</name>
  <files>
    src/components/compare/week-overview.tsx,
    src/components/compare/calendar-grid.tsx,
    src/components/search/search-form.tsx
  </files>
  <read_first>
    src/components/compare/week-overview.tsx (lines 408-440 for the text-[8px] and text-[9px] locations),
    src/components/compare/calendar-grid.tsx (lines 78-120 for inline styles),
    src/components/search/search-form.tsx (lines 218-230 and 325-475 for text-[10px] labels and h-[34px] button)
  </read_first>
  <action>
**UIFIX-04 — Typography fixes in week-overview.tsx:**

1. In `src/components/compare/week-overview.tsx`, around line 413, the session card text size conditional:
   ```tsx
   // BEFORE:
   isCompact ? "text-[9px]" : multiTutorLayout ? "text-[10px]" : "text-[11px]"
   // AFTER:
   isCompact ? "text-[10px]" : multiTutorLayout ? "text-[10px]" : "text-[11px]"
   ```
   Change `text-[9px]` to `text-[10px]` (minimum dense-UI size).

2. In `src/components/compare/week-overview.tsx`, around line 428, the overflow badge:
   ```tsx
   // BEFORE:
   className="absolute bottom-0.5 right-0.5 text-[8px] font-semibold rounded px-1 cursor-pointer"
   // AFTER:
   className="absolute bottom-0.5 right-0.5 text-[10px] font-semibold rounded px-1 cursor-pointer"
   ```
   Change `text-[8px]` to `text-[10px]` (minimum dense-UI size).

**UIFIX-04 — Typography fixes in search-form.tsx:**

3. In `src/components/search/search-form.tsx`, change ALL form label instances of `text-[10px]` to `text-xs`. The labels to change are at approximately these lines (search for `text-[10px] font-medium text-muted-foreground` within `<label>` elements):
   - Line 220: Tutor label -> `text-xs font-medium text-muted-foreground`
   - Line 327: Time Window Start label -> `text-xs font-medium text-muted-foreground`
   - Line 353: Time Window End label -> `text-xs font-medium text-muted-foreground`
   - Line 369: Duration label -> `text-xs font-medium text-muted-foreground`
   - Line 389: Day label -> `text-xs font-medium text-muted-foreground`
   - Line 407: Mode label -> `text-xs font-medium text-muted-foreground`
   - Line 440: Subject label -> `text-xs font-medium text-muted-foreground`
   - Line 457: Curriculum label -> `text-xs font-medium text-muted-foreground`
   - Line 474: Level label -> `text-xs font-medium text-muted-foreground`

   Also change the "Clear all" button text at line 227:
   - `text-[10px] text-muted-foreground hover:text-destructive` -> `text-xs text-muted-foreground hover:text-destructive`

   Do NOT change `text-[10px]` on Badge components inside the tutor dropdown results (lines ~297, ~302, ~307) — these are dense-UI areas inside the combobox dropdown.

**UIFIX-07 — Search button height in search-form.tsx:**

4. In `src/components/search/search-form.tsx`, around line 429:
   ```tsx
   // BEFORE:
   className="w-full h-[34px] text-xs"
   // AFTER:
   className="w-full h-8 text-xs"
   ```

**UIFIX-07 — Inline style cleanup in calendar-grid.tsx:**

5. In `src/components/compare/calendar-grid.tsx`, line 80:
   ```tsx
   // BEFORE:
   <div className="relative" style={{ marginLeft: 50 }}>
   // AFTER:
   <div className="relative ml-[50px]">
   ```

6. In `src/components/compare/calendar-grid.tsx`, line 106:
   ```tsx
   // BEFORE:
   <div className="absolute top-0 h-full" style={{ left: -50, width: 45 }}>
   // AFTER:
   <div className="absolute top-0 h-full -left-[50px] w-[45px]">
   ```

7. In `src/components/compare/calendar-grid.tsx`, line 113 — the time label divs have a dynamic `top` that must stay as inline style (computed from loop variable `i * HOUR_HEIGHT - 7`), but the static `width: 45` can become a Tailwind class:
   ```tsx
   // BEFORE:
   style={{ top: i * HOUR_HEIGHT - 7, width: 45 }}
   // AFTER:
   style={{ top: i * HOUR_HEIGHT - 7 }}
   ```
   And add `w-[45px]` to the className of that div:
   ```tsx
   className="absolute text-xs font-medium text-muted-foreground text-right pr-2 w-[45px]"
   ```
  </action>
  <verify>
    <automated>cd /Users/kevinhsieh/Desktop/Scheduling && echo "=== No text-[8px] or text-[9px] ===" && ! grep -rq "text-\[8px\]\|text-\[9px\]" src/components/ && echo "PASS" && echo "=== Search form labels use text-xs ===" && grep -c "text-xs font-medium text-muted-foreground" src/components/search/search-form.tsx && echo "=== h-8 on search button ===" && grep -c "h-8 text-xs" src/components/search/search-form.tsx && echo "=== No inline marginLeft in calendar-grid ===" && ! grep -q "marginLeft: 50" src/components/compare/calendar-grid.tsx && echo "PASS" && echo "=== Tailwind ml class present ===" && grep -c "ml-\[50px\]" src/components/compare/calendar-grid.tsx && echo "=== No inline left/width on time label container ===" && ! grep -q "left: -50, width: 45" src/components/compare/calendar-grid.tsx && echo "PASS" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - No file in src/components/ contains `text-[8px]`
    - No file in src/components/ contains `text-[9px]`
    - src/components/search/search-form.tsx contains at least 9 instances of `text-xs font-medium text-muted-foreground` (the form labels)
    - src/components/search/search-form.tsx does NOT contain `h-[34px]` — replaced with `h-8`
    - src/components/search/search-form.tsx still contains `text-[10px]` ONLY on Badge components inside dropdown (not on label elements)
    - src/components/compare/calendar-grid.tsx contains `ml-[50px]` (replacing inline marginLeft)
    - src/components/compare/calendar-grid.tsx contains `-left-[50px] w-[45px]` (replacing inline left/width)
    - src/components/compare/calendar-grid.tsx does NOT contain `style={{ marginLeft: 50 }}`
    - src/components/compare/calendar-grid.tsx does NOT contain `style={{ left: -50, width: 45 }}`
    - src/components/compare/week-overview.tsx line with overflow badge uses `text-[10px]` not `text-[8px]`
    - npm test exits 0 (all 82+ tests pass)
  </acceptance_criteria>
  <done>
    No text-[8px] or text-[9px] in codebase. Form labels standardized to text-xs. Dense-UI text-[10px] retained only in calendar/badge areas. Calendar grid uses Tailwind classes instead of inline style pixels. Search button uses standard h-8.
  </done>
</task>

<task type="auto">
  <name>Task 2: Data-health skeleton loading and error retry guidance (UIFIX-05)</name>
  <files>
    src/app/(app)/data-health/page.tsx
  </files>
  <read_first>
    src/app/(app)/data-health/page.tsx,
    src/components/skeletons/form-skeleton.tsx (for shimmer pattern reference — bg-muted animate-pulse)
  </read_first>
  <action>
**UIFIX-05 — Skeleton loading state:**

1. In `src/app/(app)/data-health/page.tsx`, replace the terse loading state (line 54):
   ```tsx
   // BEFORE:
   if (loading) return <div className="py-8 text-center text-muted-foreground">Loading...</div>;
   // AFTER:
   if (loading) return <DataHealthSkeleton />;
   ```

2. Add a `DataHealthSkeleton` component above the `DataHealthPage` function (keeping it in the same file since this is a page-specific skeleton not reused elsewhere):
   ```tsx
   function DataHealthSkeleton() {
     return (
       <div className="space-y-6">
         {/* Sync status cards */}
         <div className="grid grid-cols-3 gap-4">
           {Array.from({ length: 3 }, (_, i) => (
             <div key={i} className="rounded-xl border bg-card p-6 space-y-3">
               <div className="h-3 w-32 bg-muted animate-pulse rounded" />
               <div className="h-6 w-24 bg-muted animate-pulse rounded" />
             </div>
           ))}
         </div>
         {/* Stats cards */}
         <div className="grid grid-cols-5 gap-4">
           {Array.from({ length: 5 }, (_, i) => (
             <div key={i} className="rounded-xl border bg-card p-6 space-y-3">
               <div className="h-3 w-20 bg-muted animate-pulse rounded" />
               <div className="h-8 w-12 bg-muted animate-pulse rounded" />
             </div>
           ))}
         </div>
         {/* Table placeholder */}
         <div className="rounded-xl border bg-card p-6 space-y-4">
           <div className="h-5 w-40 bg-muted animate-pulse rounded" />
           <div className="space-y-2">
             {Array.from({ length: 4 }, (_, i) => (
               <div key={i} className="h-4 bg-muted/50 animate-pulse rounded" />
             ))}
           </div>
         </div>
       </div>
     );
   }
   ```

**UIFIX-05 — Error state with retry guidance:**

3. Replace the error state (line 55):
   ```tsx
   // BEFORE:
   if (!data) return <div className="py-8 text-center text-muted-foreground">Failed to load health data</div>;
   // AFTER:
   if (!data) return (
     <div className="py-12 text-center space-y-2">
       <p className="text-muted-foreground">Failed to load health data.</p>
       <p className="text-sm text-muted-foreground">Refresh the page to try again.</p>
     </div>
   );
   ```
  </action>
  <verify>
    <automated>cd /Users/kevinhsieh/Desktop/Scheduling && echo "=== Skeleton present ===" && grep -c "DataHealthSkeleton" src/app/\(app\)/data-health/page.tsx && echo "=== animate-pulse shimmer ===" && grep -c "animate-pulse" src/app/\(app\)/data-health/page.tsx && echo "=== Retry guidance ===" && grep -c "Refresh the page to try again" src/app/\(app\)/data-health/page.tsx && echo "=== No terse Loading... ===" && ! grep -q '"Loading\.\.\."' src/app/\(app\)/data-health/page.tsx && echo "PASS" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/data-health/page.tsx contains `function DataHealthSkeleton()`
    - src/app/(app)/data-health/page.tsx contains `bg-muted animate-pulse` (skeleton shimmer pattern)
    - src/app/(app)/data-health/page.tsx contains `Refresh the page to try again.`
    - src/app/(app)/data-health/page.tsx does NOT contain the string `"Loading..."` as the full loading state
    - src/app/(app)/data-health/page.tsx does NOT contain `"Failed to load health data"` as the full error state (now has additional guidance)
    - npm test exits 0 (all 82+ tests pass)
  </acceptance_criteria>
  <done>
    Data-health page shows skeleton shimmer matching card layout during loading. Error state includes actionable "Refresh the page to try again." guidance instead of a bare failure message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies only client-side UI presentation — no API changes, no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | (none applicable) | UI-only changes | accept | No security-relevant changes in this plan |
</threat_model>

<verification>
1. All 82+ existing unit tests pass (`npm test`)
2. No `text-[8px]` or `text-[9px]` exists in src/components/
3. Search form labels consistently use `text-xs` (not `text-[10px]`)
4. Calendar grid has zero inline `style=` for static dimensions
5. Data-health page has skeleton loading with `animate-pulse` and retry guidance in error state
</verification>

<success_criteria>
- UIFIX-04: No text-[8px] or text-[9px] anywhere; form labels use text-xs; text-[10px] retained only in dense-UI calendar areas
- UIFIX-05: Data-health loading shows skeleton shimmer; error state says "Refresh the page to try again."
- UIFIX-07: Calendar-grid uses Tailwind classes instead of inline style pixels; search button uses h-8
- All 82+ existing unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-audit-polish/04-02-SUMMARY.md`
</output>
