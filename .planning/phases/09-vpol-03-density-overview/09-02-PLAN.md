---
phase: 09-vpol-03-density-overview
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/compare/compare-panel.tsx
autonomous: true
requirements:
  - DENS-01
  - DENS-03
  - DENS-04

must_haves:
  truths:
    - "DensityOverview renders in ComparePanel after day tabs and before the calendar body wrapper."
    - "DensityOverview receives compareResponse.tutors, compareTutors, activeDay, and a stable day-click callback."
    - "DensityOverview remains visible in both activeDay null week view and activeDay non-null day drill-down view."
    - "Segment clicks use the existing setActiveDay path only; no click-to-hour jump or scheduling action is introduced."
    - "No API route, SearchIndex, schema, cache-version, localStorage, or response-shape change is introduced."
  artifacts:
    - path: "src/components/compare/compare-panel.tsx"
      provides: "Placement and existing day-navigation wiring for DensityOverview."
      contains: "DensityOverview"
  key_links:
    - from: "src/components/compare/compare-panel.tsx"
      to: "src/components/compare/density-overview.tsx"
      via: "Imports and renders `DensityOverview` in the compare response branch."
      pattern: "DensityOverview"
    - from: "src/components/compare/compare-panel.tsx"
      to: "active day state"
      via: "`handleDensityDayClick` calls `setActiveDay(day)`."
      pattern: "handleDensityDayClick"
---

<objective>
Wire the tested density overview into the compare panel at the locked placement and verify Phase 9 remains frontend-only.

Purpose: make the overview visible to admins without adding a server route, DB query, SearchIndex field, cached response-shape change, or sticky-legend change.

Output: one ComparePanel integration diff plus static guardrail verification.
</objective>

<execution_context>
@/Users/kevinhsieh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-vpol-03-density-overview/09-CONTEXT.md
@.planning/phases/09-vpol-03-density-overview/09-RESEARCH.md
@.planning/phases/09-vpol-03-density-overview/09-VALIDATION.md
@.planning/phases/09-vpol-03-density-overview/09-UI-SPEC.md
@src/components/compare/compare-panel.tsx
@src/components/compare/density-overview.tsx
@src/components/compare/week-overview.tsx
@src/components/compare/calendar-grid.tsx
@src/hooks/use-compare.ts
@src/lib/search/cache-version.ts
@src/lib/search/types.ts

<interfaces>
From `src/components/compare/compare-panel.tsx`:
```tsx
const {
  compareTutors,
  compareResponse,
  activeDay,
  setActiveDay,
} = compare;
```

From `src/components/compare/density-overview.tsx` after Plan 01:
```tsx
export interface DensityOverviewProps {
  tutors: CompareTutor[];
  tutorChips: TutorChip[];
  activeDay: number | null;
  onDayClick: (day: number) => void;
}
export const DensityOverview: React.MemoExoticComponent<...>;
```

From `src/lib/search/cache-version.ts`:
```ts
export const CACHE_VERSION = "v2";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert DensityOverview at the locked ComparePanel placement</name>
  <files>src/components/compare/compare-panel.tsx</files>
  <read_first>
    - src/components/compare/compare-panel.tsx
    - src/components/compare/density-overview.tsx
    - src/components/compare/week-overview.tsx
    - src/components/compare/calendar-grid.tsx
    - src/hooks/use-compare.ts
    - src/lib/search/types.ts
    - .planning/phases/09-vpol-03-density-overview/09-CONTEXT.md
    - .planning/phases/09-vpol-03-density-overview/09-UI-SPEC.md
    - node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md
  </read_first>
  <action>
    Edit only `src/components/compare/compare-panel.tsx`.

    Make these exact integration changes:
    - Change the React import from `import { useState, useMemo } from "react";` to include `useCallback`.
    - Add `import { DensityOverview } from "@/components/compare/density-overview";`.
    - After `conflictCountByDay`, add:
      ```tsx
      const handleDensityDayClick = useCallback(
        (day: number) => {
          setActiveDay(day);
        },
        [setActiveDay],
      );
      ```
    - Render `<DensityOverview />` immediately after the day-tabs `<div className="flex border-b border-border flex-shrink-0">...</div>` and immediately before the `{/* Calendar view */}` comment.
    - Pass these props exactly:
      ```tsx
      <DensityOverview
        tutors={compareResponse.tutors}
        tutorChips={compareTutors}
        activeDay={activeDay}
        onDayClick={handleDensityDayClick}
      />
      ```
    - Change the existing `WeekOverview` prop from `onDayClick={(day) => setActiveDay(day)}` to `onDayClick={handleDensityDayClick}` so both week-header clicks and density segment clicks use the same stable local path.

    Do not edit `src/components/compare/week-overview.tsx`, `src/components/compare/calendar-grid.tsx`, `src/hooks/use-compare.ts`, API routes, schema, SearchIndex, or cache-version files. Do not fold density into the sticky tutor legend. Do not make density sticky.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run --project unit src/components/compare/__tests__/density-overview.test.tsx</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "useCallback|DensityOverview|handleDensityDayClick" src/components/compare/compare-panel.tsx` finds all three patterns.
    - `rg -n "onDayClick=\\{handleDensityDayClick\\}" src/components/compare/compare-panel.tsx` finds the WeekOverview call.
    - This command exits 0:
      ```bash
      node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/compare/compare-panel.tsx','utf8'); const a=s.indexOf('{/* Day tabs */}'); const b=s.indexOf('<DensityOverview'); const c=s.indexOf('{/* Calendar view */}'); if (!(a !== -1 && b !== -1 && c !== -1 && a < b && b < c)) process.exit(1);"
      ```
    - `git diff --name-only -- src/components/compare/week-overview.tsx src/components/compare/calendar-grid.tsx src/hooks/use-compare.ts` prints no paths introduced by this plan.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>The density overview appears above both calendar bodies and routes clicks through the existing active-day state.</done>
</task>

<task type="auto">
  <name>Task 2: Prove forbidden server/cache/schema surfaces stayed untouched</name>
  <files>Verification only: src/app/api, src/lib/search/index.ts, src/lib/db/schema.ts, src/lib/search/cache-version.ts, src/components/compare/density-overview.tsx, src/components/compare/compare-panel.tsx</files>
  <read_first>
    - src/lib/search/cache-version.ts
    - src/components/compare/density-overview.tsx
    - src/components/compare/compare-panel.tsx
    - .planning/phases/09-vpol-03-density-overview/09-CONTEXT.md
    - .planning/phases/09-vpol-03-density-overview/09-VALIDATION.md
    - .planning/phases/09-vpol-03-density-overview/09-UI-SPEC.md
  </read_first>
  <action>
    Run static guardrail checks proving Phase 9 did not add server/cache/schema work.

    Required checks:
    - `git diff --exit-code -- src/app/api src/lib/search/index.ts src/lib/db/schema.ts src/lib/search/cache-version.ts`
    - `rg -n 'export const CACHE_VERSION = "v2"' src/lib/search/cache-version.ts`
    - A Node source check over `src/components/compare/density-overview.tsx` and `src/components/compare/compare-panel.tsx` that fails if either file contains `/api/`, `localStorage`, `CACHE_VERSION`, `SearchIndex`, `@/lib/db`, or `@/lib/search/index`.

    If the forbidden git diff command reports a path, stop execution and report the path. Do not revert unrelated user changes; ask for direction if the path was dirty before Phase 9.
  </action>
  <verify>
    <automated>git diff --exit-code -- src/app/api src/lib/search/index.ts src/lib/db/schema.ts src/lib/search/cache-version.ts</automated>
    <automated>rg -n 'export const CACHE_VERSION = "v2"' src/lib/search/cache-version.ts</automated>
    <automated>node -e "const fs=require('fs'); const files=['src/components/compare/density-overview.tsx','src/components/compare/compare-panel.tsx']; const re=/\\/api\\/|localStorage|CACHE_VERSION|SearchIndex|@\\/lib\\/db|@\\/lib\\/search\\/index/; for (const f of files) { const s=fs.readFileSync(f,'utf8'); if (re.test(s)) { console.error('forbidden client reference in '+f); process.exit(1); } }"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code -- src/app/api src/lib/search/index.ts src/lib/db/schema.ts src/lib/search/cache-version.ts` exits 0.
    - `src/lib/search/cache-version.ts` still contains exact text `export const CACHE_VERSION = "v2";`.
    - The Node source check exits 0 for `/api/`, `localStorage`, `CACHE_VERSION`, `SearchIndex`, `@/lib/db`, and `@/lib/search/index`.
    - No file under `src/app/api`, `src/lib/search/index.ts`, `src/lib/db/schema.ts`, or `src/lib/search/cache-version.ts` appears in this plan's final `git diff --name-only`.
  </acceptance_criteria>
  <done>The normal Phase 9 path is proven frontend-only with no cache-version bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ComparePanel state -> DensityOverview props | Existing compare response and tutor chips cross into a new client component. |
| Density segment -> active day state | Button activation changes local `activeDay` only. |
| Client code -> server/cache layer | This plan must not introduce any new server dependency. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Medium | `ComparePanel` rendering of `DensityOverview` labels | mitigate | Keep React rendering only; inherit Plan 01 source tests blocking HTML injection APIs. |
| T-09-02 | Elevation of Privilege | Medium | Segment click wiring | mitigate | Wire only to `setActiveDay(day)` through `handleDensityDayClick`; no fetch, mutation, scheduling action, or localStorage. |
| T-09-03 | Information Integrity | Medium | Placement above calendar body | mitigate | Preserve calendar as source of detail; density copy remains booked-hours/session-count only and never says available. |
| T-09-04 | Tampering | Medium | Tutor color props | mitigate | Pass existing `compareTutors` tutor chips whose colors are assigned from fixed `TUTOR_COLORS`; no API color field. |
</threat_model>

<verification>
Run:

```bash
./node_modules/.bin/vitest run --project unit src/components/compare/__tests__/density-overview.test.tsx
npm test
git diff --exit-code -- src/app/api src/lib/search/index.ts src/lib/db/schema.ts src/lib/search/cache-version.ts
```

Also run the source-order Node check from Task 1.
</verification>

<success_criteria>
- DensityOverview is rendered between day tabs and calendar body in `ComparePanel`.
- Density is visible in both week and day drill-down views.
- Segment activation opens the existing day view and nothing else.
- Forbidden server/cache/schema files remain untouched and `CACHE_VERSION` remains `"v2"`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-vpol-03-density-overview/09-02-SUMMARY.md`.
</output>
