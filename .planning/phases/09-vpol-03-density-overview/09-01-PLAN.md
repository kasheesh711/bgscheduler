---
phase: 09-vpol-03-density-overview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-vpol-03-density-overview/09-DENSITY-DESIGN-REVIEW.md
  - src/components/compare/density-overview.tsx
  - src/components/compare/__tests__/density-overview.test.tsx
autonomous: true
requirements:
  - DENS-01
  - DENS-02
  - DENS-03
  - DENS-04

must_haves:
  truths:
    - "Shape B is locked in a committed design-review artifact with A/B/C rationale per D-01 and D-04."
    - "Density rows render one selected tutor per row and seven Monday-Sunday day segments per row."
    - "Each row uses tutorChips/TUTOR_COLORS identity color only; density is also encoded by fill width, booked-hours text, and row summary."
    - "Density data is derived from CompareTutor.sessions, availabilityWindows, and weeklyHoursBooked in a useMemo path without server work."
    - "Each segment is a native button with aria-label/title text containing day, tutor, booked hours, session count, and Open day view."
    - "The density component contains no animation, shimmer, pulse, color-only encoding, custom arrow-key grid, utilization percentage, or availability claim."
  artifacts:
    - path: ".planning/phases/09-vpol-03-density-overview/09-DENSITY-DESIGN-REVIEW.md"
      provides: "DENS-02 phase-local shape decision rationale."
      contains: "Chosen shape: B - per-tutor stacked density rows"
    - path: "src/components/compare/density-overview.tsx"
      provides: "DensityOverview component plus exported buildDensityRows helper."
      exports: ["DensityOverview", "buildDensityRows"]
    - path: "src/components/compare/__tests__/density-overview.test.tsx"
      provides: "Aggregation, markup, a11y, and forbidden-string regression tests."
  key_links:
    - from: "src/components/compare/density-overview.tsx"
      to: "src/lib/search/types.ts"
      via: "Imports CompareTutor and reads sessions, availabilityWindows, weeklyHoursBooked."
      pattern: "CompareTutor"
    - from: "src/components/compare/density-overview.tsx"
      to: "src/components/compare/tutor-selector.tsx"
      via: "Imports TutorChip for fixed tutor identity colors."
      pattern: "TutorChip"
    - from: "src/components/compare/density-overview.tsx"
      to: "src/components/compare/session-colors.ts"
      via: "Imports rgba() for fixed tutor-color fills."
      pattern: "rgba"
---

<objective>
Create the Phase 9 density overview foundation: the A/B/C design-review artifact, the client component, the pure aggregation helper, and focused Vitest coverage.

Purpose: satisfy the locked Shape B decision and give the integration plan a tested component that needs no API, schema, SearchIndex, or cache-version change.

Output: one planning artifact, one compare component, and one co-located test file.
</objective>

<execution_context>
@/Users/kevinhsieh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-vpol-03-density-overview/09-CONTEXT.md
@.planning/phases/09-vpol-03-density-overview/09-RESEARCH.md
@.planning/phases/09-vpol-03-density-overview/09-VALIDATION.md
@.planning/phases/09-vpol-03-density-overview/09-UI-SPEC.md
@src/lib/search/types.ts
@src/components/compare/tutor-selector.tsx
@src/components/compare/session-colors.ts
@src/components/compare/__tests__/modality-display.test.ts
@vitest.config.ts

<interfaces>
From `src/lib/search/types.ts`:
```ts
export interface CompareSessionBlock {
  weekday: number;
  startMinute: number;
  endMinute: number;
}

export interface CompareTutor {
  tutorGroupId: string;
  displayName: string;
  sessions: CompareSessionBlock[];
  availabilityWindows: { weekday: number; startMinute: number; endMinute: number; modality: string }[];
  weeklyHoursBooked: number;
}
```

From `src/components/compare/tutor-selector.tsx`:
```ts
interface TutorChip {
  tutorGroupId: string;
  displayName: string;
  color: string;
}
export type { TutorChip };
```

From `src/components/compare/session-colors.ts`:
```ts
export const TUTOR_COLORS = ["#3b82f6", "#e67e22", "#7c3aed"];
export function rgba(color: string, alpha: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the DENS-02 design-review artifact</name>
  <files>.planning/phases/09-vpol-03-density-overview/09-DENSITY-DESIGN-REVIEW.md</files>
  <read_first>
    - .planning/phases/09-vpol-03-density-overview/09-CONTEXT.md
    - .planning/phases/09-vpol-03-density-overview/09-RESEARCH.md
    - .planning/phases/09-vpol-03-density-overview/09-UI-SPEC.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Create `.planning/phases/09-vpol-03-density-overview/09-DENSITY-DESIGN-REVIEW.md`.

    The artifact must contain these exact headings:
    - `# Phase 09 Density Design Review`
    - `## Options Compared`
    - `## Chosen Shape`
    - `## Locked Boundaries`
    - `## Decision Coverage`

    Under `## Options Compared`, include one bullet each for:
    - `Shape A - aggregate day-bar`
    - `Shape B - per-tutor stacked density rows`
    - `Shape C - heatmap`

    Under `## Chosen Shape`, include this exact sentence: `Chosen shape: B - per-tutor stacked density rows.`

    State that Shape B is chosen per D-01 because it is the best fit for 1-3 tutor comparison, exposes per-tutor load differences, and accepts medium complexity for higher compare value. State that Shape A hides which tutor is busy and Shape C is overbuilt for a seven-day, max-three-tutor compare panel.

    Under `## Locked Boundaries`, include these exact bullets:
    - `Zero server work: no API endpoint, DB query, schema change, SearchIndex change, localStorage mechanism, chart dependency, or CACHE_VERSION bump.`
    - `Visible-week only: Monday-Sunday from the loaded CompareResponse.`
    - `No utilization percentage, shared-free-slot overlay, click-to-hour jump, or replacement calendar view.`
  </action>
  <verify>
    <automated>test -f .planning/phases/09-vpol-03-density-overview/09-DENSITY-DESIGN-REVIEW.md</automated>
    <automated>rg -n "Chosen shape: B - per-tutor stacked density rows|Shape A - aggregate day-bar|Shape C - heatmap|Zero server work" .planning/phases/09-vpol-03-density-overview/09-DENSITY-DESIGN-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - The file contains the exact sentence `Chosen shape: B - per-tutor stacked density rows.`
    - The file contains exact option labels `Shape A - aggregate day-bar`, `Shape B - per-tutor stacked density rows`, and `Shape C - heatmap`.
    - The file contains the exact boundary bullet `Zero server work: no API endpoint, DB query, schema change, SearchIndex change, localStorage mechanism, chart dependency, or CACHE_VERSION bump.`
    - The file references CONTEXT decisions `D-01`, `D-04`, `D-15`, `D-16`, and `D-17`.
  </acceptance_criteria>
  <done>DENS-02 has a committed design-review artifact locking Shape B and documenting why A/C are not used.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build and test DensityOverview</name>
  <files>src/components/compare/density-overview.tsx, src/components/compare/__tests__/density-overview.test.tsx</files>
  <read_first>
    - src/lib/search/types.ts
    - src/components/compare/tutor-selector.tsx
    - src/components/compare/session-colors.ts
    - src/components/compare/__tests__/modality-display.test.ts
    - vitest.config.ts
    - node_modules/next/dist/docs/01-app/03-api-reference/01-directives/use-client.md
    - .planning/phases/09-vpol-03-density-overview/09-CONTEXT.md
    - .planning/phases/09-vpol-03-density-overview/09-RESEARCH.md
    - .planning/phases/09-vpol-03-density-overview/09-VALIDATION.md
    - .planning/phases/09-vpol-03-density-overview/09-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: two Monday sessions of 90 and 60 minutes for one tutor produce Monday `bookedMinutes: 150`, `sessionCount: 2`, and a nonzero fill ratio.
    - Test 2: a tutor with zero sessions produces seven cells, `0` booked minutes, `0` session count, and `0` fill ratio without divide-by-zero.
    - Test 3: `weeklyHoursBooked` is preserved for row labels, not recalculated into a utilization percentage.
    - Test 4: rendered static markup contains `aria-label="Visible week booking density"`, native `type="button"` segments, `aria-current="date"` on the active weekday, and segment labels containing day, tutor, booked hours, session count, and `Open day view.`
    - Test 5: the source file contains no `dangerouslySetInnerHTML`, `innerHTML`, `insertAdjacentHTML`, `animate-`, `pulse`, `shimmer`, `transition-`, `bg-available`, `bg-blocked`, `bg-free-slot`, `utilization`, or `% summary`.
  </behavior>
  <action>
    Add `src/components/compare/density-overview.tsx` as a client component with `"use client";` at the top.

    Export:
    - `export interface DensityOverviewProps`
    - `export interface DensityRow`
    - `export interface DensityDayCell`
    - `export function buildDensityRows(tutors: CompareTutor[], tutorChips: TutorChip[]): DensityRow[]`
    - `export const DensityOverview = memo(function DensityOverview(...))`

    Use these exact constants and formulas:
    - `const DISPLAY_DAYS = [1, 2, 3, 4, 5, 6, 0] as const;`
    - `const DAY_LABELS: Record<number, string> = { 0: "Sun", 1: "Mon", 2: "Tue", 3: "Wed", 4: "Thu", 5: "Fri", 6: "Sat" };`
    - Per tutor/day booked minutes: sum `Math.max(0, session.endMinute - session.startMinute)` for `session.weekday === weekday`.
    - Per tutor/day session count: count sessions where `session.weekday === weekday`.
    - Per tutor/day availability minutes: sum `Math.max(0, window.endMinute - window.startMinute)` for `window.weekday === weekday`; keep this as returned data only, do not display utilization or capacity.
    - Maximum denominator: `Math.max(60, ...allCells.map((cell) => cell.bookedMinutes))`.
    - `fillRatio` is `0` when booked minutes are `0`; otherwise `bookedMinutes / maxBookedMinutes`.
    - Visible nonzero fill width uses `Math.max(fillRatio * 100, 12)`.

    Component rendering must match the UI-SPEC:
    - Return `null` when `tutors.length === 0`.
    - Wrap rows in `<section aria-label="Visible week booking density">`.
    - Container classes include `flex-shrink-0 border-b border-border/60 py-1`.
    - Each row uses `grid grid-cols-[96px_1fr] items-center gap-2`.
    - Each row label shows truncated tutor name and `{weeklyHoursBooked}h booked`.
    - Each day segment is a native `<button type="button">` with height `h-8`, seven equal columns, `rounded`, `border border-border/60`, `bg-muted/30`, and `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-1`.
    - Each nonzero segment includes a tutor-colored fill using `rgba(row.color, 0.24)` and a `3px solid ${row.color}` left rail.
    - Each segment visible label shows compact booked hours (`1h`, `1.5h`) only when booked minutes are greater than zero.
    - Each segment `aria-label` and `title` use this shape: `{Day}: {Tutor}, {hours} booked, {sessionCount} session(s). Open day view.`
    - `aria-current="date"` is set only when `activeDay === day.weekday`.

    Use only colors from `tutorChips` matched by `tutorGroupId`, falling back to index and then `#888888`; do not read color from `CompareTutor` or API data. Do not add a chart library, tooltip dependency, custom roving focus, animation, localStorage, API fetch, or cache-version reference.

    Add `src/components/compare/__tests__/density-overview.test.tsx` using Vitest, `react-dom/server` `renderToStaticMarkup`, inline `makeTutor()` and `makeChip()` factories, and source-level grep assertions following `src/components/compare/__tests__/modality-display.test.ts`.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run --project unit src/components/compare/__tests__/density-overview.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/compare/density-overview.tsx` starts with `"use client";`.
    - `rg -n "export function buildDensityRows|export const DensityOverview|useMemo\\(" src/components/compare/density-overview.tsx` finds all three patterns.
    - `rg -n "const DISPLAY_DAYS = \\[1, 2, 3, 4, 5, 6, 0\\] as const" src/components/compare/density-overview.tsx` finds exactly one match.
    - `rg -n "Math.max\\(60|Math.max\\(fillRatio \\* 100, 12\\)|aria-label=\"Visible week booking density\"" src/components/compare/density-overview.tsx` finds all three patterns.
    - `rg -n "Open day view\\.|aria-current=|type=\"button\"" src/components/compare/density-overview.tsx src/components/compare/__tests__/density-overview.test.tsx` finds all three behaviors.
    - `rg -n "dangerouslySetInnerHTML|\\binnerHTML\\b|insertAdjacentHTML|animate-|pulse|shimmer|transition-|bg-available|bg-blocked|bg-free-slot|utilization|% summary" src/components/compare/density-overview.tsx` returns no matches.
    - `./node_modules/.bin/vitest run --project unit src/components/compare/__tests__/density-overview.test.tsx` exits 0.
  </acceptance_criteria>
  <done>DensityOverview exists, is tested, is static/a11y-labeled, and derives density only from existing compare data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompareResponse -> client DOM | Tutor/session strings from Wise-derived snapshot data render in React attributes and text. |
| Client component -> existing day navigation | Segment click changes local `activeDay`; it must not become a write or server boundary. |
| Tutor color identity -> inline styles | Colors must come from fixed tutorChips/TUTOR_COLORS, not API strings. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Medium | `DensityOverview` dynamic labels | mitigate | Render tutor/day strings as React text/attributes only; tests assert no `dangerouslySetInnerHTML`, `innerHTML`, or `insertAdjacentHTML`. |
| T-09-02 | Elevation of Privilege | Medium | `DensityOverview` segment action | mitigate | Keep actions read-only and client-local; no API fetch, localStorage, scheduling mutation, or new endpoint in this component. |
| T-09-03 | Information Integrity | Medium | Density labels and row summaries | mitigate | Label only booked hours/session counts; do not claim availability, capacity, utilization, or scheduling recommendation. |
| T-09-04 | Tampering | Medium | Inline color styles | mitigate | Source colors from `tutorChips` fixed by `TUTOR_COLORS`; use `rgba()` helper fallback for invalid color strings. |
</threat_model>

<verification>
Run:

```bash
test -f .planning/phases/09-vpol-03-density-overview/09-DENSITY-DESIGN-REVIEW.md
./node_modules/.bin/vitest run --project unit src/components/compare/__tests__/density-overview.test.tsx
```

Also run the acceptance-criteria `rg` checks before summary creation.
</verification>

<success_criteria>
- DENS-02 is documented in `09-DENSITY-DESIGN-REVIEW.md`.
- DENS-01/DENS-03/DENS-04 have a tested component foundation with no server/cache/schema work.
- The component is ready for Plan 02 integration into `ComparePanel`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-vpol-03-density-overview/09-01-SUMMARY.md`.
</output>
