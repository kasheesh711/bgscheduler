---
phase: 10-vpol-01-view-transitions
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/components/compare/compare-panel.tsx
  - src/components/compare/week-overview.tsx
  - src/app/globals.css
  - src/components/compare/__tests__/view-transitions-source.test.ts
autonomous: true
requirements:
  - TRANS-01
  - TRANS-02
  - TRANS-03
  - TRANS-04
  - TRANS-05

must_haves:
  truths:
    - "Week prev/next, Today, and calendar-popup week selection call the transition-aware week handler with date-derived forward/back direction."
    - "Day-tab, density-cell, Week-to-Day, Day-to-Day, and Day-to-Week changes use the day crossfade helper path."
    - "The same normalized time-of-day/minute offset is preserved across week changes and week/day view switches for both known scroll containers; raw scrollTop is only reused for same-view week changes where the pixel scale is unchanged."
    - "Global CSS defines week-forward, week-back, and day view-transition rules with 160 ms duration and strict reduced-motion disable rules."
    - "Source guardrail tests prevent experimental Next viewTransition, React canary APIs, animation dependencies, and cache/schema/API drift."
  artifacts:
    - path: "src/components/compare/compare-panel.tsx"
      provides: "Transition-aware compare navigation handlers, scroll refs, and named calendar transition surface."
    - path: "src/components/compare/week-overview.tsx"
      provides: "Week scroll container ref support."
    - path: "src/app/globals.css"
      provides: "Scoped view-transition CSS and reduced-motion override."
    - path: "src/components/compare/__tests__/view-transitions-source.test.ts"
      provides: "Source guardrails for Phase 10 UI-only scope."
  key_links:
    - from: "src/components/compare/compare-panel.tsx"
      to: "src/hooks/use-compare.ts"
      via: "changeWeek target with kind/skip/scroll options"
      pattern: "changeWeek\\(targetWeek"
    - from: "src/components/compare/compare-panel.tsx"
      to: "src/lib/ui/view-transitions.ts"
      via: "day transition, week direction, rapid navigation bypass"
      pattern: "runCalendarViewTransition"
    - from: "src/app/globals.css"
      to: "src/components/compare/compare-panel.tsx"
      via: "compare-calendar-transition-surface class names the transition target"
      pattern: "compare-calendar-transition-surface"
---

<objective>
Wire native view transitions into the compare panel for week and day navigation, add scroll preservation, and define scoped CSS.

Purpose: satisfy the visible Phase 10 user experience while preserving the frozen GCal-style compare baseline, Phase 8 sticky legend, Phase 9 density overview, and reduced-motion requirements.

Output: compare UI wiring, one WeekOverview ref prop, global CSS, and source guardrail tests.
</objective>

<execution_context>
@/Users/kevinhsieh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/viewTransition.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/cacheComponents.md
@node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-vpol-02-sticky-tutor-legend/08-03-SUMMARY.md
@.planning/phases/08-vpol-02-sticky-tutor-legend/08-04-SUMMARY.md
@.planning/phases/09-vpol-03-density-overview/09-02-SUMMARY.md
@.planning/phases/10-vpol-01-view-transitions/10-CONTEXT.md
@.planning/phases/10-vpol-01-view-transitions/10-RESEARCH.md
@.planning/phases/10-vpol-01-view-transitions/10-VALIDATION.md
@.planning/phases/10-vpol-01-view-transitions/10-01-SUMMARY.md
@.planning/phases/10-vpol-01-view-transitions/10-02-SUMMARY.md
@src/components/compare/compare-panel.tsx
@src/components/compare/week-overview.tsx
@src/components/compare/calendar-grid.tsx
@src/components/compare/week-calendar.tsx
@src/components/compare/density-overview.tsx
@src/hooks/use-compare.ts
@src/lib/ui/view-transitions.ts
@src/app/globals.css
@src/lib/search/cache-version.ts
@next.config.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source guardrail tests</name>
  <files>src/components/compare/__tests__/view-transitions-source.test.ts</files>
  <read_first>
    - src/components/compare/__tests__/density-overview.test.tsx
    - src/components/compare/compare-panel.tsx
    - src/app/globals.css
    - src/hooks/use-compare.ts
    - src/lib/ui/view-transitions.ts
    - src/lib/search/cache-version.ts
    - next.config.ts
    - package.json
    - .planning/phases/10-vpol-01-view-transitions/10-VALIDATION.md
  </read_first>
  <behavior>
    - Source test confirms `next.config.ts` contains `cacheComponents: true` and does not contain `viewTransition`.
    - Source test confirms `package.json` has no `framer-motion`, `"motion"`, `motion/react`, `@react-spring`, or `react-spring`.
    - Source test confirms `globals.css` contains `compare-calendar-transition-surface`, `view-transition-name: compare-calendar`, `:active-view-transition-type(week-forward)`, `:active-view-transition-type(week-back)`, `:active-view-transition-type(day)`, `animation-duration: 160ms`, and `@media (prefers-reduced-motion: reduce)`.
    - Source test confirms `compare-panel.tsx` imports `runCalendarViewTransition`, `getWeekTransitionKind`, `isRapidWeekNavigation`, and `flushSync`.
    - Source test confirms `compare-panel.tsx` uses normalized calendar time conversion with `CALENDAR_START_HOUR = 7`, `WEEK_PIXELS_PER_HOUR = 48`, `DAY_PIXELS_PER_HOUR = 60`, `captureCalendarMinuteOfDay`, and `restoreCalendarMinuteOfDay` instead of copying raw pixels across WeekOverview and CalendarGrid.
    - Source test confirms `compare-panel.tsx` contains the 5pm conversion evidence: week raw pixels `480`, normalized minute `1020`, and day raw pixels `600`.
    - Source test confirms `use-compare.ts` imports `runCalendarViewTransition` and does not import `next/navigation`.
    - Source test confirms `src/lib/search/cache-version.ts` remains `export const CACHE_VERSION = "v2";`.
  </behavior>
  <action>
    Create `src/components/compare/__tests__/view-transitions-source.test.ts`.

    Follow the source-inspection style from `src/components/compare/__tests__/density-overview.test.tsx`: import `readFileSync` from `node:fs`, `path` from `node:path`, and `describe`, `expect`, `it` from `vitest`; add a `read(relativePath: string)` helper using `path.join(process.cwd(), relativePath)`.

    Implement one `describe("Phase 10 view transition source guardrails", ...)` block with tests for the behavior bullets above. These tests are expected to fail before Tasks 2 and 3 complete because CSS and ComparePanel wiring are not present yet.
  </action>
  <verify>
    <automated>npm test -- src/components/compare/__tests__/view-transitions-source.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/compare/__tests__/view-transitions-source.test.ts` contains `compare-calendar-transition-surface`.
    - `src/components/compare/__tests__/view-transitions-source.test.ts` contains `:active-view-transition-type(week-forward)`.
    - `src/components/compare/__tests__/view-transitions-source.test.ts` contains `captureCalendarMinuteOfDay`.
    - `src/components/compare/__tests__/view-transitions-source.test.ts` contains `DAY_PIXELS_PER_HOUR = 60`.
    - `src/components/compare/__tests__/view-transitions-source.test.ts` contains `1020`.
    - `src/components/compare/__tests__/view-transitions-source.test.ts` contains `export const CACHE_VERSION = "v2";`.
    - `src/components/compare/__tests__/view-transitions-source.test.ts` contains `not.toMatch(/viewTransition`.
    - The RED run of `npm test -- src/components/compare/__tests__/view-transitions-source.test.ts` exits nonzero before Tasks 2 and 3.
  </acceptance_criteria>
  <done>Source guardrails exist for the UI-only scope, CSS hooks, helper wiring, and forbidden dependency/config changes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire compare navigation and scroll restoration</name>
  <files>src/components/compare/compare-panel.tsx, src/components/compare/week-overview.tsx</files>
  <read_first>
    - src/components/compare/compare-panel.tsx
    - src/components/compare/week-overview.tsx
    - src/components/compare/calendar-grid.tsx
    - src/components/compare/week-calendar.tsx
    - src/components/compare/density-overview.tsx
    - src/hooks/use-compare.ts
    - src/lib/ui/view-transitions.ts
    - .planning/phases/10-vpol-01-view-transitions/10-CONTEXT.md
    - .planning/phases/10-vpol-01-view-transitions/10-RESEARCH.md
    - .planning/phases/10-vpol-01-view-transitions/10-VALIDATION.md
  </read_first>
  <action>
    Update `src/components/compare/week-overview.tsx`:
    - Change the React import to include `type Ref`.
    - Add `scrollContainerRef?: Ref<HTMLDivElement>;` to `WeekOverviewProps`.
    - Include `scrollContainerRef` in the function parameters.
    - Add `ref={scrollContainerRef}` to the existing internal scroll body `<div className="flex-1 overflow-y-auto min-h-0">`.

    Update `src/components/compare/compare-panel.tsx`:
    - Change React import to `useCallback, useEffect, useMemo, useRef, useState`.
    - Add `import { flushSync } from "react-dom";`.
    - Add `import { getWeekTransitionKind, isRapidWeekNavigation, runCalendarViewTransition } from "@/lib/ui/view-transitions";`.
    - Near the existing module constants/imports, add these explicit calendar scale constants matching the rendered grids:
      `const CALENDAR_START_HOUR = 7;`
      `const WEEK_PIXELS_PER_HOUR = 48;`
      `const DAY_PIXELS_PER_HOUR = 60;`
    - After the component destructures compare state/actions, add:
      `const weekScrollRef = useRef<HTMLDivElement | null>(null);`
      `const dayScrollRef = useRef<HTMLDivElement | null>(null);`
      `const calendarChunksReady = useRef(false);`
      `const lastWeekNavigationStartedAt = useRef<number | null>(null);`
    - Add a `useEffect` that runs when `compareResponse` becomes truthy and preloads both dynamic chunks:
      `void Promise.all([import("@/components/compare/week-overview"), import("@/components/compare/calendar-grid")]).then(() => { calendarChunksReady.current = true; });`
      Set `calendarChunksReady.current = false;` before starting that Promise, and set it false when `compareResponse` is null.
    - Add `getScrollElementForDay(day: number | null)`, `getPixelsPerHourForDay(day: number | null)`, `captureCalendarMinuteOfDay(day: number | null = activeDay)`, `restoreCalendarMinuteOfDay(day: number | null, minuteOfDay: number)`, and `restoreSameViewScrollTop(day: number | null, scrollTop: number)` callbacks.
    - `getPixelsPerHourForDay` must return `WEEK_PIXELS_PER_HOUR` when `day === null` and `DAY_PIXELS_PER_HOUR` otherwise.
    - `captureCalendarMinuteOfDay` must normalize the active scroll position using this formula:
      `const minuteOfDay = CALENDAR_START_HOUR * 60 + ((el?.scrollTop ?? 0) / sourcePixelsPerHour) * 60;`
      where `sourcePixelsPerHour = getPixelsPerHourForDay(day)`.
    - `restoreCalendarMinuteOfDay` must convert the normalized minute for the target view using this formula:
      `const scrollTop = ((minuteOfDay - CALENDAR_START_HOUR * 60) / 60) * targetPixelsPerHour;`
      where `targetPixelsPerHour = getPixelsPerHourForDay(day)`.
    - Clamp the restored `scrollTop` to the target element's valid range with `Math.max(0, Math.min(scrollTop, el.scrollHeight - el.clientHeight))` before assigning it.
    - `restoreCalendarMinuteOfDay` must set `el.scrollTop` immediately when the target element exists and also call `requestAnimationFrame(restore)` once, so the browser captures the correct target time-of-day inside the view-transition update.
    - `restoreSameViewScrollTop` may preserve raw `scrollTop` only for same-view week changes because the source and target pixel scale are identical (`week` to `week` at 48 px/hour, or `day` to `day` at 60 px/hour). Do not use raw `scrollTop` for Week-to-Day, Day-to-Week, or Day-to-Day code paths.
    - Add `handleWeekChange(targetWeek: string)` that:
      - gets `kind = getWeekTransitionKind(weekStart, targetWeek)`;
      - returns when `kind === null`;
      - reads `now = performance.now()`;
      - computes `rapid = isRapidWeekNavigation(lastWeekNavigationStartedAt.current, now)`;
      - writes `lastWeekNavigationStartedAt.current = now`;
      - captures `sameViewScrollTop = getScrollElementForDay(activeDay)?.scrollTop ?? 0`;
      - calls `void changeWeek(targetWeek, { kind, skipTransition: rapid, capturedScrollTop: sameViewScrollTop, restoreScrollTop: (top) => restoreSameViewScrollTop(activeDay, top) });`.
    - Add `handleDayChange(targetDay: number | null)` that:
      - captures `minuteOfDay = captureCalendarMinuteOfDay(activeDay)`;
      - calls `void runCalendarViewTransition(() => { flushSync(() => setActiveDay(targetDay)); restoreCalendarMinuteOfDay(targetDay, minuteOfDay); }, { kind: "day", skip: !calendarChunksReady.current });`.
      - Include a short code comment or source-test-covered assertion showing the 5pm conversion: WeekOverview at 5pm is `((17 - CALENDAR_START_HOUR) * WEEK_PIXELS_PER_HOUR) = 480` raw pixels, which normalizes to `1020` minutes, then CalendarGrid restores to `((1020 - CALENDAR_START_HOUR * 60) / 60) * DAY_PIXELS_PER_HOUR = 600` raw pixels. The inverse Day-to-Week path maps `600` raw day pixels back to `480` raw week pixels. This is required for D-11 / TRANS-04.
    - Replace existing calls:
      - Previous week button uses `handleWeekChange(shiftWeek(weekStart, -1))`.
      - Next week button uses `handleWeekChange(shiftWeek(weekStart, 1))`.
      - Today button uses `handleWeekChange(getCurrentMonday())`.
      - `WeekCalendar.onWeekSelect` calls `handleWeekChange(monday); setCalendarOpen(false);`.
      - Week tab uses `handleDayChange(null)`.
      - Day tabs use `handleDayChange(day)`.
      - `handleDensityDayClick` calls `handleDayChange(day)`.
      - `WeekOverview onDayClick` receives `handleDayChange`.
    - Add `ref={activeDay !== null ? dayScrollRef : undefined}` to the existing calendar wrapper `<div>`.
    - Add the class `compare-calendar-transition-surface` to that wrapper.
    - Pass `scrollContainerRef={weekScrollRef}` to `WeekOverview`.

    Do not change the tutor selector chip strip, density overview placement, conflict summary, GCal grid layout, API calls, or fullscreen behavior.
  </action>
  <verify>
    <automated>npm test -- src/lib/ui/__tests__/view-transitions.test.ts src/components/compare/__tests__/view-transitions-source.test.ts</automated>
    <automated>rg -n 'CALENDAR_START_HOUR = 7|WEEK_PIXELS_PER_HOUR = 48|DAY_PIXELS_PER_HOUR = 60|captureCalendarMinuteOfDay|restoreCalendarMinuteOfDay|sameViewScrollTop|weekScrollRef|dayScrollRef|calendarChunksReady|lastWeekNavigationStartedAt|handleWeekChange|handleDayChange|compare-calendar-transition-surface|scrollContainerRef=\\{weekScrollRef\\}' src/components/compare/compare-panel.tsx</automated>
    <automated>rg -n 'scrollContainerRef\\?: Ref<HTMLDivElement>|ref=\\{scrollContainerRef\\}' src/components/compare/week-overview.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/compare/compare-panel.tsx` contains `import { flushSync } from "react-dom";`.
    - `src/components/compare/compare-panel.tsx` contains `getWeekTransitionKind`, `isRapidWeekNavigation`, and `runCalendarViewTransition`.
    - `src/components/compare/compare-panel.tsx` contains `const CALENDAR_START_HOUR = 7;`, `const WEEK_PIXELS_PER_HOUR = 48;`, and `const DAY_PIXELS_PER_HOUR = 60;`.
    - `src/components/compare/compare-panel.tsx` contains `captureCalendarMinuteOfDay` and the formula `CALENDAR_START_HOUR * 60 + ((el?.scrollTop ?? 0) / sourcePixelsPerHour) * 60`.
    - `src/components/compare/compare-panel.tsx` contains `restoreCalendarMinuteOfDay` and the formula `((minuteOfDay - CALENDAR_START_HOUR * 60) / 60) * targetPixelsPerHour`.
    - `src/components/compare/compare-panel.tsx` contains `calendarChunksReady.current = false`.
    - `src/components/compare/compare-panel.tsx` contains both `import("@/components/compare/week-overview")` and `import("@/components/compare/calendar-grid")`.
    - `src/components/compare/compare-panel.tsx` contains `skipTransition: rapid`.
    - `src/components/compare/compare-panel.tsx` contains `{ kind: "day", skip: !calendarChunksReady.current }`.
    - `src/components/compare/compare-panel.tsx` documents or tests the 5pm conversion from week raw pixels `480` to normalized minute `1020` to day raw pixels `600`, and the inverse day-to-week conversion from `600` to `480`.
    - `src/components/compare/compare-panel.tsx` contains `requestAnimationFrame(restore)`.
    - `src/components/compare/week-overview.tsx` contains `scrollContainerRef?: Ref<HTMLDivElement>;` and `ref={scrollContainerRef}`.
  </acceptance_criteria>
  <done>All compare week/day navigation entry points use transition-aware handlers and both known scroll containers can preserve time-of-day offset.</done>
</task>

<task type="auto">
  <name>Task 3: Add scoped view-transition CSS</name>
  <files>src/app/globals.css, src/components/compare/__tests__/view-transitions-source.test.ts</files>
  <read_first>
    - src/app/globals.css
    - src/components/compare/__tests__/view-transitions-source.test.ts
    - src/components/compare/compare-panel.tsx
    - src/lib/ui/view-transitions.ts
    - .planning/phases/10-vpol-01-view-transitions/10-CONTEXT.md
    - .planning/phases/10-vpol-01-view-transitions/10-RESEARCH.md
    - .planning/phases/10-vpol-01-view-transitions/10-VALIDATION.md
  </read_first>
  <action>
    Append a Phase 10 view-transition block to `src/app/globals.css` after the existing `@layer base` block.

    Add:
    - `.compare-calendar-transition-surface { view-transition-name: compare-calendar; contain: layout paint; }`
    - `::view-transition-group(compare-calendar) { animation-duration: 160ms; animation-timing-function: cubic-bezier(0.2, 0, 0, 1); }`
    - `::view-transition-old(compare-calendar), ::view-transition-new(compare-calendar) { animation-duration: 160ms; animation-timing-function: cubic-bezier(0.2, 0, 0, 1); }`

    Add keyframes with these exact names:
    - `compare-week-forward-old`
    - `compare-week-forward-new`
    - `compare-week-back-old`
    - `compare-week-back-new`
    - `compare-day-old`
    - `compare-day-new`

    Use small movements for week transitions:
    - forward old: `translateX(0)` to `translateX(-16px)`, opacity `1` to `0.88`
    - forward new: `translateX(16px)` to `translateX(0)`, opacity `0.88` to `1`
    - back old: `translateX(0)` to `translateX(16px)`, opacity `1` to `0.88`
    - back new: `translateX(-16px)` to `translateX(0)`, opacity `0.88` to `1`
    - day old: opacity `1` to `0`
    - day new: opacity `0` to `1`

    Add active-type selectors:
    - `:active-view-transition-type(week-forward)::view-transition-old(compare-calendar) { animation-name: compare-week-forward-old; }`
    - `:active-view-transition-type(week-forward)::view-transition-new(compare-calendar) { animation-name: compare-week-forward-new; }`
    - `:active-view-transition-type(week-back)::view-transition-old(compare-calendar) { animation-name: compare-week-back-old; }`
    - `:active-view-transition-type(week-back)::view-transition-new(compare-calendar) { animation-name: compare-week-back-new; }`
    - `:active-view-transition-type(day)::view-transition-old(compare-calendar) { animation-name: compare-day-old; }`
    - `:active-view-transition-type(day)::view-transition-new(compare-calendar) { animation-name: compare-day-new; }`

    Add root snapshot no-op selectors for all three active types so the search panel and app shell do not animate:
    - `:active-view-transition-type(week-forward)::view-transition-old(root)`
    - `:active-view-transition-type(week-forward)::view-transition-new(root)`
    - `:active-view-transition-type(week-back)::view-transition-old(root)`
    - `:active-view-transition-type(week-back)::view-transition-new(root)`
    - `:active-view-transition-type(day)::view-transition-old(root)`
    - `:active-view-transition-type(day)::view-transition-new(root)`
    Set `animation: none;` for this root group.

    Add strict reduced-motion CSS:
    `@media (prefers-reduced-motion: reduce) { ::view-transition-group(*), ::view-transition-old(*), ::view-transition-new(*) { animation: none !important; } .compare-calendar-transition-surface { view-transition-name: none; } }`
  </action>
  <verify>
    <automated>npm test -- src/lib/ui/__tests__/view-transitions.test.ts src/components/compare/__tests__/view-transitions-source.test.ts</automated>
    <automated>rg -n 'compare-calendar-transition-surface|view-transition-name: compare-calendar|compare-week-forward-old|compare-week-back-new|compare-day-new|animation-duration: 160ms|prefers-reduced-motion: reduce' src/app/globals.css</automated>
    <automated>rg -n 'viewTransition' next.config.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains `.compare-calendar-transition-surface`.
    - `src/app/globals.css` contains `view-transition-name: compare-calendar`.
    - `src/app/globals.css` contains `contain: layout paint`.
    - `src/app/globals.css` contains `animation-duration: 160ms`.
    - `src/app/globals.css` contains all six keyframe names listed in the action.
    - `src/app/globals.css` contains `:active-view-transition-type(week-forward)`, `:active-view-transition-type(week-back)`, and `:active-view-transition-type(day)`.
    - `src/app/globals.css` contains `@media (prefers-reduced-motion: reduce)`.
    - `npm test -- src/lib/ui/__tests__/view-transitions.test.ts src/components/compare/__tests__/view-transitions-source.test.ts` exits 0.
  </acceptance_criteria>
  <done>Scoped calendar view-transition CSS exists, source guardrails pass, and reduced-motion CSS disables all transition pseudo-element animations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compare client state to DOM snapshot | State commits for week/day navigation are captured by native browser view transitions. |
| CSS transition type to visual animation | Static helper types select global CSS pseudo-element rules. |
| user accessibility preference to UI behavior | `prefers-reduced-motion` must disable both helper and CSS animation paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Spoofing | `ComparePanel` week transition | mitigate | Week handler calls `changeWeek` fetch-first path; current content remains visible during fetch and final loaded content commits inside transition. |
| T-10-10 | Tampering | `globals.css` active transition selectors | mitigate | CSS only references static names `week-forward`, `week-back`, `day`, and `compare-calendar`; no user-provided selector string is interpolated. |
| T-10-11 | Denial of usability | day/week motion | mitigate | Helper bypasses reduced motion and CSS uses `@media (prefers-reduced-motion: reduce)` with `animation: none !important` plus `view-transition-name: none`. |
| T-10-12 | Information integrity | scroll restoration | mitigate | Preserve normalized time-of-day/minute offset across WeekOverview and CalendarGrid using their explicit pixel-per-hour scales; reuse raw `scrollTop` only for same-view week changes where the source and target scale are identical. |
| T-10-13 | Elevation of privilege | UI-only source scope | accept | Source guardrail test ensures `next.config.ts`, `package.json`, `src/lib/search/cache-version.ts`, API, DB, and SearchIndex surfaces are not changed for this phase. |
</threat_model>

<verification>
Run:
- `npm test -- src/lib/ui/__tests__/view-transitions.test.ts src/components/compare/__tests__/view-transitions-source.test.ts`
- `npm test`
- `npm run lint`
- `rg -n 'viewTransition' next.config.ts || true` should show no `viewTransition` config key.
- `rg -n 'framer-motion|"motion"|"@react-spring|react-spring' package.json` should show no animation dependency additions.
</verification>

<success_criteria>
Plan 03 is complete when all compare navigation entry points route through the transition-aware handlers, both scroll containers preserve normalized time-of-day offset across Week-to-Day and Day-to-Week switches around 5pm, CSS provides the three scoped transition modes with 160 ms timing, and source guardrail tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vpol-01-view-transitions/10-03-SUMMARY.md`.
</output>
