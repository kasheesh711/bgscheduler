---
phase: 10-vpol-01-view-transitions
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/ui/view-transitions.ts
  - src/lib/ui/__tests__/view-transitions.test.ts
autonomous: true
requirements:
  - TRANS-03
  - TRANS-05

must_haves:
  truths:
    - "View-transition logic is centralized in src/lib/ui/view-transitions.ts."
    - "Transition kind/direction values are static TypeScript literals, not user-provided selectors or arbitrary strings."
    - "SSR, unsupported browsers, reduced-motion users, and explicit skip paths commit instantly without calling document.startViewTransition."
    - "Rapid week navigation has a tested 300 ms bypass helper."
  artifacts:
    - path: "src/lib/ui/view-transitions.ts"
      provides: "Typed native same-document view-transition helper."
      exports: ["CalendarViewTransitionKind", "WEEK_RAPID_NAVIGATION_MS", "getWeekTransitionKind", "isRapidWeekNavigation", "runCalendarViewTransition"]
    - path: "src/lib/ui/__tests__/view-transitions.test.ts"
      provides: "Unit coverage for helper type mapping and bypass paths."
  key_links:
    - from: "src/lib/ui/view-transitions.ts"
      to: "document.startViewTransition"
      via: "feature-detected native browser API call"
      pattern: "startViewTransition"
    - from: "src/lib/ui/view-transitions.ts"
      to: "prefers-reduced-motion"
      via: "JS bypass before native transition start"
      pattern: "prefers-reduced-motion: reduce"
---

<objective>
Create the typed native view-transition helper and its unit tests.

Purpose: give later compare UI work a narrow, SSR-safe platform abstraction that honors Phase 10 D-13 through D-17 without adopting Next experimental view transitions, React canary APIs, or an animation dependency.

Output: `src/lib/ui/view-transitions.ts` plus focused Vitest coverage.
</objective>

<execution_context>
@/Users/kevinhsieh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/viewTransition.md
@node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/cacheComponents.md
@node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-vpol-01-view-transitions/10-CONTEXT.md
@.planning/phases/10-vpol-01-view-transitions/10-RESEARCH.md
@.planning/phases/10-vpol-01-view-transitions/10-VALIDATION.md
@src/lib/ui/z-index.ts
@vitest.config.ts

<interfaces>
Create these exported contracts in `src/lib/ui/view-transitions.ts`:
```ts
export type CalendarViewTransitionKind = "week-forward" | "week-back" | "day";
export const WEEK_RAPID_NAVIGATION_MS = 300;

export function getWeekTransitionKind(
  currentWeek: string,
  targetWeek: string,
): "week-forward" | "week-back" | null;

export function isRapidWeekNavigation(
  previousStartedAt: number | null,
  nowMs: number,
  thresholdMs?: number,
): boolean;

export interface RunCalendarViewTransitionOptions {
  kind: CalendarViewTransitionKind;
  skip?: boolean;
  documentRef?: Document;
  reducedMotion?: boolean;
}

export function runCalendarViewTransition(
  update: () => void | Promise<void>,
  options: RunCalendarViewTransitionOptions,
): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing helper tests</name>
  <files>src/lib/ui/__tests__/view-transitions.test.ts</files>
  <read_first>
    - src/lib/ui/z-index.ts
    - vitest.config.ts
    - .planning/phases/10-vpol-01-view-transitions/10-CONTEXT.md
    - .planning/phases/10-vpol-01-view-transitions/10-RESEARCH.md
    - .planning/phases/10-vpol-01-view-transitions/10-VALIDATION.md
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/viewTransition.md
  </read_first>
  <behavior>
    - `getWeekTransitionKind("2026-04-06", "2026-04-13")` returns `"week-forward"`.
    - `getWeekTransitionKind("2026-04-13", "2026-04-06")` returns `"week-back"`.
    - `getWeekTransitionKind("2026-04-06", "2026-04-06")` returns `null`.
    - `isRapidWeekNavigation(null, 1000)` returns `false`.
    - `isRapidWeekNavigation(1000, 1299)` returns `true`.
    - `isRapidWeekNavigation(1000, 1300)` returns `false`.
    - `runCalendarViewTransition` calls `update` directly when `skip: true`, when `documentRef` has no `startViewTransition`, and when `reducedMotion: true`.
    - `runCalendarViewTransition` calls `document.startViewTransition({ update, types: ["day"] })` for the native path.
  </behavior>
  <action>
    Create `src/lib/ui/__tests__/view-transitions.test.ts`.

    Import `describe`, `expect`, `it`, and `vi` from `vitest`. Import the helper symbols from `../view-transitions`.

    Add a local fake document object for native-path tests with this shape:
    `const fakeDocument = { startViewTransition: vi.fn((options) => { options.update(); return { finished: Promise.resolve() }; }) } as unknown as Document;`

    Write tests for the behavior bullets above. The first run should fail because `src/lib/ui/view-transitions.ts` does not exist yet. Do not add `jsdom` or change `vitest.config.ts`; these are Node environment tests with injected fakes.
  </action>
  <verify>
    <automated>npm test -- src/lib/ui/__tests__/view-transitions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/ui/__tests__/view-transitions.test.ts` contains `getWeekTransitionKind("2026-04-06", "2026-04-13")`.
    - `src/lib/ui/__tests__/view-transitions.test.ts` contains `isRapidWeekNavigation(1000, 1299)`.
    - `src/lib/ui/__tests__/view-transitions.test.ts` contains `types: ["day"]`.
    - The RED run of `npm test -- src/lib/ui/__tests__/view-transitions.test.ts` exits nonzero before Task 2 because `../view-transitions` is missing.
  </acceptance_criteria>
  <done>Failing tests exist for helper contracts, bypass paths, native `types`, and the 300 ms rapid-navigation rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the typed native helper</name>
  <files>src/lib/ui/view-transitions.ts, src/lib/ui/__tests__/view-transitions.test.ts</files>
  <read_first>
    - src/lib/ui/__tests__/view-transitions.test.ts
    - src/lib/ui/z-index.ts
    - .planning/phases/10-vpol-01-view-transitions/10-CONTEXT.md
    - .planning/phases/10-vpol-01-view-transitions/10-RESEARCH.md
    - .planning/phases/10-vpol-01-view-transitions/10-VALIDATION.md
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/viewTransition.md
    - node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/cacheComponents.md
  </read_first>
  <action>
    Create `src/lib/ui/view-transitions.ts` with the exact exported contracts listed in this plan's `<interfaces>` block.

    Implementation requirements:
    - Use `export type CalendarViewTransitionKind = "week-forward" | "week-back" | "day";`.
    - Use `export const WEEK_RAPID_NAVIGATION_MS = 300;`.
    - `getWeekTransitionKind` must compare normalized ISO week strings lexically: `targetWeek > currentWeek` returns `"week-forward"`, `targetWeek < currentWeek` returns `"week-back"`, equal returns `null`.
    - `isRapidWeekNavigation(previousStartedAt, nowMs, thresholdMs = WEEK_RAPID_NAVIGATION_MS)` must return `false` when `previousStartedAt === null`; otherwise return `nowMs - previousStartedAt < thresholdMs`.
    - `runCalendarViewTransition` must call `await update()` and return without touching the native API when `options.skip === true`, `options.reducedMotion === true`, `typeof document === "undefined"` and `options.documentRef` is absent, or `doc.startViewTransition` is not a function.
    - When `options.reducedMotion` is not provided, evaluate `window.matchMedia("(prefers-reduced-motion: reduce)").matches` only when `typeof window !== "undefined"`.
    - The native path must call `doc.startViewTransition({ update, types: [options.kind] })`.
    - Await `transition.finished` when available, and catch a rejected `finished` promise by resolving silently because the DOM update already happened.
    - Do not import from `next/navigation`, `react`, `react-dom`, or any animation package.
  </action>
  <verify>
    <automated>npm test -- src/lib/ui/__tests__/view-transitions.test.ts</automated>
    <automated>rg -n 'CalendarViewTransitionKind|WEEK_RAPID_NAVIGATION_MS = 300|startViewTransition\\(\\{ update, types: \\[options.kind\\] \\}\\)|prefers-reduced-motion: reduce' src/lib/ui/view-transitions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/ui/view-transitions.ts` contains `export type CalendarViewTransitionKind = "week-forward" | "week-back" | "day";`.
    - `src/lib/ui/view-transitions.ts` contains `export const WEEK_RAPID_NAVIGATION_MS = 300;`.
    - `src/lib/ui/view-transitions.ts` contains `targetWeek > currentWeek`.
    - `src/lib/ui/view-transitions.ts` contains `targetWeek < currentWeek`.
    - `src/lib/ui/view-transitions.ts` contains `types: [options.kind]`.
    - `src/lib/ui/view-transitions.ts` contains `prefers-reduced-motion: reduce`.
    - `src/lib/ui/view-transitions.ts` does not contain `framer`, `motion/react`, `react-spring`, `ViewTransition`, or `addTransitionType`.
    - `npm test -- src/lib/ui/__tests__/view-transitions.test.ts` exits 0.
  </acceptance_criteria>
  <done>Helper tests pass and the helper is SSR-safe, reduced-motion-safe, unsupported-browser-safe, and statically typed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI to CSS view-transition types | Compare UI selects a transition kind that the browser maps to CSS pseudo-elements. |
| browser capability to helper | Helper consumes `document`, `window.matchMedia`, and optional injected fakes in tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `src/lib/ui/view-transitions.ts` | mitigate | Use `CalendarViewTransitionKind = "week-forward" | "week-back" | "day"` and pass `types: [options.kind]`; no arbitrary selector or user string reaches CSS. |
| T-10-02 | Denial of usability | `runCalendarViewTransition` | mitigate | Bypass native transitions when `prefers-reduced-motion: reduce` matches or `options.reducedMotion === true`; later CSS adds a second guard. |
| T-10-03 | Spoofing | helper scope | accept | This helper does not read tutor data, auth state, API responses, or availability content; stale/loading capture is mitigated in downstream week timing plans. |
| T-10-04 | Elevation of privilege | phase scope | accept | No auth/session/API/schema files are touched in this plan. |
</threat_model>

<verification>
Run:
- `npm test -- src/lib/ui/__tests__/view-transitions.test.ts`
- `rg -n 'viewTransition' next.config.ts` should produce no output in this plan.
- `rg -n 'framer-motion|"motion"|"@react-spring|react-spring' package.json src/lib/ui/view-transitions.ts` should produce no animation dependency usage.
</verification>

<success_criteria>
Plan 01 is complete when the helper exports the exact contracts, focused tests pass, and the implementation has no Next experimental, React canary, or animation-library dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vpol-01-view-transitions/10-01-SUMMARY.md`.
</output>
