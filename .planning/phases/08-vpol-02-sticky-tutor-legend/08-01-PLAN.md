---
phase: 08-vpol-02-sticky-tutor-legend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - STICKY-02
  - STICKY-03

must_haves:
  truths:
    - "Audit document exists at `.planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` and is the FIRST Phase 8 commit (committed BEFORE any code edits) per D-13"
    - "Audit lists the full ancestor chain from `<html>` through `<body>` through the `(app)` layout wrapper through `search-workspace.tsx` through `compare-panel.tsx` down to BOTH sticky targets: (a) WeekOverview's new sticky legend inside the `flex-1 overflow-y-auto min-h-0` scroll container at `week-overview.tsx:310`, and (b) CalendarGrid's sticky day-header at `calendar-grid.tsx:120`"
    - "Audit includes explicit findings for every ancestor on: `overflow`, `transform`, `filter`, `backdrop-blur`, `z-index`, and `creates-stacking-context? (yes/no)` per D-11"
    - "Audit confirms NO ancestor has `overflow-hidden` (other than scroll containers themselves or `min-h-0` flex cells) that would break sticky positioning, satisfying STACK.md:91 pitfall check per D-13(c)"
    - "Audit confirms NO ancestor has `transform`, `filter`, or `backdrop-blur` that creates a stacking context confining the sticky legend's z-index per D-13(d)"
    - "Audit confirms Base UI popovers (rendered via `PopoverPrimitive.Portal` in `src/components/ui/popover.tsx:29`) render with `z-50`, which is ABOVE `Z_LEGEND = 6`, satisfying success criterion #2 per D-13(e)"
    - "REQUIREMENTS.md §STICKY-02 contains an amendment note (under the requirement bullet) recording the simplified 3-tier scale (`Z_CONTENT = 1, Z_LEGEND = 6, Z_POPOVER = 50`) and citing `08-CONTEXT.md D-08..D-10` per D-10. No new requirement ID created; §Traceability row for STICKY-02 is untouched."
  artifacts:
    - path: ".planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md"
      provides: "Pre-implementation stacking-context audit covering two sticky targets"
      min_lines: 60
      contains: "08-STACKING-AUDIT"
    - path: ".planning/REQUIREMENTS.md"
      provides: "§STICKY-02 amendment note recording scale simplification"
      contains: "Z_CONTENT"
  key_links:
    - from: ".planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md"
      to: ".planning/phases/08-vpol-02-sticky-tutor-legend/08-CONTEXT.md"
      via: "D-08..D-15 citations inline in the audit"
      pattern: "D-0[89]|D-1[0-5]"
    - from: ".planning/REQUIREMENTS.md §STICKY-02"
      to: ".planning/phases/08-vpol-02-sticky-tutor-legend/08-CONTEXT.md §D-10"
      via: "Amendment note referencing CONTEXT.md decisions"
      pattern: "08-CONTEXT.md"
---

<objective>
Ship the FIRST Phase 8 commit: a pre-implementation stacking-context audit document at `.planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` plus a minor REQUIREMENTS.md amendment recording the simplified z-index scale. No source code changes in this plan. Per D-13, the audit must land BEFORE any code edits — literal reading of STICKY-03 "pre-implementation stacking-context audit artifact committed alongside the change" converges with the Phase 5 prep-commit pattern on "audit first, code after."

Purpose:
- STICKY-03 literal requirement: the audit artifact must be committed as a pre-implementation artifact.
- D-11..D-13: audit is the single source of truth for stacking-context analysis; no inline `// STICKY-03 audit` code comments should leak across `src/`.
- D-10: record the scale simplification (original 5-slot → new 3-tier) in REQUIREMENTS.md as a same-class amendment to Phase 5 D-02's NVDA relaxation. No new requirement ID minted; STICKY-02 ID is preserved.

Output:
- 1 new file: `08-STACKING-AUDIT.md` (~60+ lines of Markdown tables covering both ancestor chains).
- 1 edited file: `.planning/REQUIREMENTS.md` (single amendment note added under STICKY-02 bullet; footer gets an appended Phase 8 line — the Phase 5 footer line is preserved).
- 2 atomic commits: (1) audit, (2) REQUIREMENTS amendment. Commit order matters: audit lands first so the commit log reflects "audit → code" per D-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-vpol-02-sticky-tutor-legend/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/app/layout.tsx
@src/app/(app)/layout.tsx
@src/app/globals.css
@src/components/search/search-workspace.tsx
@src/components/compare/compare-panel.tsx
@src/components/compare/week-overview.tsx
@src/components/compare/calendar-grid.tsx
@src/components/ui/popover.tsx

<interfaces>
<!-- Ancestor chain evidence extracted from the codebase at CONTEXT time. -->
<!-- These are the exact class lists the audit will enumerate. -->

From src/app/layout.tsx:28-30 (root layout):
```tsx
<html lang="en" className="... h-full antialiased">
  <body className="h-full flex flex-col overflow-hidden">{children}</body>
</html>
```

From src/app/(app)/layout.tsx:7 (app layout):
```tsx
<main className="flex-1 flex flex-col overflow-hidden px-4 lg:px-6 py-3">{children}</main>
```

From src/components/search/search-workspace.tsx:166 (workspace container):
```tsx
<div className="flex-1 flex gap-3 overflow-hidden min-h-0">
```

From src/components/search/search-workspace.tsx:206-210 (compare column — fullscreen toggle target):
```tsx
<div className={cn(
  "flex flex-col overflow-hidden min-w-0 transition-all duration-300 ease-in-out",
  isFullscreen ? "w-full pl-0" : "w-1/2 pl-1",
)}>
  <ComparePanel ... />
</div>
```

From src/components/compare/compare-panel.tsx (calendar-view wrapper line 248):
```tsx
<div className={`flex-1 min-h-0 mt-1 ${activeDay !== null ? "overflow-y-auto" : ""}`}>
```
- Week view (activeDay === null): NO `overflow-y-auto` here; the scroll container is inside WeekOverview itself.
- Day view (activeDay !== null): `overflow-y-auto` applied HERE; CalendarGrid's sticky day-header sticks to THIS container.

From src/components/compare/week-overview.tsx:282 (outer):
```tsx
<div className="flex flex-col h-full overflow-hidden">
```

From src/components/compare/week-overview.tsx:310 (scroll container — THE sticky context for the new legend):
```tsx
<div className="flex-1 overflow-y-auto min-h-0">
```

From src/components/compare/week-overview.tsx:314 (current per-day sticky header — TARGET FOR CONSOLIDATION in Plan 03):
```tsx
<div className="sticky top-0 z-[5] flex bg-background/90 backdrop-blur-sm" style={{ height: 20 }}>
```

From src/components/compare/calendar-grid.tsx:120 (current sticky day-header — TARGET FOR NORMALIZATION in Plan 04):
```tsx
<div className="flex border-b sticky top-0 bg-background z-10">
```

From src/components/ui/popover.tsx:29-36 (Base UI popover — renders via Portal with z-50):
```tsx
<PopoverPrimitive.Portal>
  <PopoverPrimitive.Positioner ... className="isolate z-50">
    <PopoverPrimitive.Popup ... className="z-50 ..." />
  </PopoverPrimitive.Positioner>
</PopoverPrimitive.Portal>
```

From src/app/globals.css:135-145 (global base layer — confirms no html-level transform/filter):
```css
@layer base {
  * { @apply border-border outline-ring/50; }
  body { @apply bg-background text-foreground; }
  html { @apply font-sans; }
}
```
No `transform`, `filter`, `backdrop-filter`, or per-element `overflow` directives on `html` or `body` beyond the Tailwind utilities in markup.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write 08-STACKING-AUDIT.md</name>
  <files>.planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md</files>
  <read_first>
    - .planning/phases/08-vpol-02-sticky-tutor-legend/08-CONTEXT.md §"D-11 through D-13" (audit format + minimum content)
    - .planning/phases/08-vpol-02-sticky-tutor-legend/08-CONTEXT.md §"Integration Points" (globals.css / (app)/layout.tsx / search-workspace.tsx claims to verify)
    - src/app/layout.tsx (full file — confirm `<html class="h-full antialiased">`, `<body class="h-full flex flex-col overflow-hidden">`)
    - src/app/(app)/layout.tsx (full file — confirm `<main class="flex-1 flex flex-col overflow-hidden px-4 lg:px-6 py-3">`)
    - src/app/globals.css lines 135-145 (confirm no body-level transform/filter)
    - src/components/search/search-workspace.tsx lines 165-218 (workspace + compare-column wrappers, fullscreen toggle)
    - src/components/compare/compare-panel.tsx line 248 (calendar-view wrapper)
    - src/components/compare/week-overview.tsx lines 282, 310, 314 (outer, scroll container, CURRENT sticky header)
    - src/components/compare/calendar-grid.tsx line 120 (CURRENT sticky day-header)
    - src/components/ui/popover.tsx lines 28-48 (Portal + z-50 positioner + popup)
  </read_first>
  <action>
Create a new file at `.planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` with the exact structure below. Do NOT modify any source code in this task. Do NOT sprinkle `// STICKY-03 audit` comments across `src/` (D-12 forbids).

The file content MUST be:

```markdown
# Phase 8 Stacking-Context Audit (STICKY-03)

**Committed:** {commit date, e.g., 2026-04-22}
**Purpose:** Pre-implementation stacking-context audit required by STICKY-03. Proves that every ancestor from `<html>` to the two Phase 8 sticky targets has been reviewed for `overflow`, `transform`, `filter`, `backdrop-blur`, and `z-index` — so the sticky legend cannot be silently confined by a parent stacking context after we ship.

**Sticky targets covered:**
1. **WeekOverview global sticky legend** (to be added in Plan 03 inside the `flex-1 overflow-y-auto min-h-0` scroll container at `week-overview.tsx:310`).
2. **CalendarGrid sticky day-header** (existing; normalized in Plan 04 at `calendar-grid.tsx:120`).

**Z-index scale (authoritative per 08-CONTEXT.md D-08..D-09):**

| Slot | Constant | Value | Where Used |
|------|----------|-------|------------|
| Content | `Z_INDEX.content` | `1` | Session cards, conflict bands, free-gap tints, today indicator dots, lane tints |
| Legend | `Z_INDEX.legend` | `6` | WeekOverview sticky legend (new) + CalendarGrid sticky day-header (normalized) |
| Popover | `Z_INDEX.popover` | `50` | Base UI `PopoverPrimitive.Portal` default |

Popovers render via React Portal (src/components/ui/popover.tsx:29 `<PopoverPrimitive.Portal>`) with `z-50` on both the Positioner and the Popup, so they render OUTSIDE the compare-panel DOM subtree and above every in-flow element. Confirmed: `50 > 6 > 1`, success criterion #2 satisfied by construction.

***

## Ancestor Chain A — WeekOverview global sticky legend

Target DOM path: `<html> → <body> → <main> (app layout) → <div class="flex-1 flex gap-3 overflow-hidden min-h-0"> (workspace) → <div class="flex flex-col overflow-hidden min-w-0 ..."> (compare column, toggles w-1/2 ↔ w-full on fullscreen) → ComparePanel fragment → <div class="flex-1 min-h-0 mt-1"> (calendar-view wrapper, NO overflow-y-auto in week view) → WeekOverview <div class="flex flex-col h-full overflow-hidden"> → <div class="flex-1 overflow-y-auto min-h-0"> ← **THIS is the scroll-parent; sticky attaches here** → <div class="sticky top-0 ..."> ← target`.

| File:line | Element / classes | overflow | transform | filter | backdrop-blur | z-index | Creates stacking context? | Notes |
|-----------|-------------------|----------|-----------|--------|---------------|---------|---------------------------|-------|
| `src/app/layout.tsx:27` | `<html class="h-full antialiased">` | (visible) | none | none | none | auto | no | No global html-level transforms/filters per globals.css:135-145 base layer. |
| `src/app/layout.tsx:30` | `<body class="h-full flex flex-col overflow-hidden">` | **hidden** | none | none | none | auto | no | `overflow: hidden` on body. Does NOT break sticky inside WeekOverview because the sticky context is scoped to the nearest SCROLLING ancestor (`overflow-y-auto`), which is `week-overview.tsx:310`. Body's hidden only prevents page-level scroll. |
| `src/app/(app)/layout.tsx:7` | `<main class="flex-1 flex flex-col overflow-hidden px-4 lg:px-6 py-3">` | **hidden** | none | none | none | auto | no | Same analysis: non-scrolling `overflow-hidden`; sticky context still scoped to the inner scroll container. |
| `src/components/search/search-workspace.tsx:166` | `<div class="flex-1 flex gap-3 overflow-hidden min-h-0">` | **hidden** | none | none | none | auto | no | `min-h-0` enables the inner scroll container to claim vertical space correctly under flex layout. |
| `src/components/search/search-workspace.tsx:206-210` | `<div class="flex flex-col overflow-hidden min-w-0 transition-all duration-300 ease-in-out {w-1/2 \| w-full}">` | **hidden** | **`transition-all`** (animates width, not transform) | none | none | auto | **no** | **Critical for D-14 (fullscreen preservation):** `transition-all` covers `width` and `padding` but Tailwind's `transition-all` does NOT include `transform`. Zero stacking-context creation. The fullscreen toggle only changes `w-1/2 ↔ w-full` + `pl-1 ↔ pl-0`; the inner ComparePanel DOM is NOT remounted, so the sticky legend survives the toggle. |
| `src/components/compare/compare-panel.tsx:~248` | `<div class="flex-1 min-h-0 mt-1 {overflow-y-auto \| ""}">` | **auto (day view only)** / visible (week view) | none | none | none | auto | no | In **week view** (`activeDay === null`) this is NOT a scroll container, so WeekOverview's own scroll container at `week-overview.tsx:310` owns the sticky context. In **day view** (`activeDay !== null`) this becomes the scroll container for CalendarGrid (see Chain B). |
| `src/components/compare/week-overview.tsx:282` | `<div class="flex flex-col h-full overflow-hidden">` | **hidden** | none | none | none | auto | no | Outer WeekOverview wrapper. Non-scrolling; inner `overflow-y-auto` is the sticky context. |
| `src/components/compare/week-overview.tsx:310` | `<div class="flex-1 overflow-y-auto min-h-0">` | **auto (scroll)** | none | none | none | auto | **yes (scroll container)** | **This is the sticky context for Chain A.** Sticky children compute `top: 0` relative to THIS element. |
| `src/components/compare/week-overview.tsx:{new}` (Plan 03) | `<div class="sticky top-0 bg-background flex items-center gap-3 px-2 border-b border-border/30" style={{ zIndex: Z_INDEX.legend /* 6 */, height: 24 }}>` — the new consolidated legend | visible | none | none | **none (solid bg, per 08-CONTEXT.md Claude's Discretion: prefer solid `bg-background` over `backdrop-blur-sm` for simplicity)** | **6** | **yes** (sticky + z-index creates one) | THE TARGET. Z_INDEX.legend = 6 is above content (1) and below popover (50). |

**Pitfall checks (per D-13):**

- **(c) `overflow-hidden` breaking sticky?** No. The only `overflow-hidden` ancestors are non-scrolling (`<body>`, `<main>`, workspace, compare column, WeekOverview outer). The sticky context is scoped to the nearest SCROLLING ancestor, which is `week-overview.tsx:310` (`overflow-y-auto`). STACK.md:91 pitfall avoided.
- **(d) `transform`/`filter`/`backdrop-blur` creating confining stacking contexts?** No `transform`, `filter`, or `backdrop-filter` on any ancestor from `<html>` to the legend target. Tailwind `transition-all` on the compare column animates width (not transform). The new legend itself uses solid `bg-background` (no `backdrop-blur-sm`) to avoid creating a backdrop-filter stacking context. (Alternative: if `backdrop-blur-sm` is chosen for visual consistency with the retired per-day lane header, the legend ITSELF becomes a stacking context, which is harmless because its `z-index: 6` sits below popover `z-index: 50` on the parent stacking context — popovers render via Portal outside this subtree so portal hoisting applies either way.)
- **(e) Popovers above legend?** Yes. `src/components/ui/popover.tsx:29` uses `<PopoverPrimitive.Portal>` which appends the popover content outside the compare-panel DOM subtree. Both Positioner and Popup carry `z-50` (>`6`). Confirmed rendered ABOVE the sticky legend.

***

## Ancestor Chain B — CalendarGrid sticky day-header (day view)

Target DOM path: same as Chain A up to `compare-panel.tsx:~248`, then `<div class="flex-1 min-h-0 mt-1 overflow-y-auto">` (day view only — `activeDay !== null`) → `<CalendarGrid>` → `<div class="relative ml-[50px]">` → **`<div class="flex border-b sticky top-0 bg-background z-10">` ← target (Plan 04 normalizes `z-10` → `zIndex: Z_INDEX.legend`)**.

| File:line | Element / classes | overflow | transform | filter | backdrop-blur | z-index | Creates stacking context? | Notes |
|-----------|-------------------|----------|-----------|--------|---------------|---------|---------------------------|-------|
| `src/app/layout.tsx:27` | `<html class="h-full antialiased">` | visible | none | none | none | auto | no | Same as Chain A. |
| `src/app/layout.tsx:30` | `<body class="h-full flex flex-col overflow-hidden">` | hidden | none | none | none | auto | no | Same as Chain A. |
| `src/app/(app)/layout.tsx:7` | `<main class="flex-1 flex flex-col overflow-hidden px-4 lg:px-6 py-3">` | hidden | none | none | none | auto | no | Same as Chain A. |
| `src/components/search/search-workspace.tsx:166` | `<div class="flex-1 flex gap-3 overflow-hidden min-h-0">` | hidden | none | none | none | auto | no | Same as Chain A. |
| `src/components/search/search-workspace.tsx:206-210` | compare column (`w-1/2 \| w-full`) | hidden | `transition-all` (width only) | none | none | auto | no | Same as Chain A — fullscreen preservation confirmed. |
| `src/components/compare/compare-panel.tsx:~248` | `<div class="flex-1 min-h-0 mt-1 overflow-y-auto">` (day view) | **auto (scroll)** | none | none | none | auto | **yes (scroll container)** | **This is the sticky context for Chain B** (note: different from Chain A — in day view, the scroll container lives one level higher than in week view). |
| `src/components/compare/calendar-grid.tsx:118` | `<div class="relative ml-[50px]">` | visible | none | none | none | auto | no (but `position: relative` creates a containing block for `absolute` children; does NOT create a stacking context without a z-index) | Fine. |
| `src/components/compare/calendar-grid.tsx:120` (current state — BEFORE Plan 04) | `<div class="flex border-b sticky top-0 bg-background z-10">` | visible | none | none | none | **10** | **yes** (sticky + z-index) | CURRENT value `z-10` will be normalized to `zIndex: Z_INDEX.legend` (6) in Plan 04. The `TutorProfilePopover` click-trigger + popover content rendered via Portal at `z-50` remains ABOVE this sticky header. |
| `src/components/compare/calendar-grid.tsx:120` (AFTER Plan 04) | `<div class="flex border-b sticky top-0 bg-background" style={{ zIndex: Z_INDEX.legend /* 6 */ }}>` | visible | none | none | none | **6** | yes | Post-normalization. Identical stacking behavior vs `z-10` in practice because popovers portal to body at `z-50` (not constrained by this subtree). |

**Pitfall checks for Chain B:**

- **(c) `overflow-hidden` breaking sticky?** No. Sticky context = `compare-panel.tsx:~248` (`overflow-y-auto` in day view). Non-scrolling `overflow-hidden` ancestors above it do not interfere.
- **(d) `transform`/`filter`/`backdrop-blur` creating confining stacking contexts?** None on the chain. `calendar-grid.tsx:120` uses solid `bg-background`, no `backdrop-blur`.
- **(e) Popovers above header?** Yes. `TutorProfilePopover` (inside `calendar-grid.tsx:125-135`) uses the same Base UI Popover primitive with `Portal + z-50`. Renders above the sticky header.

***

## Fullscreen preservation (STICKY-04 / D-14)

The fullscreen toggle at `search-workspace.tsx:206-217` transitions only the **outer compare column's** `width` (and `padding`) from `w-1/2 pl-1` to `w-full pl-0`. Tailwind `transition-all duration-300 ease-in-out` applies to `width` and `padding` but DOES NOT add a `transform` to this element. No React remount occurs (ComparePanel is not key-swapped; the `isFullscreen` prop flows in but the component tree is stable). Therefore:

- The WeekOverview scroll container at `week-overview.tsx:310` is unchanged (same DOM node, same `overflow-y-auto`).
- The sticky legend inside it is unchanged (same DOM node, same `sticky top-0`).
- The column simply becomes wider; the sticky legend widens with it (it's `display: flex` + full-width by default).

D-14 is satisfied BY CONSTRUCTION. No additional plumbing. Plan 05 records a visual verification check.

***

## Summary of findings

- ✅ No ancestor breaks sticky via `overflow-hidden` on a scrolling context.
- ✅ No ancestor creates a confining stacking context via `transform`, `filter`, or `backdrop-filter`.
- ✅ Popovers portal to `<body>` at `z-50`, above the sticky legend's `z-6`.
- ✅ Fullscreen toggle preserves sticky by construction — no remount, no transform.
- ✅ Two sticky contexts exist (one per view): WeekOverview's internal scroll container in week view; compare-panel's calendar-view wrapper in day view. Both normalized to `Z_INDEX.legend = 6` in Plans 03 + 04.

**Decision:** Proceed with Plan 02 (Z_INDEX constant) → Plan 03 (WeekOverview consolidation) → Plan 04 (CalendarGrid normalization) → Plan 05 (verification). No stacking-context defensive plumbing needed beyond the constant + markup refactor.

***

*Audit completed: {date}. Referenced by 08-CONTEXT.md D-11..D-13.*
```

Replace `{date}` with the current date (`date +%Y-%m-%d` output, e.g., `2026-04-22`). Replace `{commit date, ...}` with the same literal date.

Commit as:
```
git add .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md
git commit -m "docs(08): pre-implementation stacking-context audit (STICKY-03)"
```
Include the Co-Authored-By trailer.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md && grep -c "Phase 8 Stacking-Context Audit" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md | awk '{exit ($1==1?0:1)}' && grep -cE "Ancestor Chain A|Ancestor Chain B" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md | awk '{exit ($1==2?0:1)}' && grep -c "Z_INDEX.legend" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md | awk '{exit ($1>=3?0:1)}' && grep -cE "overflow-hidden|transform|filter|backdrop-blur" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md | awk '{exit ($1>=4?0:1)}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` exits 0
    - `wc -l .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md | awk '{print $1}'` returns at least `60`
    - `grep -c "^# Phase 8 Stacking-Context Audit" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns exactly `1`
    - `grep -c "Ancestor Chain A" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns at least `1`
    - `grep -c "Ancestor Chain B" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns at least `1`
    - `grep -c "week-overview.tsx:310" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns at least `1` (WeekOverview scroll container documented)
    - `grep -c "calendar-grid.tsx:120" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns at least `1` (CalendarGrid sticky header documented)
    - `grep -c "Z_INDEX.legend" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns at least `3`
    - `grep -cE "overflow|transform|filter|backdrop" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns at least `10` (audit covers all four stacking-context triggers)
    - `grep -c "Fullscreen preservation" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns at least `1` (D-14 / STICKY-04 covered)
    - `grep -c "PopoverPrimitive.Portal\|Portal + z-50\|portal to body" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` returns at least `1` (popover z-50 hoist documented)
    - `git log -1 --format="%s"` outputs `docs(08): pre-implementation stacking-context audit (STICKY-03)`
    - `git log -1 --name-only | grep -c "08-STACKING-AUDIT.md"` returns `1` (single-file commit)
    - No source code files were touched in this commit: `git log -1 --name-only | grep -c "^src/"` returns `0`
    - No inline code comments added: `grep -rn "STICKY-03 audit" src/ 2>/dev/null | wc -l` returns `0` (per D-12)
  </acceptance_criteria>
  <done>The audit document exists, covers both ancestor chains, documents all four stacking-context triggers (overflow / transform / filter / backdrop-blur) for every ancestor, confirms popover hoisting via Portal, confirms fullscreen preservation by construction, and is committed as the first Phase 8 commit with no `src/` edits.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Amend REQUIREMENTS.md §STICKY-02 with the simplified scale note</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md §"Visual Polish — Sticky Tutor Legend (VPOL-02)" lines 28-33 (current STICKY-01..04 bullets)
    - .planning/REQUIREMENTS.md §Traceability row for STICKY-02 (line ~155)
    - .planning/REQUIREMENTS.md footer (last ~5 lines — contains the Phase 5 D-02 amendment footer that must be PRESERVED per Warning 5 append-only convention)
    - .planning/phases/08-vpol-02-sticky-tutor-legend/08-CONTEXT.md §"D-08 through D-10" (amendment rationale)
    - .planning/phases/05-polish-drain/05-CONTEXT.md §"D-02" (precedent: NVDA relaxation amendment pattern)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` — add ONE amendment note under the STICKY-02 bullet at line 31. Do NOT change the STICKY-02 ID, do NOT create a new requirement, do NOT touch the §Traceability table, do NOT touch any other requirement bullet.

**BEFORE (at lines 30-33):**

```markdown
- [ ] **STICKY-01**: Tutor legend (chip strip in compare panel) remains visible during calendar vertical scroll via `position: sticky`
- [ ] **STICKY-02**: Z-index scale constant is introduced (`z-[2]` content, `z-[4]` legend, `z-[5]` lane headers, `z-[6]` sticky day-header, `z-50` popovers) and applied consistently
- [ ] **STICKY-03**: Pre-implementation stacking-context audit artifact committed alongside the change
- [ ] **STICKY-04**: Fullscreen compare mode preserves sticky legend behavior (no regression)
```

**AFTER:**

```markdown
- [ ] **STICKY-01**: Tutor legend (chip strip in compare panel) remains visible during calendar vertical scroll via `position: sticky`
- [ ] **STICKY-02**: Z-index scale constant is introduced (`z-[2]` content, `z-[4]` legend, `z-[5]` lane headers, `z-[6]` sticky day-header, `z-50` popovers) and applied consistently
  - *Amendment (2026-04-22, Phase 8 CONTEXT D-08..D-10):* Scale simplified at CONTEXT time to a 3-tier scale `Z_CONTENT = 1`, `Z_LEGEND = 6`, `Z_POPOVER = 50` because Phase 8 D-01 consolidates per-day lane headers INTO the single sticky legend — the separate `z-[5]` lane-headers slot is no longer a live surface. CalendarGrid's existing sticky day-header reuses `Z_LEGEND = 6`. Constant ships at `src/lib/ui/z-index.ts` (`export const Z_INDEX = { content: 1, legend: 6, popover: 50 } as const;`). Same class as Phase 5 D-02's NVDA relaxation — no new requirement ID, §Traceability row for STICKY-02 unchanged.
- [ ] **STICKY-03**: Pre-implementation stacking-context audit artifact committed alongside the change
- [ ] **STICKY-04**: Fullscreen compare mode preserves sticky legend behavior (no regression)
```

Date inside the amendment (`2026-04-22`) MUST be the commit date. If committing on a different date, substitute accordingly via `$(date +%Y-%m-%d)`.

**Do NOT:**
- Remove or rewrite the original STICKY-02 bullet (the original 5-slot scale text stays — the amendment is an addition, not a replacement).
- Add a new row to the §Traceability table (the mapping `STICKY-02 → Phase 8 → Pending` is correct as-is).
- Touch STICKY-01, STICKY-03, or STICKY-04 bullets.
- Touch any other requirement (MOD, PAST, DENS, TRANS, POLISH).

**Footer amendment — APPEND, do NOT replace (Warning 5 fix, append-only Markdown convention).** The current footer (line ~200) reads:

```markdown
*Last updated: 2026-04-21 — Phase 5 CONTEXT.md D-02 amendment: POLISH-01 scoped to VoiceOver-only; NVDA-v12 added to v1.2+ deferred list*
```

**Keep that line verbatim.** Immediately below it (same block, new line, no blank line), add:

```markdown
*Phase 8 D-10 amendment: 2026-04-22 — STICKY-02 scale simplified from 5-slot to 3-tier post-consolidation (see 08-CONTEXT.md D-08..D-10).*
```

After edit, the footer block contains TWO lines (Phase 5 first, Phase 8 second). Do not alter, collapse, or remove the Phase 5 line. This preserves the canonical audit trail of amendments.

Commit as:
```
git add .planning/REQUIREMENTS.md
git commit -m "docs(08): amend STICKY-02 with simplified 3-tier z-index scale"
```
Include the Co-Authored-By trailer.
  </action>
  <verify>
    <automated>grep -c "Amendment (2026-04-22, Phase 8 CONTEXT D-08..D-10)" .planning/REQUIREMENTS.md | awk '{exit ($1==1?0:1)}' && grep -c "Z_CONTENT = 1, Z_LEGEND = 6, Z_POPOVER = 50\|Z_CONTENT = 1\`, \`Z_LEGEND = 6\`, \`Z_POPOVER = 50" .planning/REQUIREMENTS.md | awk '{exit ($1>=1?0:1)}' && grep -c "src/lib/ui/z-index.ts" .planning/REQUIREMENTS.md | awk '{exit ($1>=1?0:1)}' && grep -c "Phase 8 D-10 amendment: 2026-04-22" .planning/REQUIREMENTS.md | awk '{exit ($1==1?0:1)}' && grep -c "Phase 5 CONTEXT.md D-02 amendment" .planning/REQUIREMENTS.md | awk '{exit ($1==1?0:1)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Amendment (2026-04-22, Phase 8 CONTEXT D-08..D-10)" .planning/REQUIREMENTS.md` returns exactly `1`
    - `grep -c "Z_CONTENT = 1" .planning/REQUIREMENTS.md` returns at least `1`
    - `grep -c "Z_LEGEND = 6" .planning/REQUIREMENTS.md` returns at least `1`
    - `grep -c "Z_POPOVER = 50" .planning/REQUIREMENTS.md` returns at least `1`
    - `grep -c "src/lib/ui/z-index.ts" .planning/REQUIREMENTS.md` returns at least `1`
    - `grep -c "\\*\\*STICKY-02\\*\\*" .planning/REQUIREMENTS.md` returns exactly `1` (still a single STICKY-02 bullet — no duplicate ID, no new requirement row)
    - The original STICKY-02 text is preserved verbatim (Blocker 1 fix: outer single-quotes, `.` wildcards inside regex so backticks lose meaning): `grep -c 'z-\[2\]. content, .z-\[4\]. legend, .z-\[5\]. lane headers, .z-\[6\]. sticky day-header, .z-50. popovers' .planning/REQUIREMENTS.md` returns `1` (the original 5-slot wording is untouched; the amendment is an addendum)
    - The §Traceability row for STICKY-02 is unchanged: `grep -c "| STICKY-02 | Phase 8 | Pending |" .planning/REQUIREMENTS.md` returns `1`
    - The §Traceability coverage count is untouched (Warning 3 fix: anchored to start/end of line via `-E`): `grep -cE "^- v1.1 requirements: 40 total$" .planning/REQUIREMENTS.md` returns `1` (no new requirement minted)
    - Phase 5 footer line PRESERVED (Warning 5 fix — append-only): `grep -c "Last updated: 2026-04-21 — Phase 5 CONTEXT.md D-02 amendment" .planning/REQUIREMENTS.md` returns `1`
    - Phase 8 footer amendment APPENDED (Warning 5 fix): `grep -c "Phase 8 D-10 amendment: 2026-04-22 — STICKY-02 scale simplified from 5-slot to 3-tier post-consolidation" .planning/REQUIREMENTS.md` returns `1`
    - Footer block has TWO italic lines at the end: `grep -c "^\\*" .planning/REQUIREMENTS.md` returns a value consistent with prior footer + 1 new line (baseline + 1; executor confirms by eyeballing `tail -5 .planning/REQUIREMENTS.md`)
    - `git log -1 --format="%s"` outputs `docs(08): amend STICKY-02 with simplified 3-tier z-index scale`
    - `git log -1 --name-only | grep -c "^.planning/REQUIREMENTS.md$"` returns `1` (single-file commit)
    - No source code files touched: `git log -1 --name-only | grep -c "^src/"` returns `0`
  </acceptance_criteria>
  <done>REQUIREMENTS.md §STICKY-02 has the D-10 amendment note recorded inline under the bullet (not replacing it). Traceability and requirement count unchanged. Footer Phase 5 line preserved verbatim; a new Phase 8 amendment line is appended below it. Two commits total in this plan (audit first, amendment second).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan produces a planning-artifact Markdown file and a planning-requirements Markdown amendment. No code paths, no input surfaces, no auth changes, no env vars.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Spoofing | audit / amendment authorship | accept | Commits are signed Co-Authored-By; no tampering risk beyond normal git history. |
| T-08-01-02 | Tampering | REQUIREMENTS.md content integrity | accept | Markdown text; reviewed as part of normal git review; no runtime consumer. |
| T-08-01-03 | Repudiation | amendment attribution | accept | Amendment note cites commit date + CONTEXT.md decision IDs — auditable. |
| T-08-01-04 | Info Disclosure | audit surfaces DOM classes | accept | Classes already public in client bundle; no secrets. |
| T-08-01-05 | Denial of Service | N/A | N/A | Pure docs change, no runtime impact. |
| T-08-01-06 | Elevation of Privilege | N/A | N/A | No privilege surfaces. |

LOW severity overall — UI refactor with no net-new input surfaces (per 08-CONTEXT.md threat conclusion).
</threat_model>

<verification>
After both tasks commit:

1. `test -f .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` exits 0
2. `wc -l .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md | awk '{print $1}'` returns ≥ 60
3. `grep -c "Amendment (2026-04-22, Phase 8 CONTEXT D-08..D-10)" .planning/REQUIREMENTS.md` returns `1`
4. Phase 5 footer preserved AND Phase 8 footer appended: `grep -c "Phase 5 CONTEXT.md D-02 amendment" .planning/REQUIREMENTS.md` returns `1` AND `grep -c "Phase 8 D-10 amendment: 2026-04-22" .planning/REQUIREMENTS.md` returns `1`
5. `git log --oneline -2 -- .planning/phases/08-vpol-02-sticky-tutor-legend/ .planning/REQUIREMENTS.md` shows exactly two commits, with `docs(08): pre-implementation stacking-context audit (STICKY-03)` appearing BEFORE `docs(08): amend STICKY-02 with simplified 3-tier z-index scale` (i.e., audit landed first per D-13)
6. `git log -2 --name-only | grep -c "^src/"` returns `0` (zero source files touched in either commit)
7. `npm test --run 2>&1 | tail -5 | grep -qE "Tests.*passed"` — baseline 141 tests still pass (no code changes → no test impact)
</verification>

<success_criteria>
- Audit file exists with both ancestor chains, all four stacking-context triggers documented, and fullscreen + popover findings
- REQUIREMENTS.md §STICKY-02 has the amendment note; §Traceability unchanged; no new requirement ID
- Phase 5 footer line PRESERVED verbatim; Phase 8 amendment line APPENDED below it (append-only Markdown convention)
- Two atomic commits in chronological order: audit → amendment
- Zero source code files touched
- All 141 existing tests still pass
- `08-01-SUMMARY.md` records both commit SHAs + audit row count
</success_criteria>

<output>
After completion, create `.planning/phases/08-vpol-02-sticky-tutor-legend/08-01-SUMMARY.md` documenting:
- Two commit SHAs (audit + amendment)
- Audit file line count (`wc -l`)
- Confirmation that audit landed BEFORE the amendment per D-13
- Confirmation that zero `src/` files were touched
- Confirmation that the Phase 5 footer line is intact and the Phase 8 line sits below it
- Baseline test count (141) unchanged post-commit
</output>
</content>
