---
phase: 08-vpol-02-sticky-tutor-legend
plan: 05
type: execute
wave: 4
depends_on: [08-03, 08-04]
files_modified:
  - .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md
autonomous: false
requirements:
  - STICKY-01
  - STICKY-02
  - STICKY-03
  - STICKY-04

must_haves:
  truths:
    - "A verification document at `.planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` exists and captures proof that all 4 ROADMAP.md §Phase 8 success criteria are satisfied on the deployed preview/production URL"
    - "Automated regression pass captured: `npm test --run` shows all 141 baseline tests passing post-Plan-03 + Plan-04; `npx tsc --noEmit` shows no new errors scoped to the edited files"
    - "Full codebase grep confirms Z_INDEX is imported AND consumed in both sticky-target files and nowhere else (no orphaned imports, no extra consumers)"
    - "Human verified on a deployed Vercel preview (or localhost `npm run dev`) that: (a) scrolling week view vertically keeps the sticky legend pinned, (b) clicking a top-of-grid session renders its popover ABOVE the sticky legend, (c) fullscreen toggle preserves the legend, (d) all 7 day-of-week tabs + day drill-down views work, (e) 1-tutor / 2-tutor / 3-tutor layouts all render the legend identically"
    - "A checkpoint pauses for the user to complete the visual walkthrough — this is the one Phase 8 task that cannot be fully automated"
  artifacts:
    - path: ".planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md"
      provides: "Phase 8 verification attestation covering all 4 success criteria with captured command outputs + user sign-off"
      min_lines: 60
      contains: "STICKY-01"
  key_links:
    - from: ".planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md"
      to: "ROADMAP.md §Phase 8 Success Criteria + REQUIREMENTS.md §STICKY-01..04"
      via: "Explicit one-to-one mapping table with captured evidence per requirement"
      pattern: "STICKY-0[1-4]"
    - from: ".planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md"
      to: "08-STACKING-AUDIT.md (Plan 01)"
      via: "Cross-reference: audit findings + post-implementation confirmation"
      pattern: "08-STACKING-AUDIT.md"
---

<objective>
Final Phase 8 plan: produce an attestation document `08-VERIFICATION.md` that captures (a) the automated regression pass after Plans 03 + 04 land, (b) codebase-wide grep evidence that Z_INDEX is consumed exactly where expected, and (c) a human visual-QA walkthrough proving all 4 ROADMAP.md Phase 8 success criteria are satisfied on a deployed preview (or localhost). This is the mandatory `checkpoint:human-verify` for Phase 8 — sticky behavior is CSS, CSS is visually verified.

Purpose:
- STICKY-01: confirm the legend stays pinned during vertical scroll in week view.
- STICKY-02: confirm Z_INDEX is consistently applied and popovers render above.
- STICKY-03: cross-reference the audit artifact committed in Plan 01 with post-implementation reality.
- STICKY-04: confirm fullscreen toggle preserves the sticky legend (D-14 by construction; this is the visual confirmation).
- Mirrors Phase 5 Plan 07's "one prod-site sitting" human-QA walkthrough cadence.

Output:
- 1 new file: `08-VERIFICATION.md` (~60+ lines).
- 1 atomic commit.
- A single `checkpoint:human-verify` task that pauses for the user to run the walkthrough and paste results back.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-vpol-02-sticky-tutor-legend/08-CONTEXT.md
@.planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/lib/ui/z-index.ts
@src/components/compare/week-overview.tsx
@src/components/compare/calendar-grid.tsx

<interfaces>
<!-- Expected post-Plan-04 state of Z_INDEX consumers across the codebase. -->

Expected Z_INDEX imports across the codebase (after Plans 02-04):

| File | Expected count | Notes |
|------|----------------|-------|
| `src/lib/ui/z-index.ts` | N/A (declaration site) | Exports `Z_INDEX` once |
| `src/components/compare/week-overview.tsx` | 1 import, 1 use (`Z_INDEX.legend`) | Plan 03 |
| `src/components/compare/calendar-grid.tsx` | 1 import, 1 use (`Z_INDEX.legend`) | Plan 04 |
| All other files | 0 | Phase 8 scope is legend + day-header only |

Expected sticky-element inventory:

| File:line | Sticky element | z-index source | Notes |
|-----------|----------------|----------------|-------|
| `src/components/compare/week-overview.tsx` | Consolidated sticky tutor legend | `Z_INDEX.legend` (6) | Plan 03 — new |
| `src/components/compare/calendar-grid.tsx:120` | Sticky day-header with TutorProfilePopover | `Z_INDEX.legend` (6) | Plan 04 — normalized from `z-10` |
| (nowhere else) | — | — | No other `sticky top-0` remains in `src/components/compare/` |

<!-- Expected ROADMAP success criteria (Plan 05 attests all four): -->
<!-- 1. Admin scrolling the week view down sees the tutor legend remain pinned at the top of the scrollable container at all times. -->
<!-- 2. Admin clicking a session near the top of the visible calendar area sees the popover render ABOVE the sticky legend. -->
<!-- 3. Stacking-context audit document is committed alongside the code change. -->
<!-- 4. Admin toggling fullscreen compare mode sees the sticky legend preserved at full width with no regression. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold 08-VERIFICATION.md with automated evidence + walkthrough checklist</name>
  <files>.planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 8: VPOL-02 Sticky Tutor Legend" (4 success criteria that map one-to-one to verification steps)
    - .planning/REQUIREMENTS.md §"Visual Polish — Sticky Tutor Legend (VPOL-02)" (STICKY-01..04 bullets + the D-10 amendment note from Plan 01)
    - .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md (the pre-implementation audit — verify its findings still hold post-Plans-03/04)
    - .planning/phases/05-polish-drain/05-07-PLAN.md (if it exists — reference for the "one prod-site sitting" walkthrough cadence; do NOT copy its scope, only match the format)
    - src/lib/ui/z-index.ts (confirm final state — `Z_INDEX = { content: 1, legend: 6, popover: 50 } as const`)
    - src/components/compare/week-overview.tsx (confirm the new sticky legend block exists with `Z_INDEX.legend` per Plan 03)
    - src/components/compare/calendar-grid.tsx (confirm line ~120 uses `zIndex: Z_INDEX.legend` per Plan 04; no `z-10` remains)
  </read_first>
  <action>
Create a new file at `.planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` with the following structure. The automated-evidence section MUST be populated by running the grep/test commands locally and pasting their outputs; the human-walkthrough section starts as a checklist the user fills in during the checkpoint.

**File content template:**

```markdown
# Phase 8 VPOL-02 Sticky Tutor Legend — Verification

**Date:** {commit date, e.g., 2026-04-22}
**Status:** Verification pending user walkthrough / Verification complete (strike whichever does not apply)
**Ship target:** Vercel preview at {preview URL} → production at https://bgscheduler.vercel.app

## Phase 8 Success Criteria (from ROADMAP.md §"Phase 8: VPOL-02 Sticky Tutor Legend")

| # | Criterion | Verification method | Result |
|---|-----------|---------------------|--------|
| 1 | Admin scrolling the week view down sees the tutor legend remain pinned at the top of the scrollable container at all times | Human visual QA (week view + vertical scroll at 15+ grid rows) + automated grep | {PENDING \| PASS \| FAIL} |
| 2 | Admin clicking a session near the top of the visible calendar area sees the popover render ABOVE the sticky legend (z-index scale constant applied consistently across content, legend, lane headers, day-header, popovers) | Human visual QA (click a 7-8am session after scrolling down) + automated grep for Z_INDEX consumers | {PENDING \| PASS \| FAIL} |
| 3 | Stacking-context audit document is committed alongside the code change (proving every ancestor's `overflow`, `transform`, `filter`, `backdrop-blur`, and `z-index` were reviewed) | File exists; audit was FIRST commit of Phase 8 per D-13 | PASS (see Plan 01 commit) |
| 4 | Admin toggling fullscreen compare mode sees the sticky legend preserved at full width with no regression | Human visual QA (toggle fullscreen in week view while scrolled mid-grid) | {PENDING \| PASS \| FAIL} |

***

## Section A — Automated Evidence (must be populated BEFORE the checkpoint)

### A.1 Regression test pass

```bash
$ npm test --run 2>&1 | tail -10
{paste output — must show ≥ 141 tests passing, 0 failing}
```

Result: {PASS / FAIL}

### A.2 TypeScript compilation (scoped to Phase 8 edits)

```bash
$ npx tsc --noEmit 2>&1 | grep -E "z-index\\.ts|week-overview\\.tsx|calendar-grid\\.tsx"
{paste output — expected: empty}
```

Result: {PASS / FAIL}

(Note: the project has pre-existing environmental TS errors on `vitest`, `tailwind-merge`, `next/navigation` per STATE.md anti-patterns; those are acceptable. The grep scopes the check to the files Phase 8 touched.)

### A.3 Z_INDEX inventory grep

```bash
$ grep -rn "Z_INDEX" src/ 2>/dev/null
{paste output — expected three categories:
  1. src/lib/ui/z-index.ts: the export declaration + JSDoc references
  2. src/components/compare/week-overview.tsx: 1 import + 1 use (Z_INDEX.legend)
  3. src/components/compare/calendar-grid.tsx: 1 import + 1 use (Z_INDEX.legend)
 No other consumers.}
```

Consumer count:
- `grep -rc 'from "@/lib/ui/z-index"' src/` returns: {2}
- `grep -rc "Z_INDEX.legend" src/components/compare/` returns: {2}
- `grep -rc "Z_INDEX.content" src/` returns: {0}   (content-layer normalization out of scope for Phase 8)
- `grep -rc "Z_INDEX.popover" src/` returns: {0}   (popover z-50 handled by Base UI Portal default)

Result: {PASS / FAIL} — consumers match the expected 2/2/0/0 pattern.

### A.4 No stale `z-10` remains in CalendarGrid sticky header

```bash
$ grep -c "z-10" src/components/compare/calendar-grid.tsx
0
```

Result: {PASS / FAIL}

### A.5 No stale per-day sticky lane header remains in WeekOverview

```bash
$ grep -c "Sticky lane headers — CAL-02" src/components/compare/week-overview.tsx
0

$ grep -c "Consolidated sticky tutor legend" src/components/compare/week-overview.tsx
1

$ grep -c 'aria-label="Tutor legend"' src/components/compare/week-overview.tsx
1
```

Result: {PASS / FAIL}

### A.6 Stacking-context audit artifact exists

```bash
$ test -f .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md && echo EXISTS
EXISTS

$ wc -l .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md | awk '{print $1}'
{paste row count — expected ≥ 60}

$ grep -c "Ancestor Chain A\\|Ancestor Chain B" .planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md
2
```

Result: PASS (Plan 01 artifact — unchanged by Plans 02-04)

### A.7 REQUIREMENTS.md §STICKY-02 amendment

```bash
$ grep -c "Amendment (2026-04-22, Phase 8 CONTEXT D-08..D-10)" .planning/REQUIREMENTS.md
1
```

Result: PASS (Plan 01 amendment — unchanged by Plans 02-04)

### A.8 Commit inventory (Phase 8 commit graph)

```bash
$ git log --oneline --first-parent origin/main..HEAD -- .planning/phases/08-vpol-02-sticky-tutor-legend/ .planning/REQUIREMENTS.md src/lib/ui/z-index.ts src/components/compare/week-overview.tsx src/components/compare/calendar-grid.tsx
{paste output — expected ordering:
  1. docs(08): pre-implementation stacking-context audit (STICKY-03)           # Plan 01, commit 1
  2. docs(08): amend STICKY-02 with simplified 3-tier z-index scale              # Plan 01, commit 2
  3. feat(08): add Z_INDEX scale constant at src/lib/ui/z-index.ts (STICKY-02)  # Plan 02
  4. feat(08): consolidate per-day lane headers into global sticky legend (STICKY-01, STICKY-02)  # Plan 03
  5. refactor(08): normalize CalendarGrid sticky header to Z_INDEX.legend (STICKY-02)  # Plan 04
  (Plan 05 will add the verification commit at the END — this grep may include it if run post-commit)
}
```

D-13 audit-first ordering confirmed: commit 1 is the audit; all code commits come AFTER.

Result: {PASS / FAIL}

***

## Section B — Human Visual QA Walkthrough (checkpoint)

**Environment:** Deployed Vercel preview URL: {paste URL — either from `npx vercel` output or `vercel.com/dashboard`; localhost:3000 is acceptable if a preview deploy is unavailable}.

**Prerequisites (complete before starting the walkthrough):**

- [ ] You are signed in at the preview URL with one of the 9 admin emails (see AGENTS.md §"Admin Users")
- [ ] Select 3 tutors via the chip strip to exercise the multi-tutor layout (if fewer than 3 tutors are available with real data, 2 is acceptable; 1 must also be tested at minimum)
- [ ] Week view is active (activeDay === null — the "Week" tab is highlighted)

### B.1 STICKY-01 — Legend stays pinned during vertical scroll (1 tutor)

- [ ] Select ONE tutor
- [ ] Verify the sticky legend renders a single slot `[● {tutor name}]` at the top of the scroll container (D-04: legend renders always, even for 1 tutor)
- [ ] Scroll the week view down until at least 5 hours of grid have scrolled past
- [ ] **Expected:** Legend stays pinned at the top of the scroll container. Never disappears off-screen, never goes under the day-name row above.
- [ ] **Record:** {PASS / FAIL} — {short observation}

### B.2 STICKY-01 — Legend stays pinned during vertical scroll (2-3 tutors)

- [ ] Select 3 tutors (via chip strip or discovery panel)
- [ ] Verify the sticky legend renders `[● name1] [● name2] [● name3]` in a single left-aligned row
- [ ] Scroll the week view down to the bottom of the grid
- [ ] **Expected:** All three slots remain visible and color-coded. Dot colors match the per-session card border colors below.
- [ ] **Record:** {PASS / FAIL} — {short observation}

### B.3 STICKY-02 — Popover renders above the legend

- [ ] With 3 tutors selected, find a session near 7-8am (top of grid, just under the sticky legend)
- [ ] Scroll the grid down slightly so that 7am is just below the legend but the session card is still fully visible
- [ ] Click the 7-8am session card
- [ ] **Expected:** The popover (white card with tutor name + session details + badges) renders at `z-50` and overlays the sticky legend. No part of the legend appears ON TOP of the popover.
- [ ] **Record:** {PASS / FAIL} — {short observation}

### B.4 STICKY-02 — CalendarGrid day view popover still works (D-07 asymmetric)

- [ ] Click any day tab (e.g., "Mon") to enter day view
- [ ] **Expected:** Sticky day-header renders with `zIndex: Z_INDEX.legend = 6` (functionally identical to prior `z-10` — no visible change, tutors' names + subtitles still show, sticky still works)
- [ ] Click the tutor name (linked button with hover underline) in the sticky day-header
- [ ] **Expected:** TutorProfilePopover opens ABOVE the sticky header (Portal-hoisted, `z-50`). D-07 asymmetric interaction preserved: day view legend is clickable, week view legend is display-only.
- [ ] **Record:** {PASS / FAIL} — {short observation}

### B.5 STICKY-04 — Fullscreen toggle preserves sticky legend

- [ ] Return to week view (click "Week" tab)
- [ ] With 3 tutors selected, click the maximize icon at the right of the chip strip to enter fullscreen
- [ ] **Expected:** The compare column animates to full width over ~300ms. The sticky legend is STILL pinned at the top of the scroll container at full width (D-14 by construction).
- [ ] Scroll the fullscreen week view down
- [ ] **Expected:** Legend stays pinned; grid scrolls underneath.
- [ ] Click the minimize icon to exit fullscreen
- [ ] **Expected:** Compare column animates back to 50% width. Legend is still pinned.
- [ ] **Record:** {PASS / FAIL} — {short observation}

### B.6 Responsive narrow-panel sanity (Claude's Discretion)

- [ ] In non-fullscreen mode (50% compare panel) on a 13" MacBook (or whatever the user's primary display is), with 3 tutors selected
- [ ] **Expected:** All three legend slots are readable. Tutor names may truncate (`truncate` class is applied) on very long names but dots are never clipped.
- [ ] **Record:** {PASS / FAIL — if FAIL, a compact-legend fallback is a valid out-of-scope follow-up for v1.2 per 08-CONTEXT.md `<deferred>`}

### B.7 STICKY-03 — Audit artifact cross-check

- [ ] Open `.planning/phases/08-vpol-02-sticky-tutor-legend/08-STACKING-AUDIT.md` in an editor or `cat` it
- [ ] Verify: (a) Ancestor Chain A (WeekOverview) is documented, (b) Ancestor Chain B (CalendarGrid) is documented, (c) the four pitfall checks (overflow / transform / filter / backdrop-blur / popover hoisting) are all marked satisfied, (d) fullscreen preservation is documented
- [ ] **Record:** PASS (audit landed in Plan 01; Plans 02-04 did not invalidate any of its findings)

***

## Section C — Sign-off

- [ ] All automated checks in Section A: PASS
- [ ] All human-QA checks in Section B: PASS (or documented known-issue with follow-up)
- [ ] All 4 ROADMAP.md Phase 8 success criteria: satisfied
- [ ] Phase 8 requirements STICKY-01..04 in REQUIREMENTS.md: ready to be marked Complete in §Traceability (done in a follow-up commit or in the Phase 8 SUMMARY)

**Signed:** {user email / date}

**Known issues carried forward to v1.2 (if any):** {list; can be empty}

***

*Verification completed: {date}. References 08-CONTEXT.md, 08-STACKING-AUDIT.md, and ROADMAP.md §Phase 8.*
```

Replace `{date}`, `{commit date, ...}`, `{preview URL}`, `{paste output}`, `{PASS / FAIL}`, `{short observation}`, `{user email / date}`, and `{list; can be empty}` with real values during the checkpoint. `{2}`, `{0}`, `{PASS}`-marked static assertions should be verified locally by running the command and confirming the expected value.

**Commit (after scaffolding the file, BEFORE the checkpoint):**

```bash
git add .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md
git commit -m "docs(08): scaffold Phase 8 verification checklist"
```
Include the Co-Authored-By trailer.

(The user will amend/update the document during the checkpoint and create a FOLLOW-UP commit with the completed walkthrough results. Do NOT try to pre-fill the human-QA section in this scaffolding step.)
  </action>
  <verify>
    <automated>test -f .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md && grep -c "Phase 8 Success Criteria" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md | awk '{exit ($1>=1?0:1)}' && grep -c "STICKY-01\\|STICKY-02\\|STICKY-03\\|STICKY-04" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md | awk '{exit ($1>=4?0:1)}' && grep -cE "Section A|Section B|Section C" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md | awk '{exit ($1==3?0:1)}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` exits 0
    - `wc -l .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md | awk '{print $1}'` returns at least `60`
    - `grep -c "^# Phase 8 VPOL-02 Sticky Tutor Legend — Verification" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` returns `1`
    - All 4 STICKY IDs appear: `grep -c "STICKY-01" file`, `STICKY-02`, `STICKY-03`, `STICKY-04` each return at least `1`
    - All 4 success-criteria rows present in the table: `grep -cE "\\| [1-4] \\|" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` returns at least `4`
    - Section structure present: `grep -c "^## Section A" file` returns `1`, same for Section B and Section C
    - Commit-graph expectation present: `grep -c "docs(08): pre-implementation stacking-context audit" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` returns at least `1` (Plan 01 commit referenced)
    - Walkthrough items B.1-B.7 present: `grep -cE "^### B\\.[1-7]" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` returns at least `7`
    - Cross-reference to audit present: `grep -c "08-STACKING-AUDIT.md" file` returns at least `1`
    - Cross-reference to ROADMAP present: `grep -c "ROADMAP.md" file` returns at least `1`
    - `git log -1 --format="%s"` outputs `docs(08): scaffold Phase 8 verification checklist`
    - `git log -1 --name-only | grep -c "08-VERIFICATION.md"` returns `1` (single-file commit)
    - `git log -1 --name-only | grep -c "^src/"` returns `0` (no source edits in this commit)
  </acceptance_criteria>
  <done>The verification scaffold exists with all four STICKY success criteria mapped to evidence rows, an automated-evidence section populated with exact grep/test commands, and a 7-step human-QA walkthrough checklist. The scaffold is committed BEFORE the checkpoint so the user has a concrete document to fill in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual QA walkthrough on deployed preview</name>
  <files>.planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <what-built>
Phase 8 VPOL-02 Sticky Tutor Legend is now implemented across Plans 01-04:
- Plan 01: `08-STACKING-AUDIT.md` pre-implementation audit + REQUIREMENTS.md amendment (2 commits)
- Plan 02: `src/lib/ui/z-index.ts` with `Z_INDEX = { content: 1, legend: 6, popover: 50 } as const` (1 commit)
- Plan 03: `src/components/compare/week-overview.tsx` per-day lane header → consolidated global sticky legend (1 commit)
- Plan 04: `src/components/compare/calendar-grid.tsx` sticky day-header normalized from `z-10` to `Z_INDEX.legend` while preserving TutorProfilePopover click behavior (1 commit)
- Plan 05 so far: `08-VERIFICATION.md` scaffold (1 commit)

Deployed to Vercel preview: {Claude will run `npx vercel` before the checkpoint if a preview is available, or inform the user to run `npm run dev` locally if Vercel CLI is unavailable}.
  </what-built>
  <action>
Hand control to the user to perform the visual QA walkthrough documented in `<how-to-verify>` below. This is a `checkpoint:human-verify` task per the GSD checkpoint convention — the executor must NOT auto-PASS; the user physically interacts with the deployed application (or localhost) and records PASS/FAIL per walkthrough step B.1 through B.7 in `08-VERIFICATION.md`. After the user types `approved` on the resume signal, the executor proceeds to create two follow-up commits: (a) the completed walkthrough, (b) the REQUIREMENTS.md §Traceability flip to Complete. See `<how-to-verify>` for the full script.
  </action>
  <how-to-verify>
1. Open `.planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` in your editor — this is the exact checklist to follow.

2. If Vercel preview is available:
   - Open the preview URL printed by `npx vercel`
   - Sign in with your admin Google account
   
   Otherwise:
   - Run `npm run dev` locally and open `http://localhost:3000`
   - Sign in with `kevhsh7@gmail.com` (or any admin email)

3. Populate **Section A (Automated Evidence)** by running these commands locally and pasting the outputs into the doc:
   ```bash
   npm test --run 2>&1 | tail -10
   npx tsc --noEmit 2>&1 | grep -E "z-index\\.ts|week-overview\\.tsx|calendar-grid\\.tsx"
   grep -rn "Z_INDEX" src/ 2>/dev/null
   grep -rc 'from "@/lib/ui/z-index"' src/
   grep -rc "Z_INDEX.legend" src/components/compare/
   grep -c "z-10" src/components/compare/calendar-grid.tsx
   grep -c "Sticky lane headers — CAL-02" src/components/compare/week-overview.tsx
   grep -c "Consolidated sticky tutor legend" src/components/compare/week-overview.tsx
   git log --oneline --first-parent origin/main..HEAD -- .planning/phases/08-vpol-02-sticky-tutor-legend/ .planning/REQUIREMENTS.md src/lib/ui/z-index.ts src/components/compare/week-overview.tsx src/components/compare/calendar-grid.tsx
   ```

4. Execute **Section B (Human Visual QA Walkthrough)** — steps B.1 through B.7:
   - B.1: 1-tutor sticky pin during scroll
   - B.2: 3-tutor sticky pin during scroll
   - B.3: Popover renders ABOVE the sticky legend (z-50 > z-6)
   - B.4: Day-view CalendarGrid sticky header + TutorProfilePopover click still works (D-07 asymmetric)
   - B.5: Fullscreen toggle preserves sticky legend
   - B.6: Narrow-panel readability sanity check on your 13" MacBook
   - B.7: Audit cross-check (Plan 01 artifact still reflects reality)
   - Mark each PASS/FAIL and add a one-line observation.

5. Complete **Section C (Sign-off)** with your email + today's date.

6. Create a FOLLOW-UP commit with the completed walkthrough:
   ```bash
   git add .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md
   git commit -m "docs(08): complete Phase 8 verification walkthrough"
   ```
   Include the Co-Authored-By trailer.

7. Update `.planning/REQUIREMENTS.md` §Traceability to flip STICKY-01..04 from `Pending` to `Complete` in a SEPARATE commit:
   ```bash
   # Edit .planning/REQUIREMENTS.md §Traceability rows:
   #   | STICKY-01 | Phase 8 | Pending | → | STICKY-01 | Phase 8 | Complete |
   #   (and STICKY-02, STICKY-03, STICKY-04 rows likewise)
   git add .planning/REQUIREMENTS.md
   git commit -m "docs(08): mark STICKY-01..04 complete in Traceability"
   ```

If ANY walkthrough step fails:
- STOP and describe the failure precisely (which step, what was expected, what happened)
- Do NOT mark STICKY complete in REQUIREMENTS.md
- Create a gap-closure plan via `/gsd-plan-phase 8 --gaps` to address the regression

If all pass:
- Deploy to production: `npx vercel --prod`
- Update STATE.md `last_activity` to today's date and record Phase 8 completion in the Latest Ship section
  </how-to-verify>
  <resume-signal>Type "approved" (walkthrough complete, all 4 criteria PASS) or describe issues for gap closure</resume-signal>
  <verify>
    <automated>grep -cE "PASS" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md | awk '{exit ($1>=4?0:1)}' && grep -c "PENDING" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md | awk '{exit ($1==0?0:1)}' && grep -cE "\\| STICKY-0[1-4] \\| Phase 8 \\| Complete \\|" .planning/REQUIREMENTS.md | awk '{exit ($1==4?0:1)}'</automated>
  </verify>
  <done>User has completed Section B walkthrough B.1 through B.7 on a deployed preview (or localhost), all four ROADMAP.md success criteria marked PASS in 08-VERIFICATION.md, REQUIREMENTS.md §Traceability flipped for STICKY-01..04 to Complete, and two follow-up commits recorded (walkthrough-complete + traceability-complete). Production deploy (`npx vercel --prod`) is also complete if all checks passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in Plan 05. The verification document itself is a Markdown file; the walkthrough examines the deployed application via admin-authenticated session (normal Auth.js + Google OAuth flow, unchanged from v1.0).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Spoofing | Admin session during walkthrough | accept | Uses existing admin_users allowlist; unchanged auth path. |
| T-08-05-02 | Tampering | Verification document content | accept | Markdown artifact; git-tracked with signed commits. |
| T-08-05-03 | Repudiation | Walkthrough attribution | mitigate | Section C captures user email + date; commit attribution via Co-Authored-By. |
| T-08-05-04 | Information Disclosure | N/A | N/A | Walkthrough views existing admin UI; no net-new data exposure. |
| T-08-05-05 | Denial of Service | N/A | N/A | Visual verification only. |
| T-08-05-06 | Elevation of Privilege | N/A | N/A | Uses existing admin session. |

LOW severity overall — UI refactor with no net-new input surfaces (per 08-CONTEXT.md threat conclusion).
</threat_model>

<verification>
After Task 1 commits + Task 2 (human checkpoint) completes:

1. `test -f .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` exits 0
2. `grep -cE "\\{PASS\\}|PASS" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` returns at least 4 (one per success criterion row after user walkthrough)
3. No `{PENDING}` placeholders remain after the user's follow-up commit: `grep -c "PENDING" .planning/phases/08-vpol-02-sticky-tutor-legend/08-VERIFICATION.md` returns `0`
4. REQUIREMENTS.md §Traceability reflects STICKY-01..04 `Complete`: `grep -cE "\\| STICKY-0[1-4] \\| Phase 8 \\| Complete \\|" .planning/REQUIREMENTS.md` returns `4`
5. Commit graph shows the full Phase 8 story (6-8 commits): audit → amendment → Z_INDEX constant → WeekOverview refactor → CalendarGrid normalize → verification scaffold → verification complete → Traceability update
</verification>

<success_criteria>
- `08-VERIFICATION.md` exists with full Section A automated evidence + Section B walkthrough results + Section C user sign-off
- All 4 ROADMAP.md Phase 8 success criteria marked PASS
- REQUIREMENTS.md §Traceability for STICKY-01..04 updated to Complete
- All 141 baseline tests still pass
- Deployed to production if all checks pass
- STATE.md updated with Phase 8 completion
</success_criteria>

<output>
After completion (both the scaffold commit AND the user's walkthrough commits), create `.planning/phases/08-vpol-02-sticky-tutor-legend/08-05-SUMMARY.md` documenting:
- All Phase 8 commit SHAs in order (audit → amendment → constant → WeekOverview → CalendarGrid → verification scaffold → verification complete → Traceability update → production deploy)
- Final verification document line count
- Summary table of all 4 success criteria with PASS/FAIL
- Test count (baseline 141 preserved)
- Production URL where sticky legend is live
- Any known issues carried forward to v1.2
</output>
</content>
</invoke>
