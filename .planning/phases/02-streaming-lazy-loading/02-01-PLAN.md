---
phase: 02-streaming-lazy-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/skeletons/search-skeleton.tsx
  - src/components/skeletons/form-skeleton.tsx
  - src/components/skeletons/calendar-skeleton.tsx
  - src/app/(app)/search/loading.tsx
autonomous: true
requirements: [PERF-05, INFRA-01]

must_haves:
  truths:
    - "Navigating to /search shows a shimmer skeleton immediately (no blank white screen)"
    - "Skeleton mirrors the side-by-side panel layout with form field shapes and calendar grid outline"
    - "Skeleton uses animate-pulse shimmer animation"
    - "Three reusable skeleton components exist for form, calendar, and full-page"
  artifacts:
    - path: "src/components/skeletons/search-skeleton.tsx"
      provides: "Full-page skeleton matching side-by-side layout"
      exports: ["SearchSkeleton"]
    - path: "src/components/skeletons/form-skeleton.tsx"
      provides: "Search form skeleton with 3-column grid placeholder"
      exports: ["FormSkeleton"]
    - path: "src/components/skeletons/calendar-skeleton.tsx"
      provides: "Calendar grid skeleton with time axis and day columns"
      exports: ["CalendarSkeleton"]
    - path: "src/app/(app)/search/loading.tsx"
      provides: "Route-level loading skeleton shown during navigation"
      exports: ["default"]
  key_links:
    - from: "src/app/(app)/search/loading.tsx"
      to: "src/components/skeletons/search-skeleton.tsx"
      via: "import SearchSkeleton"
      pattern: "import.*SearchSkeleton.*from.*skeletons/search-skeleton"
---

<objective>
Create shimmer skeleton components and route-level loading.tsx for the search page.

Purpose: Provide instant visual feedback during page navigation (INFRA-01) and reusable skeleton components for Suspense fallbacks in later plans (PERF-05). These must be high-fidelity skeletons matching the exact layout of the real page (D-04) with subtle pulse/shimmer animation (D-03).

Output: Four new files -- three skeleton components and one loading.tsx route file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-streaming-lazy-loading/02-CONTEXT.md
@.planning/phases/02-streaming-lazy-loading/02-RESEARCH.md

@src/components/search/search-workspace.tsx
@src/components/search/search-form.tsx
@src/components/compare/compare-panel.tsx

<interfaces>
<!-- Key layout structure the skeletons must mirror -->

From src/components/search/search-workspace.tsx:
```typescript
// The real layout: flex row, two 50% panels
<div className="flex-1 flex gap-3 overflow-hidden min-h-0">
  <div className="w-1/2 flex flex-col overflow-hidden min-w-0 border-r border-border/50 pr-3">
    {/* SearchForm + SearchResults */}
  </div>
  <div className="w-1/2 flex flex-col overflow-hidden min-w-0 pl-1">
    {/* ComparePanel */}
  </div>
</div>
```

From src/components/search/search-form.tsx:
```typescript
// Form layout: 3-column grids for dropdowns, then action button
export interface SearchFormProps {
  filterOptions: FilterOptions | null;
  onSearchResponse: (response: RangeSearchResponse, context: SearchContext) => void;
  onError: (error: string | null) => void;
}
```

From src/components/compare/compare-panel.tsx:
```typescript
// Compare panel: tutor chips row, week picker, day tabs, calendar grid
export interface ComparePanelProps {
  compare: UseCompareReturn;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton components (FormSkeleton, CalendarSkeleton, SearchSkeleton)</name>
  <files>
    src/components/skeletons/search-skeleton.tsx
    src/components/skeletons/form-skeleton.tsx
    src/components/skeletons/calendar-skeleton.tsx
  </files>
  <read_first>
    src/components/search/search-workspace.tsx
    src/components/search/search-form.tsx
    src/components/compare/compare-panel.tsx
  </read_first>
  <action>
Create three skeleton component files in `src/components/skeletons/`. All skeletons use Tailwind `animate-pulse` on `bg-muted` blocks for shimmer effect (per D-03). No `"use client"` directive needed -- these are pure presentational components with no hooks or interactivity.

**src/components/skeletons/form-skeleton.tsx:**
Export `FormSkeleton`. Mirror the SearchForm layout:
- A 3-column grid (`grid grid-cols-3 gap-2`) with three `h-8 rounded-md bg-muted animate-pulse` blocks (representing subject/curriculum/level dropdowns)
- A second 3-column grid row with three more `h-8` blocks (day/time/duration fields)
- A mode toggle row: two `h-7 w-16 rounded-md bg-muted animate-pulse` blocks side by side
- A search button: `h-8 w-24 rounded-md bg-muted animate-pulse`

**src/components/skeletons/calendar-skeleton.tsx:**
Export `CalendarSkeleton`. Mirror the WeekOverview calendar grid:
- A top row of 7 day header placeholders: `h-6 w-12 rounded bg-muted animate-pulse` in a flex row with gap-2
- Below, a time grid area: 14 rows (7AM-9PM) each containing a time label placeholder (`h-4 w-8 bg-muted animate-pulse`) on the left and a full-width `h-10 bg-muted/30 animate-pulse rounded` on the right
- Wrap in a div with `space-y-1`

**src/components/skeletons/search-skeleton.tsx:**
Export `SearchSkeleton`. High-fidelity full-page skeleton matching SearchWorkspace layout (per D-04):
- Outer container: `className="flex-1 flex gap-3 overflow-hidden min-h-0"` (matches search-workspace.tsx exactly)
- Left panel (50%): `className="w-1/2 flex flex-col overflow-hidden min-w-0 border-r border-border/50 pr-3"`
  - Render `<FormSkeleton />` at top
  - Below form: 6 result row placeholders (`h-10 rounded-md bg-muted/50 animate-pulse` in `space-y-1.5 mt-2`)
- Right panel (50%): `className="w-1/2 flex flex-col overflow-hidden min-w-0 pl-1"`
  - Tutor selector row: `h-7 w-32 rounded-md bg-muted animate-pulse` + `h-4 w-8 rounded bg-muted animate-pulse ml-auto`
  - Empty compare placeholder centered: `h-4 w-28 rounded bg-muted animate-pulse mx-auto` + `h-3 w-48 rounded bg-muted animate-pulse mx-auto` in a centered flex column
  </action>
  <verify>
    <automated>ls -la src/components/skeletons/search-skeleton.tsx src/components/skeletons/form-skeleton.tsx src/components/skeletons/calendar-skeleton.tsx && grep -l "export function" src/components/skeletons/*.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - src/components/skeletons/form-skeleton.tsx contains `export function FormSkeleton`
    - src/components/skeletons/form-skeleton.tsx contains `animate-pulse`
    - src/components/skeletons/form-skeleton.tsx contains `grid-cols-3`
    - src/components/skeletons/calendar-skeleton.tsx contains `export function CalendarSkeleton`
    - src/components/skeletons/calendar-skeleton.tsx contains `animate-pulse`
    - src/components/skeletons/search-skeleton.tsx contains `export function SearchSkeleton`
    - src/components/skeletons/search-skeleton.tsx contains `w-1/2`
    - src/components/skeletons/search-skeleton.tsx contains `border-r border-border/50`
    - src/components/skeletons/search-skeleton.tsx imports FormSkeleton from `./form-skeleton`
    - No file contains `"use client"` directive
  </acceptance_criteria>
  <done>Three skeleton components exist with shimmer animation, matching real layout dimensions</done>
</task>

<task type="auto">
  <name>Task 2: Create route-level loading.tsx</name>
  <files>src/app/(app)/search/loading.tsx</files>
  <read_first>
    src/components/skeletons/search-skeleton.tsx
    src/app/(app)/search/page.tsx
  </read_first>
  <action>
Create `src/app/(app)/search/loading.tsx` that exports a default function `Loading` which renders `<SearchSkeleton />`. This is the Next.js file convention that wraps the page in a Suspense boundary automatically during navigation (per D-05 route-level skeleton).

```typescript
import { SearchSkeleton } from "@/components/skeletons/search-skeleton";

export default function Loading() {
  return <SearchSkeleton />;
}
```

This file must NOT have `"use client"` -- it is a Server Component that renders the skeleton during navigation transitions.
  </action>
  <verify>
    <automated>cat src/app/\(app\)/search/loading.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/search/loading.tsx exists
    - File contains `export default function Loading`
    - File contains `import { SearchSkeleton } from "@/components/skeletons/search-skeleton"`
    - File contains `<SearchSkeleton />`
    - File does NOT contain `"use client"`
    - TypeScript compiles with zero errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>Route-level loading.tsx shows SearchSkeleton during navigation to /search</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Skeleton components are pure presentational with no data access or user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | Skeleton components | accept | Skeletons render static placeholder shapes with no real data. No PII or business data exposed. |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npm test` passes (82 tests, no regressions)
3. `loading.tsx` exists at correct path
4. All three skeleton files export named components
</verification>

<success_criteria>
- loading.tsx renders SearchSkeleton on navigation to /search
- Three skeleton components (FormSkeleton, CalendarSkeleton, SearchSkeleton) exist with shimmer animation
- TypeScript compiles, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-lazy-loading/02-01-SUMMARY.md`
</output>
