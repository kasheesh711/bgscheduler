---
phase: quick-260907-qg5
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0078_admin_access_controls.sql
  - drizzle/meta/_journal.json
  - src/lib/auth-access.ts
  - src/lib/auth.ts
  - src/lib/auth-edge.ts
  - src/types/next-auth.d.ts
  - src/middleware.ts
  - src/proxy.ts
  - src/lib/admin-users/access.ts
  - src/lib/admin-users/data.ts
  - src/lib/admin-users/types.ts
  - src/app/api/admin/users/route.ts
  - src/app/(app)/admin/users/page.tsx
  - src/components/admin-users/manage-access.tsx
  - src/components/layout/app-nav.tsx
  - src/app/(app)/layout.tsx
  - src/lib/preview-policy.ts
  - src/components/layout/preview-banner.tsx
  - src/lib/admin-users/__tests__/access.test.ts
  - src/lib/admin-users/__tests__/data.integration.test.ts
  - src/app/api/admin/users/__tests__/route.test.ts
  - src/lib/__tests__/auth-access.test.ts
  - src/lib/auth/__tests__/signin-callback.test.ts
  - src/lib/__tests__/preview-policy.test.ts
  - src/__tests__/middleware.test.ts
  - .github/CODEOWNERS
  - .github/workflows/ci.yml
  - package.json
  - package-lock.json
  - scripts/check-sales-dashboard-scope.mjs
  - docs/README.md
  - docs/operations/aoeng-windows-setup.md
  - docs/operations/owner-access-runbook.md
  - docs/operations/auth-and-access.md
  - docs/reference/env.md
autonomous: true
requirements: [AOENG-ACCESS, OWNER-REVOCATION, PREVIEW-ISOLATION, INDEPENDENT-PUBLISHING, WINDOWS-SETUP]
must_haves:
  truths:
    - "Aoeng and Waritpariya can view Unearned Revenue; Kevin remains its sole access manager."
    - "Kevin can disable website access immediately; old sessions remain invalid after re-enabling."
    - "Aoeng can publish ordinary changes after required checks; protected changes require Kevin's approval."
    - "Aoeng's protected preview uses isolated data and cannot send real messages or mutate production integrations."
    - "Aoeng can follow the Windows guide using Claude Code without production secrets or a local database."
  artifacts:
    - path: src/app/api/admin/users/route.ts
      provides: Owner-protected versioned access management
      exports: [GET, PATCH]
    - path: src/app/(app)/admin/users/page.tsx
      provides: Owner-only Manage Access page
    - path: src/proxy.ts
      provides: Current-admin validation on authenticated requests
    - path: .github/CODEOWNERS
      provides: Kevin-only review requirements for protected controls
    - path: docs/operations/aoeng-windows-setup.md
      provides: Beginner Windows and Claude Code publishing guide
    - path: docs/operations/owner-access-runbook.md
      provides: Independent website, GitHub, and preview revocation procedures
  key_links:
    - from: src/app/api/admin/users/route.ts
      to: src/lib/admin-users/data.ts
      via: Owner guard followed by atomic version-checked update and audit append
    - from: src/lib/auth.ts
      to: src/lib/db/schema.ts
      via: Fresh disabled-status and access-version validation for admin sessions
    - from: .github/CODEOWNERS
      to: GitHub main branch protection
      via: Required code-owner review plus five required CI checks
---

<objective>
Implement the user's accepted Aoeng collaboration plan completely, preserving Kevin's ownership and revocation controls. Deliver application access controls, independent publishing rules, an isolated working preview, two audited dashboard grants, and a beginner Windows guide.
Purpose: Let Aoeng safely develop and ship everyday improvements while Kevin controls sensitive changes and can remove access.
Output: Tested implementation, documentation, verified live access/settings, and a factual completion record.
</objective>

<execution_context>
@/Users/kevinhsieh/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@docs/README.md
@docs/operations/auth-and-access.md
@src/lib/auth-access.ts
@src/lib/unearned-revenue/access.ts
@src/middleware.ts
@.github/workflows/ci.yml
@node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md

The root has already initialized this quick workflow. Shared working tree: preserve other agents' changes. This plan intentionally groups the accepted work into the three requested execution responsibilities; implementation can be delegated within those responsibilities. No additional product decision or approval is needed for authorized reversible setup.

Existing contracts: `resolveUserAccess(email, db): Promise<UserAccess | null>` resolves admin before admissions/teacher; `UserAccess` contains role and allowedPages. `replaceUnearnedRevenueCapabilities({ actorEmail, targetEmail, capabilities, expectedVersion, note?, db? })` performs audited capability replacement under a transaction. `listUnearnedRevenueAccessRows(db)` returns the required current version. Auth.js server and edge configurations currently retain admin claims in JWTs. Next.js 16 calls the request interception file `proxy.ts`; keep independent server authorization because Proxy alone is insufficient.

Decision coverage: D-01 dashboard viewer grants and Kevin's manager role → task 3; D-02 owner management, immediate revocation and fresh-login re-enable → task 1; D-03 ordinary publishing with protected reviews → tasks 2–3; D-04 persistent protected preview branch and isolated database/secrets/OAuth → tasks 1 and 3; D-05 English Windows/Claude/GSD guide and owner runbook → task 2; D-06 preserve unrelated access, repository visibility and ownership, no broad Vercel membership → all tasks. All decisions must be delivered in full; disclose any external setup gate instead of marking it complete.

Root's live inspection found approximately 128 GB of mostly old credit-control snapshots. Provision the preview through existing PostgreSQL CREATEDB/CREATEROLE authority: use a separate database and restricted role, copying active snapshots and representative feature data with consistent references, without dumping historical data. No Neon platform API is required. Verify that the preview role cannot read or write production tables or create production objects. Preserve unrelated cluster-level CONNECT grants; connection alone grants no data access.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement owner controls, immediate session revocation, and preview-safe authentication</name>
  <files>src/lib/db/schema.ts, drizzle/0078_admin_access_controls.sql, drizzle/meta/_journal.json, src/lib/auth-access.ts, src/lib/auth.ts, src/lib/auth-edge.ts, src/types/next-auth.d.ts, src/middleware.ts, src/proxy.ts, src/lib/admin-users/access.ts, src/lib/admin-users/data.ts, src/lib/admin-users/types.ts, src/app/api/admin/users/route.ts, src/app/(app)/admin/users/page.tsx, src/components/admin-users/manage-access.tsx, src/components/layout/app-nav.tsx, src/app/(app)/layout.tsx, src/lib/preview-policy.ts, src/components/layout/preview-banner.tsx, src/lib/admin-users/__tests__/access.test.ts, src/lib/admin-users/__tests__/data.integration.test.ts, src/app/api/admin/users/__tests__/route.test.ts, src/lib/__tests__/auth-access.test.ts, src/lib/auth/__tests__/signin-callback.test.ts, src/lib/__tests__/preview-policy.test.ts, src/__tests__/middleware.test.ts</files>
  <behavior>Owner-only GET/PATCH succeeds for an active configured super-admin; nonowners fail. Disabled/deleted admins and stale session versions fail on the next page/API/session-based manual-sync request; re-enabling never revives prior tokens. Concurrent edits conflict without partial audit writes; owner self-lockout is rejected. Preview Google authorization requests identity scopes only and persists no Sheets/Drive tokens. Existing maintenance, public routes, non-admin roles and cron-secret flows retain their behavior.</behavior>
  <action>Per D-02/D-04/D-06, define admin access types first, then add enabled-by-default disabled status and an integer access version to admin_users, plus an append-only audit table. Use the next unoccupied additive migration number and update Drizzle metadata consistently. Add normalized SUPER_ADMIN_EMAILS parsing; configure Kevin externally in task 3 and require both active admin status and owner designation. GET /api/admin/users returns current admins with disabled/version state; PATCH accepts {email, disabled, expectedVersion}, validates its body, and atomically checks current state, increments version and appends actor/before/after audit data. Return 409 on stale writes, 404 for unknown targets and 403/401 for denied actors. Protect every configured owner from disabling. Render the owner-only Manage Access page with controls, pending/error/conflict states and owner navigation. Keep existing feature grants intact. Make auth() and the Node Proxy freshly validate admin status/version; never allow a disabled admin to fall through into teacher/admissions access, and never refresh a stale token's version silently. Explicitly handle pre-migration tokens so previously revoked sessions cannot regain access; force fresh login where a safe version is absent. Migrate middleware to proxy while retaining matcher, maintenance and public-route behavior and updating imports/tests. Validate session-based cron fallbacks through the same server auth check. Database errors deny protected access. Add preview environment policy, identity-only OAuth configuration, no integration-token persistence, and a visible preview banner. Use existing cacheComponents/Suspense and shadcn conventions. Root owns live migrations and env configuration.</action>
  <verify><automated>npx vitest run --project unit src/lib/admin-users src/app/api/admin/users src/lib/__tests__/auth-access.test.ts src/lib/auth/__tests__/signin-callback.test.ts src/lib/__tests__/preview-policy.test.ts src/__tests__/middleware.test.ts</automated></verify>
  <done>Owner API/UI, additive schema, fresh authorization and preview-safe OAuth are implemented with regression tests; integration tests prove atomic state/audit updates and stale-write rejection. No production settings or data were changed by this task.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare scoped publishing controls and Aoeng's Windows handbook</name>
  <files>.github/CODEOWNERS, .github/workflows/ci.yml, package.json, package-lock.json, scripts/check-sales-dashboard-scope.mjs, docs/README.md, docs/operations/aoeng-windows-setup.md, docs/operations/owner-access-runbook.md, docs/operations/auth-and-access.md, docs/reference/env.md</files>
  <action>Per D-03/D-05/D-06, replace blanket ownership with @kasheesh711 ownership of authentication/access/owner-management code, schema/migrations, dependency manifests, hosting/cron config, CI/release guards, agent instructions and CODEOWNERS itself; include newly introduced controls from task 1. Remove Sales Dashboard-only workflow/script restrictions. Set all five existing CI jobs to Node 24 and align package runtime metadata without changing unrelated dependencies. Retain exact required check names: lint, typecheck, unit-tests, build, release-guards. Write an English guide addressed to Aoeng with copyable PowerShell commands and expected results: Git for Windows, Node 24, GitHub CLI and native Claude Code installation; account login; repository clone/npm ci; CLAUDE.md and required GSD setup; persistent codex/aoeng-preview branch updates from main, preview, PR and merge-commit publishing; reusable Claude prompts; required-review explanations and recovery steps. Use official installation docs and actual verified preview URL/config from root. No local server/database or production credentials required. Explain branch retention, conflicts, failed checks, preview login, Kevin-required changes and rollbacks in plain language. Write the owner runbook covering distinct website/GitHub/preview revocation, owner recovery, rollback, env ownership and safe preview provisioning. Link both guides from the handbook and update auth/env references. Keep invitations and first-login status factual; no secrets in documentation.</action>
  <verify><automated>git diff --check &amp;&amp; npm run guard:production-route-surface &amp;&amp; npm run lint</automated></verify>
  <done>Ordinary feature files no longer require blanket owner review, sensitive controls remain owned by Kevin, CI runs Node 24, and the guides describe a complete Windows/Claude workflow plus all three revocation surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Apply audited grants, provision the isolated preview, and roll out verified publishing access</name>
  <files>.planning/quick/260907-qg5-implement-aoeng-editing-access-owner-con/260907-qg5-SUMMARY.md</files>
  <action>Root owns all live operations, per D-01/D-03/D-04/D-06. Inspect current state before mutation and preserve unrelated permissions. Grant viewer to aoengnatchasmith@gmail.com and k.waritpariya@gmail.com using the existing audited capability API/service and fresh expected versions, with Kevin as actor; preserve Kevin's viewer/access_manager and verify he is the sole manager. Preserve aoengnatchasmith-spec's GitHub Write permission, Kevin's ownership and repository visibility. Create/retain codex/aoeng-preview and a stable protected deployment; share it directly with Aoeng's email without broad team membership. Prepare a distinct preview database from a production snapshot and remove copied OAuth/integration credentials and public capability links; preserve representative dashboard data. Prove distinct database credentials and use only separate preview AUTH_SECRET, cron secret and Google OAuth client. Set its callback to the verified stable preview URL; strip production integration credentials and disable all messaging/external-write integrations. Verify runtime preview guards and absence of credential fallback, including database-stored tokens. Apply the reviewed additive migration before deploying task 1 and set SUPER_ADMIN_EMAILS=kevhsh7@gmail.com. Merge protected CODEOWNERS/config changes and establish preview isolation before relaxing main protection. Keep PRs mandatory; required approving review count 0, required code-owner review on, last-push approval off, stale-review dismissal on, required five CI checks, no force pushes/deletions. Preserve the preview branch after merge and use merge commits. Verify repository rule/ruleset interactions do not bypass protected approvals. Run release validation, confirm production/preview URLs and login/authorization behavior, and perform preview-to-production smoke testing without real sends or production-data mutations. If an actual external authentication gate prevents OAuth or sharing setup, finish independently actionable work and record the exact remaining gate rather than claiming completion. Record invitation acceptance/first login separately from automated checks; do not send messages beyond invitations explicitly authorized by this setup.</action>
  <verify><automated>npm run typecheck &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run guard:production-route-surface &amp;&amp; git diff --check</automated></verify>
  <done>Both viewer grants and audit records are verified; Kevin retains all owner controls. Ordinary/protected PR behavior is proven against live settings. A working isolated protected preview is available to Aoeng, production has the validated release, and completion evidence distinguishes completed setup from any user-only login/invitation action.</done>
</task>

</tasks>

<threat_model>
| Boundary | Threat | Disposition | Mitigation |
|---|---|---|---|
| JWT → protected application | T-QG5-01 S/E: stale sessions retain removed access | mitigate | Fresh admin lookup and version comparison in Proxy and server auth; disabled users cannot fall through roles; re-enable increments version. |
| Owner API → Postgres | T-QG5-02 T/R/E: forged actor, lost update or missing audit | mitigate | Active-owner guard, validated body, transactional expected-version update and append-only audit. |
| Preview → production/services | T-QG5-03 I/T: copied secrets enable live writes or disclosure | mitigate | Separate database/credentials/OAuth, scrub persisted tokens/capability links, remove production env secrets, disable integrations and test preview guards. |
| Contributor → main | T-QG5-04 T/E: bypass owner controls | mitigate | Protected CODEOWNERS/CI/agent/security files, mandatory PR/checks, owner review on those files and no force push/deletion. Trusted ordinary code publishing remains the user's accepted access model. |
</threat_model>

<verification>
Tasks 1 and 2 may progress independently with agreed file ownership; task 2 incorporates task 1's final protected paths. Task 3 readiness inspection can run immediately, but migration/deployment and branch-rule relaxation follow the stated safety order. Verify database failure denial, owner self-lockout, active-session disable/re-enable, stale edits, untouched unrelated roles/public/cron paths, viewer access without access management, isolated preview data and zero real sends. Run full required CI and database integration checks before release. Validate Windows commands against official vendor instructions and available Windows execution; report any unexecuted OS-specific check accurately.
</verification>

<success_criteria>
All six accepted decision groups are implemented in full, tested, and either verified live or explicitly recorded as blocked by a concrete external prerequisite. Completion must not conflate source readiness with live setup, or sending an invitation with acceptance.
</success_criteria>

<output>
After execution, create `.planning/quick/260907-qg5-implement-aoeng-editing-access-owner-con/260907-qg5-SUMMARY.md` with changed files/commits, checks, verified live configuration, preview and guide links, and any remaining invitation/login prerequisite. Root owns commits and STATE updates.
</output>
