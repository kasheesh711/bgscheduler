# Open Questions

This document is the consolidated list of questions that **only a human can answer** — design intent, production state, ownership boundaries, and "is this dead or just dormant?" calls that cannot be settled by reading the code alone.

It was produced by an automated documentation pass that re-documented the whole repository feature-by-feature. Every item was gathered by a documentation or verification agent and is **stated against the working tree at HEAD + uncommitted WIP**. Where a collected note had been overtaken by the actual tree state, the reconciled, code-verified status is recorded rather than the original claim. Verification-only notes (agents confirming that a doc claim was accurate) were folded into the genuine question they bear on, or dropped.

Each item is phrased to be specific and actionable: it names the file(s)/line(s) involved and states the decision a human needs to make. Items are grouped by theme.

---

## Branch & merge state

- **Working branch is `main`.** `git rev-parse --abbrev-ref HEAD` reports `main` and `git rev-list --count main..HEAD` is `0`. (An earlier revision of this file asserted the docs were generated on `docs/full-redocumentation` ~16 commits ahead of `main`; that is no longer the case at this revision.) Confirm the intended branch/merge path for the documentation refresh.
- **Progress Tests landed via PRs #7–#11** (latest commit `a530d10`), post-dating the GSD/prose sources. `.planning/codebase/CONVENTIONS.md` and `AGENTS.md` still carry stale absolute counts (e.g. "78 tables", "21 pgEnum declarations", "130 test files"); the handbook deliberately avoids restating them. If the GSD sources are refreshed, re-confirm those numbers.

---

## Maturity & lifecycle

The maturity-badge map handed to the documentation run was empty (`{}`), so every `Status:` badge on the feature docs was left as written and could not be cross-checked against an authoritative source. There are no `@deprecated`/`@experimental` markers anywhere in code. Confirm the intended status for each feature — especially where code state and badge sit oddly together:

- **AI scheduler — "experimental" but live for all admins.** No feature flag gates the AI-scheduler routes or UI; the feature is reachable by every authenticated admin. Confirm the intended rollout state.
- **Proposals — "experimental" but live for all admins, and the reference disagrees with the feature doc.** Routes and the in-`/search` UI are reachable by any authenticated admin with no flag. `docs/reference/api/proposals.md:3` labels the same endpoints **stable** while `docs/features/proposals.md:3` says **experimental**. Decide the correct label and whether holds should be flag-gated.
- **LINE integration — "stable" but the Wise write-path never writes.** Only the read/classify/review/send-reply loop is production-stable; `confirmLineWiseAction` (`src/lib/wise/operations.ts:71-94`) records a dry run only, even when `WISE_SESSION_OPERATIONS_VERIFIED=true`. Confirm whether "stable" is the right label given the write-path is permanently dry-run in V1, and whether a live cancel/reschedule call is on the roadmap.
- **Leave Requests — committed at HEAD.** ✅ Resolved (2026-06-05): the feature (API routes, `/leave-requests` page, workspace component, nav link, and the `15,45 * * * *` cron) is tracked and the tree is clean; schema lives at `schema.ts:1325-1466`. The framing is now **"live (preview-only Wise cancel)"** across AGENTS.md, README.md, the feature doc, and the STACK/TESTING maps. `docs/reference/database/erd-leave-requests.md` now carries a "young / stabilizing — Wise-cancel is preview-only" banner instead of the old "IN PROGRESS / uncommitted" one, and its line citations were re-verified against `schema.ts:1325-1466` (the old `1185-1346` range is gone). Remaining genuine caveat: the Wise-cancel path is preview-only by design and the parser/normalization heuristics are young, so the 5 `leave_request*` table shapes and sync side effects are stabilizing rather than frozen.
- **Progress Tests — shipped and live.** ✅ Resolved (2026-06-05): `docs/features/progress-tests.md` (257 lines) and the dedicated `docs/reference/api/progress-tests.md` now exist alongside the 8 tables (`schema.ts:2022-2205`), `/progress-tests` page, 6 API routes, 2 crons, and cross-snapshot ledger. Remaining (genuine) open question: confirm the intended status badge, and that the on-disk `docs/features/student-promotions.md` and the new Progress Tests doc describe the same surface with an aligned cross-link (teacher access targets `/progress-tests` — `src/lib/auth-access.ts:19`, `src/lib/db/seed.ts:48`).

---

## Suspected dead or dormant code

Each of these has **no non-test caller** (or no observable effect) at HEAD. Decide keep-as-future-surface vs. remove:

### Search / compare
- **`parseSlotInput` free-text parser** (`src/lib/search/parser.ts`) — fully tested but the only non-test file referencing it is `parser.ts` itself; `search-form.tsx` posts structured dropdown params. Reserved for planned NL input / AI scheduler, or residual?
- **Legacy `POST /api/search`** (and its multi-slot intersection path) — the live UI only calls `/api/search/range` (`search-form.tsx`). Intended retention for external callers, or removable? Note the two paths derive the one-time weekday differently (`new Date(slot.date).getDay()` at `engine.ts:68`, local-TZ-dependent, vs `weekdayForIsoDate` at `range-search.ts:142`), which can disagree near the Asia/Bangkok day boundary — so this is not a pure no-op duplicate. Is the legacy one-time path still exercised?
- **`compare` request `mode` field is vestigial.** `compareRequestSchema` requires `mode: z.enum(["recurring","one_time"])` (`src/app/api/compare/route.ts:26`) and the client always sends `"recurring"` (`use-compare.ts:140`), but the route never destructures `mode` (it pulls `tutorGroupIds, dayOfWeek, date, weekStart, fetchOnly` at `route.ts:133`) and `buildCompareTutor` has no `mode` parameter — it keys off `weekStart`/`dateRange`. Drop the field, or wire it up?
- **`CompareRequest` interface** (`types.ts:113-119`) omits `fetchOnly` and is unused outside its own definition; the route defines its own `compareRequestSchema`. Dead, or meant to stay in sync with the route schema?
- **Legacy `/compare` redirect** (`compare/page.tsx`) — retire entirely, or keep indefinitely for old-bookmark compatibility?

### AI scheduler
- **Legacy single-shot parser** — `parseSchedulingRequestWithOpenAi` (`scheduler.ts:533`) and `normalizeAiSchedulerModelParse` (`scheduler.ts:329`) have no non-test callers and appear superseded by the multi-turn `scheduler-conversation.ts`. **But** the request schema `aiSchedulerRequestSchema` (`scheduler.ts:141`) and the `AiSchedulerResponse` family (`scheduler.ts:72`) are still the live wire format of `POST /api/search/assistant` and its `search/ai-scheduler-panel.tsx` client. Confirm whether `/api/search/assistant` is being kept (preserve schema/types, remove only the parse functions) or migrated to the conversational shapes (then both can go). This single-shot path is also a likely gap in `docs/features/ai-scheduler.md`, which presents the conversational route as the only live scheduler runtime — consider cross-linking it.
- **Shadow-eval wiring** — `aiSchedulerShadowModel()` (`scheduler.ts:465-467`, env `OPENAI_SCHEDULER_SHADOW_MODEL`) is defined but the live turn doesn't appear to run a shadow comparison. Is shadow eval only exercised by `scripts/`, or is production wiring still pending?

### Payroll
- **`may-reconciliation.ts`** (legacy Aggregate/Long sheet parsers + `buildMayPayrollReconciliationReport`) has zero repo-wide callers outside its own test. One-off cutover aid, dead code, or pending UI wiring?
- **`parsePayRateRows`** (`rate-card.ts:98-160`) is referenced only by tests. Rate tables are *created and seeded* by migration `0037_payroll_rate_cards.sql` (active `PayRate May 2026` version + rules at SQL `:40`/`:181`, registered at `_journal.json:247`), so expected-rate checks DO fire after `npm run db:migrate` — but refreshing rates after the initial seed needs a new migration or manual SQL. Is a runtime rate-card importer planned, and has 0037 actually been applied to production Neon?

### Room capacity
- **The four forecast model tables** (`room_capacity_model_runs` / `_forecast_drivers` / `_demand_mix` / `_package_mix`) have no in-repo writer — `grep` finds zero `insert()` into them anywhere in `src/` (only reads in `data.ts:296-355`), so `getRoomCapacityForecast` always returns `model.status='missing'` until populated out-of-band. Where is the model-run importer meant to live?
- **`buildPackageMixFromSales`** (`src/lib/room-capacity/package-mix.ts:29`) has no non-test caller — staged helper for the missing importer, or dead?
- **`getRoomCapacityMonth` / `getRoomCapacityForecast`** are implemented, tested, and exposed via authenticated routes, but no page consumes them (`/room-capacity` is utilization-only; the strings `room-capacity/month` and `room-capacity/forecast` appear in `src/` only in the route handlers and their test). Is a capacity/forecast page planned, or are these intentionally API-only?

### Credit control
- **Snapshot-diff / history apparatus** (`buildSummaryDeltas`, `updateHistory`, `buildWeeklyBuckets`, `buildSnapshotForPersistence`, `HISTORY_LIMIT` in `analytics.ts:30-68,558-670`) is inert: `getCreditControlPayload` (`service.ts:83-88`) always passes `{ lastSnapshot: null, history: [] }` and discards the returned `snapshotState`, so deltas are always `null` and `statusChange` is always `"new"`. Intended-but-unfinished, or removable?
- **Sheet-shaped intermediate** — `loadCreditControlSources` reshapes Postgres rows into synthetic Google-Sheets-shaped objects for the legacy projection layer. Intentional (to preserve the legacy engine), or should it become a direct row→model path? Confirm no live sheet ingestion remains.
- **Dual admin-ownership resolvers** — the Sheets majority-vote resolver `buildStudentAdminOwnershipMap` and `RemainingCredits` config are now only reachable from the seed script; the live read uses the Postgres sidecar with an empty Sheets map. Retain only for seeding, or residual dead code?
- **`Should_Credit` pending-deduction branch** — the DB→SheetSnapshot shim emits an empty string in that slot, so the live read always takes the duration fallback. Is the non-fallback branch dead under the current Wise pipeline?

### LINE
- **`test-data-cleanup.ts`** (`src/lib/line/test-data-cleanup.ts`) has no production caller — only its own test imports `buildLineTestDataCleanupTargets`, and `LINE_TEST_DATA_DELETE_CONFIRMATION` is used only inside that module. Dead code, integration-test fixture, or planned-but-unwired admin cleanup endpoint?
- **`LineWritebackStatus` values `confirmed` / `failed`** (`src/lib/line/data.ts:25-31`) are defined but never written by any code path in this build (the write-path is dry-run only). Reserved for the future live writeback, or vestigial?

### Sales dashboard
- **Write-Sheets path** — `updateGoogleSheetCell` / `getGoogleSheetsWriteAccessToken` are never invoked by Sales Dashboard code, and the UI only requests `spreadsheets.readonly`. Future work, shared with another domain (e.g. leave-request status writes), or dead here?
- **No-op status ternary** — `upsertSalesDashboardSource` sets `status: sourceMonth === currentBangkokMonthStart(now) ? "active" : "active"` (`data.ts:190`) — both branches yield `"active"`. Intentional placeholder, or leftover from a removed branch (new sources are always created `"active"` regardless of month)?

### Tutor profiles
- **`tutorContacts` (`tutor_contacts`)** is grouped under Tutor Profiles and shares `canonicalKey` keying but is not read/written by any in-scope path (lib, routes, workspace, import). Confirm its real owner/consumer, or whether it is orphaned here.
- **Hard-coded import filenames** — the importer stamps literal `'BeGifted Tutors-3.xlsx'` (`tutor-profile-import.ts:469,510`) and `'Availability.xlsx'` (`:485,490`) into internal notes / `verificationSource` regardless of the actual uploaded filename. Intended provenance label, or a bug?
- **Two-row-header branch** — `parseAvailabilityWorkbook` has a fixed-column-index branch (`tutor-profile-import.ts:346-356`) encoding a legacy spreadsheet layout. Still used, or dead-but-defensive?

### Classrooms
- **`ruleTrace` column** on `classroom_assignment_rows` is written server-side but has no reader in the client layer (the popover surfaces only `warnings`). Intentional write-only audit telemetry, or dead surface?
- **`failed` run status** is a declared-but-unreachable value of `classroomAssignmentRunStatusEnum` (`schema.ts:54-59`) — no code path sets it for a run. Keep or prune?

---

## Data, schema & env

- **The env table says "9 required", but the Zod schema disagrees.** `src/lib/env.ts` defines **7 strictly required** (`DATABASE_URL`, `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`, `AUTH_SECRET`, `WISE_USER_ID`, `WISE_API_KEY`, `CRON_SECRET`) + **2 defaulted** (`WISE_NAMESPACE`, `WISE_INSTITUTE_ID`, lines 10–11, which never block startup) + optional LINE vars. Recommend correcting the env table to "7 required, 2 defaulted, 3 optional" or footnoting it. The `LEAVE_REQUESTS_*` rows in that table are read directly from `process.env` elsewhere, not validated by `env.ts`.
- **The validated `env` object is imported by zero modules.** `grep -rln "@/lib/env"` over `src` returns no importer outside tests — it is a throw-on-import startup guard that never fires because nothing imports it. Decide whether to (a) import `env` at a real entrypoint so validation runs, or (b) treat `env.ts` as documentation-only and acknowledge the per-consumer `process.env` guards as the real contract. Consequence today: `DATABASE_URL` is validated as `z.string().url()` in the schema but the live consumer (`db/index.ts:6-9`) only checks truthiness; `WISE_USER_ID`/`WISE_API_KEY` are `.min(1)` in schema vs a non-null assertion at `wise/client.ts:161-162`. The URL-shape/min-length validation is effectively dead.
- **~9 runtime vars are read from `process.env` but appear in neither `env.ts` nor `.env.example`:** `WISE_SESSION_OPERATIONS_VERIFIED`, `WISE_SESSION_CREATE_VERIFIED`, `OPENAI_SCHEDULER_SHADOW_MODEL`, `OPENAI_SCHEDULER_REASONING_EFFORT`, `OPENAI_PROGRESS_TEST_MODEL`, `LINE_VALIDATION_LEAD_EMAILS`, `SALES_DASHBOARD_CONNECTED_EMAIL`, `VERCEL_PROJECT_PRODUCTION_URL`, `SCHEDULE_EMAIL_PUBLIC_BASE_URL`. Add them to `.env.example` so operators can discover them without grepping.
- **Two personal-email fallbacks live in non-test source.** `SCHEDULE_EMAIL_REPLY_TO` defaults to `'kevhsh7@gmail.com'` (`schedule-email.ts:607`) and `LINE_VALIDATION_LEAD_EMAILS` defaults to `['kevhsh7@gmail.com','kevinhsieh711@gmail.com']` (`link-validation.ts:119-122`). Confirm these are the intended permanent defaults, or change to all-admins/empty for shared/prod config.
- **OAuth scope divergence.** `src/lib/auth.ts:23` requests full `…/auth/spreadsheets` while `src/lib/auth-edge.ts:11` requests `spreadsheets.readonly`. The edge config never drives a sign-in (middleware only verifies), so its scope string is inert, but confirm which scope the production consent screen actually grants — and whether the write scope is needed at all given Sales Dashboard's write path is currently dormant.
- **Teacher Google tokens are persisted.** `auth.ts` stores the OAuth token via `storeGoogleOAuthTokenForUser` on every successful sign-in, **including teachers**, even though teachers only reach the read-only `/progress-tests` view. Confirm whether teacher tokens should be persisted at all.
- **Live admin count is data, not code.** The actual `admin_users` population (full admins where `allowed_pages IS NULL`, plus restricted rows) can only be confirmed by querying the target DB. `AGENTS.md` cites a fixed number not derivable from source — it may be stale.
- **`data_issues.severity`** (`schema.ts:1899`) is a 4-level enum, but no part of the `/data-health` payload reads or groups by it — issues are only bucketed by `type`. Is severity consumed anywhere (alerts, sorting), or is it dead metadata? It was not traced whether any sync write path sets severity below the `high` default (the orchestrator seeds issues from `identityIssues` at `orchestrator.ts:97`).
- **`completeness` and `sync` data-issue types** count toward `totalDataIssues`/`issuesByType` but are excluded from the `issueDetails` buckets (`dashboard.ts:258`), so a reviewer cannot see them on the surface. Intentional, or a gap?
- **`studentPromotionRuns.sourceSnapshotId`** FKs to `creditControlSnapshots` (`schema.ts:673`), a Credit Control table — not the core `snapshots` table. The three `studentPromotion*` tables sit in the Core line ranges but their snapshot lineage belongs to Credit Control. Confirm whether they should be grouped under Core or Credit Control in the database index.
- **`roomUtilizationSessions`** is physically in the Core section of `schema.ts` but is functionally owned by Room Capacity (no `snapshot_id`). Confirm whether it belongs in the Room Capacity ERD vs. Core to avoid duplication.
- **GIST overlap constraints** on the proposal tables are defined only in migration SQL (`0006`, scoped to `status IN ('pending','confirmed')`), not in the Drizzle table builder. Confirm schema and migrations are still in sync for these constraints.
- **`isToday` is hardcoded `true`** at the credit-control DB layer (`db.ts:200`) before being recomputed by `sanitizeStudentActionState`. Drop the misleading literal?
- **`ADMIN_OWNER_REGISTRY`** (6 named admins, credit control) is a separate hardcoded list from the auth allowlist. Should owners derive from a single source of truth?
- **Preferred rooms, TV-required tutors, and tutor contact emails/phones** are hardcoded in `rooms.ts` / `tutor-contacts.ts` rather than DB tables. Intentional, or should they become managed tables as staff turns over?

---

## Migrations & snapshot recovery

- **Migration directory has gaps and duplicate numbers.** 40 `.sql` files: `0009`/`0010`/`0025` are absent, and there are two `0038` (`0038_cynical_karma.sql` + `0038_data_health_cron_invocations.sql`) and two `0039` (`0039_overrated_krista_starr.sql` + `0039_line_link_validation_source_indexes.sql`) from a merge collision. The `drizzle/meta/_journal.json` is authoritative for apply order, but confirm both duplicate-numbered pairs were intended and apply cleanly in journal order before relying on this during manual recovery. (There is no `db:push` script.)
- **No first-class re-promote action for an older snapshot.** Recovery relies on re-running the sync or a manual `UPDATE snapshots SET active=…` flip, and pruning physically deletes snapshots beyond the most recent 30 (`SNAPSHOT_RETENTION_COUNT`). Should a guarded re-promote endpoint exist?
- **Failed-candidate rows are GC'd only by pruning.** The Neon-http driver has no transactions, so the orchestrator's bulk inserts (step 10) and stats (step 11) are non-transactional writes to the inactive candidate snapshot, made consistent only by (a) never being read until promote and (b) pruning cleaning up orphaned candidates on a later success. On failure, `orchestrator.ts:561-599` only marks the `sync_run` failed — there is no explicit cleanup of the partial candidate rows. Confirm reliance on pruning is the intended GC mechanism.
- **`student-promotions/` has no `*_sync_runs` single-flight guard table**, unlike every other subsystem. The cron fires annually (`5 17 30 6 *`). Confirm the run is idempotent / safe to re-trigger manually.

---

## Operations & crons

- **`vercel.json` declares 10 cron entries, contradicting the "7 crons / 5 sync-\* jobs" prose.** The live array is: `sync-wise` (`*/30`), `sync-sales-dashboard` (`10,40`), `sync-credit-control` (`20,50`), `sync-progress-tests` (`25,55`), `progress-tests/admin-digest` (`35 0`), `sync-wise-activity` (`5,35`), `sync-leave-requests` (`15,45`), `class-assignments/morning` (`45 23`), `class-assignments/admin-email` (`0,10,20,30 0`), `student-promotions/july-1` (`5 17 30 6`). `AGENTS.md`'s "7 crons / five `sync-*` at 5-min offsets" is stale; the architecture doc's single `*/30 sync-wise` assertion is correct. The full inventory belongs in `docs/reference/crons.md`. (Per-row `vercel.json` line citations elsewhere are brittle — they drift if the file is reordered.)
- **`sync-room-utilization` is the only `/api/internal/*` route absent from `vercel.json`.** It is intentionally manual (`cron-registry.ts` marks `room_utilization` `manualOnly:true`, `schedule:null`, `routeMethod:"POST"`) and is reachable from the Room Capacity dashboard and Data Health, so it is **not dead**. But it is **POST-only** while Vercel cron triggers via GET, so it could never run on a schedule even if added to `vercel.json` without also adding a GET handler. Is the lack of any automated refresh for room-utilization data intended, or should it become a scheduled GET cron like the other `sync-*` jobs?
- **`student-promotions/july-1` leaves no audit trail.** It is the only scheduled handler that does **not** wrap `withCronInvocationAudit` (it returns directly at `route.ts:33-43`), so its annual run writes no `cron_invocations` row and will not surface on the Data Health timeline. Compounding this: `pickJobRuns` (`dashboard.ts:137-236`) has no branch for it, so it falls into the room-utilization default branch and reads `roomUtilizationSessions` — semantically wrong, so the job will display as "unknown" forever. Wrap it for observability, or accept it as a deliberate one-off?
- **Three cron keys render as manual buttons but 404 when run.** `progress_tests`, `progress_tests_digest`, and `student_promotions_july_1` are valid `CronJobKey`s (so the route-level 404 check and dangerous-confirmation gate pass) but have **no executor branch** in `runDataHealthJob` — they fall through to `404 {error:'Unknown job'}` (`src/lib/data-health/run-job.ts:121`; branches exist only for `wise_snapshot`, `wise_activity`, `sales_dashboard`, `credit_control`, `leave_requests`, `classroom_morning`, `classroom_admin_email`, `room_utilization`). A confirmed dashboard run of `student_promotions_july_1` returns a confusing 404 instead of applying promotions. Deliberate (cron-only), or missing dispatch branches? (`pickJobRuns` likely needs matching explicit branches for the progress-tests and student-promotions run ledgers too — `progress_tests`/`progress_tests_digest` do at least write `cron_invocations` so direct cron proof would normally win, but `student_promotions_july_1` has no proof source of its own.)
- **`student-promotions/july-1` is a fixed-date cron that perpetually 409s after the window.** `5 17 30 6 *` = 17:05 UTC June 30 = 00:05 July 1 2026 Bangkok, and the handler hard-guards on `STUDENT_PROMOTION_TARGET_DATE='2026-07-01'` (`rules.ts:1`, `route.ts:27-31`). After July 1 2026 the entry keeps firing annually but every run returns HTTP 409. Remove it from `vercel.json` after the window passes to stop the perpetual 409s? Also confirm the apply service can never emit a "not found" message for the no-argument cron path, since `studentPromotionErrorResponse` includes a 404 branch for `/not found/i`.
- **Abandoned `running`-row clearing was not traced.** `AGENTS.md` claims abandoned `running` rows are auto-failed after a timeout, but that logic lives in the single-flight wrappers (`run-wise-sync.ts`, per-domain `run-sync-request.ts`), which were not read for the runbook — the Data Health dashboard only **detects/flags** stuck runs (`status.ts:203`), it does not clear them. Verify the exact timeout value and the row-clearing mechanism against those wrappers.
- **`getDataHealthDashboardPayload`** (the ~700-line multi-ledger aggregator in `dashboard.ts`) has no dedicated unit test — only the pure `evaluateCronJobStatus` and the route wrappers are tested. Intentional, or a coverage gap?
- **Wise reconciliation makes live outbound API calls on a GET path** (`fetchWiseFeesPaidTrends` / `fetchWiseReceiptTransactions`). Read-only toward Wise, but the GET is not purely DB-backed and can be slow/rate-limited — flagged as behavior consumers should know.
- **`wise_activity_events` is never pruned.** It persists full payload + raw JSON with no truncation/TTL (cross-snapshot, `schema.ts:265-266`). Confirm whether a retention policy is wanted. Relatedly, actor/role and other denormalized event columns are passthrough from Wise (`actorRole` taken verbatim at `sync.ts:99`) with no normalization/fail-closed handling — confirm no downstream consumer treats them as authoritative.
- **Wise-activity cron ingests only a rolling 3-day window** (`CRON_LOOKBACK_DAYS=3`, `sync.ts:9`); closing historical reconciliation gaps requires the manual "Backfill selected range". Is periodic deeper backfill desired?
- **Morning classroom cron runs at `45 23 * * *` UTC (~06:45 Bangkok).** Confirm the intended local trigger time hasn't drifted from this UTC value.
- **Prior classroom runs and `classroom_automation_events`** are persisted but have no read API or UI. Is automation-event history meant to be surfaced (audit view), or is it write-only telemetry?
- **`clearRecoveredActionStates` writes during a cached GET.** It performs a delete + log insert during the 5-minute-cached `GET /api/credit-control` read. Is mutating during a cached GET intended?
- **`autoResolveConfirmedProposalItems` runs per-request.** It fires on every list/create/patch **and** on every range search (`range-search.ts:116` calls `listActiveProposalHolds`), each doing a snapshot lookup + `future_session_blocks` join. Is per-request reconciliation cost acceptable at scale, or should it move to the sync pipeline / a cron?

---

## Ambiguous behavior & contracts

- **`revalidateTag("snapshot", { expire: 0 })` uses an undocumented second-arg form.** `run-wise-sync.ts:161` passes an object, but the vendored Next 16 docs document `revalidateTag`'s second arg as a cacheLife profile **string** (e.g. `'max'`). The `{ expire: 0 }` object form is not in the vendored docs — possibly a newer/undocumented signature or a misuse. Confirm it behaves as intended.
- **`cacheComponents` + `process.env` reads were not swept exhaustively.** With `cacheComponents` enabled, Next 16 normally forbids reading `process.env` outside a `connection()`/dynamic boundary. The search page calls `await connection()` before its cached helpers, but not every Server Component / route was audited for missing boundaries — a broader sweep could surface build-time-vs-runtime env-read pitfalls.
- **`src/middleware.ts` still ships on the edge runtime** rather than migrating to a `proxy` file. Whether that is a deliberate retained-edge-auth decision or a pending migration was not confirmed against any planning doc.
- **Duplicated cron-secret check.** `src/app/api/internal/sync-wise/route.ts:11-29` reimplements the constant-time comparison instead of importing `rejectInvalidCronSecret`/`getCronSecretStatus` from `src/lib/internal/cron-auth.ts`. Behaviorally identical, but a candidate for consolidation so REL-07 has one source of truth. (`student-promotions/july-1/route.ts` also rolls its own.)
- **API authorization model trusts any present session.** The search/compare/tutors/filters/leave/profiles/admin route handlers enforce only session-presence, not an explicit admin-role check — the allowlist is enforced at sign-in (`auth.ts` signIn callback) and via page-level `allowedPages` in middleware. Confirm that trusting any present session as "allowed" is the intended authorization model (e.g. a teacher session cannot reach `/api/admin/sync-wise` only because middleware blocks the *page*; the API route itself has no role guard). `GET /api/progress-tests` is session-gated via `requireProgressTestsSession()` but additionally admits teacher-role sessions with a scoped read-only view — there is no separate "teacher" tier in the public/admin/cron scheme.
- **`POST /api/progress-tests/book` parses `modality` but never forwards it.** `BookProgressTestSchema` accepts `'online'|'offline'` but `bookTest()` is never passed it. Dead input, or missed wiring that should affect the booked Wise session?
- **Payroll UI vs. server approval strictness mismatch.** The Approve button is disabled whenever `issueCount > 0` (any issue, dashboard line 390), but the server (`review/route.ts:29-33`) only blocks on the three expected-rate issue types — so a month with only missing/orphan-invoice issues is server-approvable yet UI-blocked. Align them, or intentional?
- **Payroll `financeOnly` includes `%payout%`** (`data.ts:108-116`) but the reconciliation inbound filter **excludes** it (`NOT ILIKE %payout%`, `reconciliation.ts:836`). Confirm the split is intentional, not drift.
- **`missing_tier` trigger is broader than "no tier tag".** `data.ts:316/:369` use `if (!tier || tier.normalizedTier === "Unassigned")`, so the flag also fires when a present-but-unrecognized tag normalizes to Unassigned. The user-facing message ("Tutor has no Wise tier tag for this payroll month") narrows it — consider tightening to "no *recognized* Wise tier tag".
- **Leave-requests mutating routes hand-parse `request.json()`** and swallow parse errors into `{}` instead of using Zod `.safeParse()`, diverging from the project's standard 4-step route contract (verified: no `zod`/`safeParse` import across `src/app/api/leave-requests/**` and `.../sync-leave-requests/`). Close the gap, or intentional?
- **Leave-requests dedup uses two different idempotency keys** — a per-run key `leave-requests:{syncRunId}:{recipient}` passed to the email sender (`sync.ts:329/338`) vs. a per-request key `leave-request:new:{requestId}:{recipient}` persisted on the notification row (the DB unique constraint, `sync.ts:355`). Confirm this is intended dedup semantics, not a refactor leftover.
- **Leave-requests rows are never deleted.** Rows are upserted by `source_row_number` and never removed when a source sheet row disappears or is reordered. Confirm stale rows are acceptable and that reordering can't mis-key existing rows. (`LEAVE_REQUESTS_SPREADSHEET_ID` also defaults to a hard-coded literal Sheet ID when unset — `config.ts:1-2`; confirm that fallback is intended for production.)
- **Leave-requests Wise cancellation is preview-only** (logs DELETE endpoints, imports no Wise client). Is a future flag-gated automated cancel intended (mirroring the LINE dry-run pattern), or is preview-only permanent?
- **AI-scheduler version stamps are hand-maintained.** `AI_SCHEDULER_VERSION` / `AI_SCHEDULER_PROMPT_VERSION` (`scheduler-service.ts:20-21`) are date-stamped by hand. Is there a process to bump them when the prompt/solver changes, or can they silently drift? (Run status is also stored as a plain `text` column, `schema.ts:1620`, constrained to `solved/needs_clarification/failed` only at the application layer — no DB enum guarantee.)
- **AI-scheduler model identifiers are beyond the assistant's knowledge cutoff.** `DEFAULT_AI_SCHEDULER_MODEL='gpt-5.4-mini'` (`scheduler.ts:8`), the `xhigh` reasoning-effort option, and eval reports citing `gpt-5.5` are taken verbatim from code and cannot be validated as real/current OpenAI model identifiers.
- **`/scheduler/metrics` renders only the correction slice.** `GET /api/ai-scheduler/metrics` returns scheduler + LINE + correction slices, but `SchedulerMetricsView` renders only correction. Intentional (surfaced elsewhere), or unfinished UI?
- **Several routes return library results verbatim or skip validation/guards** — documented as-is, confirm each contract:
  - `DELETE /api/payroll/adjustments/[adjustmentId]` has no try/catch, so a DB error yields the framework-default 500, not the `{error}` envelope used by the other four payroll handlers.
  - `GET /api/leave-requests/[requestId]` has no try/catch around `getLeaveRequestDetail`, unlike its sibling PATCH.
  - `GET /api/class-assignments/runs/[runId]/teacher-schedule` maps **all** errors (including "Assignment run not found") to 500, whereas sibling schedule-email routes map that same message to 404.
  - `GET /api/room-capacity/month` passes `startDate`/`endDate` to `getRoomCapacityMonth` with **no** YYYY-MM-DD or `start<=end` validation (unlike `/utilization`), so a malformed date likely surfaces as a 500 rather than a 400.
  - `POST /api/ai-scheduler/.../messages` returns `touchSchedulerConversationAfterMessage` as `conversation`, which can be `null` if the row vanished mid-request, still with HTTP 200.
  - `POST /api/ai-scheduler/.../feedback` performs no existence/FK validation on `messageId`/`conversationId`/`schedulerRunId`, so feedback rows can reference non-resolving ids.
  - `GET /api/ai-scheduler/conversations/[conversationId]` returns `source='manual'` with zeroed LINE rollups regardless of actual LINE linkage (only the list endpoint computes real rollups).

---

## Normalization & engine internals (verify before restating numbers)

- **Identity-cascade step count.** `AGENTS.md` calls `resolveIdentities` a "5-step cascade"; verify the exact step count against `src/lib/normalization/identity.ts` before restating a number in feature docs.
- **Modality derivation rules.** The "never guessed" modality claim is grounded in orchestrator behavior (starts `unresolved`, emits `conflict_model` issues), but the internal derivation rules in `deriveModality`/`detectSessionModalityConflict` (MOD-01) should be verified in the modality feature/reference docs. Note the two modality-evidence vocabularies are not unified: `deriveModality` (`modality.ts:38-63`) vs. the separate `ONLINE/ONSITE_SESSION_TYPES` sets in `compare.ts:5-7` (which add `scheduled`/`virtual` per MOD-UAT-01).
- **Promotion-gate stats may double-count.** The gate keys only on identity issues (`unresolvedRatio = identityIssues.length / max(groups.length,1) < 0.5`, `orchestrator.ts:472-476`), matching the "block at >50% unresolved identity" prose. But `snapshot_stats.unresolvedGroups = identityIssues.length` (`orchestrator.ts:463`) includes REL-03 collision issues and per-teacher solo-group issues, so "unresolved groups" in the stats display may exceed the count of distinct unresolvable identity groups. Confirm whether that double-counting is intended for the stats display.
- **Sync concurrency.** `createWiseClient()` hardcodes `maxConcurrency:15` (`client.ts:164`), while docstrings/`AGENTS.md` say "max 5, raised for production sync". 15 is the only production value; the constructor default 5 (`client.ts:48`) is used only when a client is constructed directly (e.g. tests). Confirm no other call site constructs a sync client with a different concurrency.
- **Availability horizon.** `fetchTeacherFullAvailability` defaults `horizonDays` to 180 ⇒ 26 windows (`fetchers.ts:61-93`), and the orchestrator calls it without an explicit horizon (`orchestrator.ts:176-180`), so 180 days / 26 windows is the effective production value. Flagged in case the horizon is meant to be configurable per environment.
- **`maxEventPages`** is route-clamped to 1–2000 but `runPayrollSync` defaults to 1000. Whether 2000 is ever required for a heavy month is unverified.
- **`adjustedRemaining` clamp nuance.** `adjustedRemaining = Math.max(0, currentRemaining - pendingDeduction)` (`packages.ts:323`) is clamped at 0, so pending deductions cannot by themselves drive it negative — the "fail-toward-action" framing is accurate (it lowers the balance toward/at zero), but the clamp is an undocumented nuance worth a note.
- **TODO/FIXME/HACK claim.** The GSD source asserts "zero TODO/FIXME/HACK in non-test source" but it was not independently re-verified in this pass; the handbook omits the claim to stay safe.

---

## Coverage gaps (tests)

Decide whether each is acceptable debt for the feature's maturity:

- **Credit control** — no DB-backed tests for `data.ts` (the GiST conflict path, orphan-bundle rollback, expiry sweep, auto-resolve join are only covered at the pure-helper level); no dedicated tests for the in-app `/api/credit-control/*` action/ownership/inactive handlers, the analytics scoring/queue/calendar builders, or the React components.
- **Proposals** — `src/lib/proposals/data.ts` has no DB-backed tests (same GiST/rollback/sweep/auto-resolve surface, helper-level only).
- **Sales dashboard** — the internal `sync-sales-dashboard` cron route has a `__tests__` directory but no executable cases at this revision (intended coverage: CRON_SECRET timing-safe check + admin-session POST fallback). `DEFAULT_SALES_SOURCES` hardcodes 14 production spreadsheet IDs (2025-04 .. 2026-05, `default-sources.ts:3-18`) — confirm these are canonical and whether the list is hand-extended monthly or superseded by manual source entry.
- **Tutor profiles** — no direct tests for `tutor-business-profiles.ts` (upsert/list/merge-on-read) or any of the four route handlers; only `buildTutorProfileImportPreview` is covered.
- **Data Health** — `getDataHealthDashboardPayload` has no dedicated test (see Operations).
- **Integration-test table hygiene** — the `truncateAll` list in `src/tests/integration/db-helper.ts` covers only ~19 core-snapshot tables and is not kept in sync with the full schema, so newer feature tables won't be reset between integration tests. A latent test-hygiene gap.

---

## Documentation cross-links to reconcile

These are doc-vs-doc drifts surfaced during the pass — not code defects, but they break navigation or citation:

- **`docs/reference/database/index.md` cited stale `schema.ts` line ranges** for the payroll tables (it listed `payrollSyncRuns` at 855-873 etc.; actual is `payrollSyncRuns` 995-1014 … `payrollRateRules` 1137-1155) and very likely all later-domain ranges; regenerate against the current file. The same downward drift (~58–140 lines) appears in several `erd-*.md` pages: `erd-tutor-profiles.md` (off ~140 — cites `tutorContacts` at 1057-1073, actual 1197-1212), `erd-line.md` (off ~140, now corrected to 1666-1894), `erd-classrooms.md` (off, now corrected), `erd-leave-requests.md` (banner + line citations now corrected to `schema.ts:1325-1466`), `erd-payroll.md`, and the proposals reference (`proposalBundles`/`proposalItems` cited at 1392/1404, actually 1532/1544). Confirm the same drift hasn't propagated into the remaining sibling ERD pages, and re-verify enum-definition line numbers in `enums.md` (the prior cites e.g. 134-140 / 19-23 are likely stale given the ~58-line shift). Still to verify: the un-corrected ERD pages' line citations against current `schema.ts`.
- **`docs/reference/api/index.md` Internal/Cron group link + newer-route enumeration.** ✅ Resolved (2026-06-05): the Internal/Cron heading now points to `internal-crons.md` (was `./internal.md`), and the index enumerates the newer routes — `sync-progress-tests` GET/POST, `progress-tests/admin-digest`, and `student-promotions/july-1` GET/POST.
- **Endpoint-count denominator.** ✅ Resolved (2026-06-05): `docs/reference/api/index.md` now pins **111 route files / 130 endpoints**, consistent with `README.md`. `GET /api/home/summary` is documented (Home / Ops Hub). The earlier-suspected `index.md`-only phantom `GET /api/line/scheduler-reviews/[reviewId]` was verified absent: `index.md` carries no such row — only the real `PATCH /…/[reviewId]` plus the legitimate `…/[reviewId]/context` and `…/[reviewId]/wise-actions` sub-routes (`src/app/api/line/scheduler-reviews/[reviewId]/route.ts` exports only `PATCH`). Nothing left to scrub.
- **Test-file count.** ✅ Resolved (2026-06-05): pinned to **162** (`153 *.test.ts` + `9 *.test.tsx`, of which 3 are `*.integration.test.ts`), the filesystem-verified figure.
- **Lib-module list omits real directories.** ✅ Partially resolved (2026-06-05): `src/lib/home/` (`summary.ts`) and `src/lib/navigation/` (`tools.ts`) are now documented via `docs/features/ops-hub.md` (and `src/lib/student-promotions/` via the student-promotions docs). If OPEN-QUESTIONS or another doc still maintains a canonical 25-module list, add these three so it reaches ~27 dirs.
- **Page count convention.** The authoritative figure is **17 routable `(app)` pages** — the `/` Ops Hub home + 15 nav-tool pages + the legacy `/compare` redirect; `/login` sits outside the `(app)` group. Confirm whether the convention should include `/login`.
- **`sales-dashboard` reference overwrite.** An existing `docs/reference/api/sales-dashboard.md` (dated 2026-06-04) was overwritten per the explicit write-to-this-path instruction; if the prior version had hand-curated content, confirm the overwrite was intended.

---

## Completeness review (automated)

A second automated pass reviewed the generated `docs/` tree purely for **structural completeness** — missing feature/reference pages, broken intra-doc links, count claims that contradict the filesystem, and footer coverage. Every item below was verified by opening the cited file. (Footer coverage is clean: all 56 handbook/feature/reference/operations `*.md` files carry the required `_Verified…_` footer.) These overlap with, but are scoped more narrowly than, the questions above.

- **Progress Tests `features/` doc.** ✅ Resolved (2026-06-05): `docs/features/progress-tests.md` now exists (257 lines), alongside the dedicated `docs/reference/api/progress-tests.md`, the prior reference coverage (tables in `erd-core.md` rows 24-31 and `database/index.md:95-102`), `src/app/(app)/progress-tests/page.tsx`, six public routes (`src/app/api/progress-tests/*`), two internal cron routes (`sync-progress-tests`, `progress-tests/admin-digest`), and the 8 `progress_test_*` tables (`schema.ts:2022-2205`). The meaning/purpose/flows layer is no longer missing.
- **Section-header links in `docs/reference/api/index.md`.** ✅ Resolved (2026-06-05): every section-header link in `index.md` now resolves (0 broken targets verified). Search/Compare/Tutors/Tutor-Profiles/Leave-Requests/Data-Health/Admin point into `misc.md#…`; Class-Assignments/Classrooms → `classrooms-and-assignments.md`; Internal/Cron → `internal-crons.md`; Progress Tests → the new `progress-tests.md`; the Auth heading is de-linked. The 9 headings that already had a real matching file (`ai-scheduler`, `line`, `student-promotions`, `sales-dashboard`, `credit-control`, `payroll`, `room-capacity`, `proposals`, `wise-activity`) still resolve.
- **Dedicated `reference/api/progress-tests.md`.** ✅ Resolved (2026-06-05): the dedicated page was created and the `index.md` Progress Tests heading now points to it (no longer relying on the `misc.md` endpoint listing alone).
- **API endpoint/route-file counts contradict the filesystem and each other.** ✅ Resolved (2026-06-05): `reference/api/index.md` now pins **111 route files / 130 endpoints** (54 GET + 60 POST + 12 PATCH + 4 DELETE), consistent with `README.md`. (The `database/index.md` "90 tables" claim was verified exact: `grep -c "= pgTable(" schema.ts` = 90.)
- **`GET /api/home/summary` is a real route that appears in no reference page.** ✅ Resolved (2026-06-05): the endpoint is now documented in `reference/api/index.md` (Home / Ops Hub), `reference/api/misc.md#home--ops-hub`, and the feature doc `docs/features/ops-hub.md`. Home Hub is committed (`d4fe6d3`).

Non-findings confirmed during this pass (so they are not re-raised): no handbook doc is a stub (shortest substantive page is `handbook/overview.md` at 41 lines, intentionally a tour); the three `(app)`-path links in `features/wise-activity-audit.md:37`, `features/room-capacity.md:47`, and `handbook/not-the-nextjs-you-know.md:167` are **valid** (a naive link scanner mis-flags them because the `)` in the `src/app/(app)` path truncates the parser — the targets resolve); and every foundation topic (architecture, data-flow, conventions, glossary, not-the-nextjs-you-know, overview) has a present, footer-bearing page.

---

_Verified against HEAD `d4fe6d3` on 2026-06-05._
